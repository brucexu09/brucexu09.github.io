<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://brucexu09.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://brucexu09.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-05-17T07:43:12+00:00</updated><id>https://brucexu09.github.io/feed.xml</id><title type="html">blank</title><subtitle>Boxun Xu — UCSB Ph.D. candidate working on efficient generative modeling and HW/algorithm co-design. </subtitle><entry><title type="html">Post-Training 面试速记: 从分词到对齐</title><link href="https://brucexu09.github.io/blog/2026/post-training-interview-notes/" rel="alternate" type="text/html" title="Post-Training 面试速记: 从分词到对齐"/><published>2026-05-17T06:00:00+00:00</published><updated>2026-05-17T06:00:00+00:00</updated><id>https://brucexu09.github.io/blog/2026/post-training-interview-notes</id><content type="html" xml:base="https://brucexu09.github.io/blog/2026/post-training-interview-notes/"><![CDATA[<blockquote> <p>整理自 Day3 复习笔记。面向”白纸能写出来 + 能讲明白”的面试目标,每节固定四块:<strong>Key Insight → 实现层次图 → 代码 → 高频追问</strong>。</p> </blockquote> <hr/> <h2 id="目录">目录</h2> <ol> <li><a href="#1-minhash--海量文档去重">MinHash — 海量文档去重</a></li> <li><a href="#2-tokenizer-三件套-bpe--wordpiece--unigram">Tokenizer 三件套: BPE / WordPiece / Unigram</a></li> <li><a href="#3-反向传播--softmax--attention-手推">反向传播 — Softmax + Attention 手推</a></li> <li><a href="#4-memory-efficient-attention--online-softmax--flashattention">Memory-Efficient Attention — Online Softmax → FlashAttention</a></li> <li><a href="#5-reward-model--bradley-terry-pairwise">Reward Model — Bradley-Terry pairwise</a></li> <li><a href="#6-rl-fine-tuning--策略梯度--组内归一化">RL Fine-tuning — 策略梯度 + 组内归一化</a></li> <li><a href="#7-gae--广义优势估计">GAE — 广义优势估计</a></li> <li><a href="#8-ppo--clipped-surrogate--kl">PPO — Clipped Surrogate + KL</a></li> <li><a href="#9-grpo--ppo-砍掉-value-网络">GRPO — PPO 砍掉 Value 网络</a></li> <li><a href="#10-dpo--把-rm--rl-合成一个分类损失">DPO — 把 RM + RL 合成一个分类损失</a></li> <li><a href="#11-一图全景-四种对齐算法对比">一图全景: 四种对齐算法对比</a></li> </ol> <hr/> <h2 id="1-minhash--海量文档去重">1. MinHash — 海量文档去重</h2> <blockquote> <p><strong>Key Insight</strong>: 对一个随机 hash <code class="language-plaintext highlighter-rouge">h</code>,<strong><code class="language-plaintext highlighter-rouge">P(min h(A) == min h(B)) = J(A, B)</code></strong>。 用 <code class="language-plaintext highlighter-rouge">num_perm</code> 个独立 hash 取最小,签名向量逐位相等的比例就是 Jaccard 的无偏估计。</p> </blockquote> <h3 id="为什么需要">为什么需要</h3> <p>预训练前必须去重: 重复样本会让模型<strong>记忆</strong>而不是<strong>泛化</strong>,还会让评测集泄露到训练集。 两两算 Jaccard 是 O(N²),百万文档跑不动。MinHash 把”集合比较”压缩到”向量比较”。</p> <h3 id="实现层次">实现层次</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>                                ┌──────────────────────────┐
原始文档(string)               │  create_shingles(text, k) │   预处理
   │                            │  滑动 k 字符窗口          │
   └──────────────────────────►│  → set of strings        │
                                └─────────────┬────────────┘
                                              ▼
                                       shingles set
                                              │
                                ┌─────────────▼────────────────────┐
                                │  compute_signature(shingles)     │   签名生成
                                │  for shingle in document:        │
                                │    for i, (a,b) in hash_funcs:   │
                                │      h = (a*x + b) mod prime     │
                                │      sig[i] = min(sig[i], h)     │
                                └─────────────┬────────────────────┘
                                              ▼
                                  signature [num_perm]
                                              │
                                ┌─────────────▼─────────────┐
                                │  similarity(sig1, sig2)    │   相似度
                                │  Σ(sig1[i]==sig2[i])/N    │
                                │  ≈ Jaccard(A, B)          │
                                └────────────────────────────┘

工程外壳 (在 MinHash 类外):
   LSH 分桶 → 桶内两两比签名 → Union-Find 聚簇 → 每簇留一篇
</code></pre></div></div> <h3 id="代码骨架">代码骨架</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">MinHash</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">num_perm</span><span class="o">=</span><span class="mi">128</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">num_perm</span> <span class="o">=</span> <span class="n">num_perm</span>
        <span class="n">self</span><span class="p">.</span><span class="n">prime</span> <span class="o">=</span> <span class="mi">2147483647</span>
        <span class="n">self</span><span class="p">.</span><span class="n">hash_funcs</span> <span class="o">=</span> <span class="p">[]</span>
        <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">num_perm</span><span class="p">):</span>
            <span class="n">seed</span> <span class="o">=</span> <span class="n">hashlib</span><span class="p">.</span><span class="nf">sha256</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">minhash_</span><span class="si">{</span><span class="n">i</span><span class="si">}</span><span class="sh">"</span><span class="p">.</span><span class="nf">encode</span><span class="p">()).</span><span class="nf">digest</span><span class="p">()</span>
            <span class="n">a</span> <span class="o">=</span> <span class="nb">int</span><span class="p">.</span><span class="nf">from_bytes</span><span class="p">(</span><span class="n">seed</span><span class="p">[:</span><span class="mi">4</span><span class="p">],</span> <span class="sh">"</span><span class="s">big</span><span class="sh">"</span><span class="p">)</span> <span class="o">%</span> <span class="n">self</span><span class="p">.</span><span class="n">prime</span> <span class="ow">or</span> <span class="mi">1</span>
            <span class="n">b</span> <span class="o">=</span> <span class="nb">int</span><span class="p">.</span><span class="nf">from_bytes</span><span class="p">(</span><span class="n">seed</span><span class="p">[</span><span class="mi">4</span><span class="p">:</span><span class="mi">8</span><span class="p">],</span> <span class="sh">"</span><span class="s">big</span><span class="sh">"</span><span class="p">)</span> <span class="o">%</span> <span class="n">self</span><span class="p">.</span><span class="n">prime</span>
            <span class="n">self</span><span class="p">.</span><span class="n">hash_funcs</span><span class="p">.</span><span class="nf">append</span><span class="p">((</span><span class="n">a</span><span class="p">,</span> <span class="n">b</span><span class="p">))</span>           <span class="c1"># h(x) = (a*x + b) mod p
</span>
    <span class="k">def</span> <span class="nf">compute_signature</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">document</span><span class="p">:</span> <span class="nb">set</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">list</span><span class="p">:</span>
        <span class="n">sig</span> <span class="o">=</span> <span class="p">[</span><span class="nf">float</span><span class="p">(</span><span class="sh">"</span><span class="s">inf</span><span class="sh">"</span><span class="p">)]</span> <span class="o">*</span> <span class="n">self</span><span class="p">.</span><span class="n">num_perm</span>
        <span class="k">for</span> <span class="n">shingle</span> <span class="ow">in</span> <span class="n">document</span><span class="p">:</span>
            <span class="k">for</span> <span class="n">i</span><span class="p">,</span> <span class="p">(</span><span class="n">a</span><span class="p">,</span> <span class="n">b</span><span class="p">)</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">hash_funcs</span><span class="p">):</span>
                <span class="n">sig</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">=</span> <span class="nf">min</span><span class="p">(</span><span class="n">sig</span><span class="p">[</span><span class="n">i</span><span class="p">],</span> <span class="n">self</span><span class="p">.</span><span class="nf">_compute_hash</span><span class="p">(</span><span class="n">shingle</span><span class="p">,</span> <span class="n">a</span><span class="p">,</span> <span class="n">b</span><span class="p">))</span>
        <span class="k">return</span> <span class="n">sig</span>

    <span class="k">def</span> <span class="nf">similarity</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">sig1</span><span class="p">,</span> <span class="n">sig2</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">float</span><span class="p">:</span>
        <span class="k">return</span> <span class="nf">sum</span><span class="p">(</span><span class="n">x</span> <span class="o">==</span> <span class="n">y</span> <span class="k">for</span> <span class="n">x</span><span class="p">,</span> <span class="n">y</span> <span class="ow">in</span> <span class="nf">zip</span><span class="p">(</span><span class="n">sig1</span><span class="p">,</span> <span class="n">sig2</span><span class="p">))</span> <span class="o">/</span> <span class="nf">len</span><span class="p">(</span><span class="n">sig1</span><span class="p">)</span>
</code></pre></div></div> <h3 id="关键定理的证明梗概">关键定理的证明梗概</h3> <p><strong>命题</strong>: 对一个<strong>随机排列</strong> π,<code class="language-plaintext highlighter-rouge">P(min π(A) == min π(B)) = J(A, B) = |A∩B| / |A∪B|</code>。</p> <p><strong>证明</strong>:</p> <ul> <li>令 <code class="language-plaintext highlighter-rouge">U = A ∪ B</code>,在 U 上看 π 的最小值。π 在 U 上是均匀随机的全排列。</li> <li>“min π(A) == min π(B)” ⟺ U 中最小值同时属于 A 和 B ⟺ U 中最小值属于 <code class="language-plaintext highlighter-rouge">A ∩ B</code>。</li> <li>由对称性,U 中任一元素被选为最小的概率均为 <code class="language-plaintext highlighter-rouge">1/|U|</code>。</li> <li>所以概率 = <code class="language-plaintext highlighter-rouge">|A∩B| / |U| = |A∩B| / |A∪B| = J(A, B)</code>。</li> </ul> <p><strong>工程化</strong>: 真实排列 π 太贵,用 <strong>universal hash family</strong> <code class="language-plaintext highlighter-rouge">h(x) = (a·x + b) mod p</code>(<code class="language-plaintext highlighter-rouge">p</code> 是大素数,<code class="language-plaintext highlighter-rouge">a, b</code> 随机) 近似一个随机排列 —— 工程上够好。</p> <h3 id="估计精度-num_perm-怎么选">估计精度 (num_perm 怎么选)</h3> <p><code class="language-plaintext highlighter-rouge">num_perm = K</code> 个独立 hash 各自取最小,签名等值率 <code class="language-plaintext highlighter-rouge">Ĵ = X / K</code>,其中 <code class="language-plaintext highlighter-rouge">X ~ Binomial(K, J)</code>。</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>E[Ĵ] = J              ← 无偏
Var[Ĵ] = J(1-J) / K   ← 方差随 K 线性下降
</code></pre></div></div> <table> <thead> <tr> <th>K</th> <th>标准差 (J=0.5)</th> <th>95% CI 宽度</th> </tr> </thead> <tbody> <tr> <td>64</td> <td>±6.3%</td> <td>~12.5%</td> </tr> <tr> <td>128</td> <td>±4.4%</td> <td>~8.7%</td> </tr> <tr> <td>256</td> <td>±3.1%</td> <td>~6.2%</td> </tr> <tr> <td>1024</td> <td>±1.6%</td> <td>~3.1%</td> </tr> </tbody> </table> <p>工业经验: <strong>128-256 起步</strong>,严格去重(LSH 阈值 0.7+)可以加到 512。</p> <h3 id="lsh-分桶-把-on-压成近线性">LSH 分桶: 把 O(N²) 压成近线性</h3> <p>直接两两比签名仍是 <code class="language-plaintext highlighter-rouge">O(N²·K)</code>。<strong>LSH (Locality-Sensitive Hashing) banding</strong> 解法:</p> <p>把 K 个 hash 切成 <code class="language-plaintext highlighter-rouge">b</code> 个 band,每 band 包含 <code class="language-plaintext highlighter-rouge">r</code> 行 (<code class="language-plaintext highlighter-rouge">b·r = K</code>)。 两文档在<strong>至少一个 band 内 r 行全等</strong> → 进入同桶 → 候选对。</p> <p><strong>S 曲线</strong>: 两文档 Jaccard 为 <code class="language-plaintext highlighter-rouge">J</code>,被选为候选对的概率:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>P(候选) = 1 - (1 - J^r)^b
</code></pre></div></div> <table> <thead> <tr> <th>(b, r)</th> <th>J=0.5</th> <th>J=0.7</th> <th>J=0.8</th> <th>J=0.9</th> </tr> </thead> <tbody> <tr> <td>(20, 5)</td> <td>0.47</td> <td>0.97</td> <td>1.00</td> <td>1.00</td> </tr> <tr> <td>(50, 4)</td> <td>0.95</td> <td>1.00</td> <td>1.00</td> <td>1.00</td> </tr> <tr> <td>(25, 10)</td> <td>0.02</td> <td>0.36</td> <td>0.82</td> <td>1.00</td> </tr> </tbody> </table> <p><code class="language-plaintext highlighter-rouge">(b, r)</code> 控制 S 曲线的<strong>陡峭程度和阈值位置</strong>: 想滤掉 J&lt;0.7,选大 <code class="language-plaintext highlighter-rouge">r</code> 小 <code class="language-plaintext highlighter-rouge">b</code>;想宽松些,反之。</p> <h3 id="完整去重-pipeline">完整去重 pipeline</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">dedupe_corpus</span><span class="p">(</span><span class="n">documents</span><span class="p">):</span>
    <span class="n">mh</span> <span class="o">=</span> <span class="nc">MinHash</span><span class="p">(</span><span class="n">num_perm</span><span class="o">=</span><span class="mi">256</span><span class="p">)</span>
    <span class="n">signatures</span> <span class="o">=</span> <span class="p">[</span><span class="n">mh</span><span class="p">.</span><span class="nf">compute_signature</span><span class="p">(</span><span class="nf">shingles</span><span class="p">(</span><span class="n">d</span><span class="p">))</span> <span class="k">for</span> <span class="n">d</span> <span class="ow">in</span> <span class="n">documents</span><span class="p">]</span>

    <span class="c1"># LSH banding 分桶
</span>    <span class="n">buckets</span> <span class="o">=</span> <span class="nf">defaultdict</span><span class="p">(</span><span class="nb">list</span><span class="p">)</span>
    <span class="n">b</span><span class="p">,</span> <span class="n">r</span> <span class="o">=</span> <span class="mi">50</span><span class="p">,</span> <span class="mi">4</span>   <span class="c1"># 50 bands × 4 rows = 200 (用前 200 维)
</span>    <span class="k">for</span> <span class="n">idx</span><span class="p">,</span> <span class="n">sig</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">signatures</span><span class="p">):</span>
        <span class="k">for</span> <span class="n">band</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">b</span><span class="p">):</span>
            <span class="n">key</span> <span class="o">=</span> <span class="p">(</span><span class="n">band</span><span class="p">,</span> <span class="nf">tuple</span><span class="p">(</span><span class="n">sig</span><span class="p">[</span><span class="n">band</span><span class="o">*</span><span class="n">r</span> <span class="p">:</span> <span class="p">(</span><span class="n">band</span><span class="o">+</span><span class="mi">1</span><span class="p">)</span><span class="o">*</span><span class="n">r</span><span class="p">]))</span>
            <span class="n">buckets</span><span class="p">[</span><span class="n">key</span><span class="p">].</span><span class="nf">append</span><span class="p">(</span><span class="n">idx</span><span class="p">)</span>

    <span class="c1"># Union-Find 聚簇
</span>    <span class="n">uf</span> <span class="o">=</span> <span class="nc">UnionFind</span><span class="p">(</span><span class="nf">len</span><span class="p">(</span><span class="n">documents</span><span class="p">))</span>
    <span class="k">for</span> <span class="n">bucket</span> <span class="ow">in</span> <span class="n">buckets</span><span class="p">.</span><span class="nf">values</span><span class="p">():</span>
        <span class="k">if</span> <span class="nf">len</span><span class="p">(</span><span class="n">bucket</span><span class="p">)</span> <span class="o">&gt;</span> <span class="mi">1</span><span class="p">:</span>
            <span class="k">for</span> <span class="n">j</span> <span class="ow">in</span> <span class="n">bucket</span><span class="p">[</span><span class="mi">1</span><span class="p">:]:</span>
                <span class="k">if</span> <span class="n">mh</span><span class="p">.</span><span class="nf">similarity</span><span class="p">(</span><span class="n">signatures</span><span class="p">[</span><span class="n">bucket</span><span class="p">[</span><span class="mi">0</span><span class="p">]],</span> <span class="n">signatures</span><span class="p">[</span><span class="n">j</span><span class="p">])</span> <span class="o">&gt;</span> <span class="mf">0.8</span><span class="p">:</span>
                    <span class="n">uf</span><span class="p">.</span><span class="nf">union</span><span class="p">(</span><span class="n">bucket</span><span class="p">[</span><span class="mi">0</span><span class="p">],</span> <span class="n">j</span><span class="p">)</span>

    <span class="c1"># 每簇留一篇
</span>    <span class="k">return</span> <span class="p">[</span><span class="n">documents</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="n">uf</span><span class="p">.</span><span class="nf">representatives</span><span class="p">()]</span>
</code></pre></div></div> <p>时间复杂度: 签名 <code class="language-plaintext highlighter-rouge">O(N·K·|shingles|)</code> + 分桶 <code class="language-plaintext highlighter-rouge">O(N·b)</code> + 桶内验证 <code class="language-plaintext highlighter-rouge">O(候选数 · K)</code> ≈ <strong>近线性</strong>。</p> <h3 id="数值例子">数值例子</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>d1 = "The quick brown fox jumps over the lazy dog."
d2 = "The quick brown fox jumps over the lazy dog!"   # 只差一个标点
d3 = "Completely unrelated sentence about cats."

sim(d1, d2) ≈ 0.97   ← 近重复 (会被聚到同一簇)
sim(d1, d3) ≈ 0.00   ← 不相关
</code></pre></div></div> <h3 id="minhash-vs-simhash-vs-嵌入">MinHash vs SimHash vs 嵌入</h3> <table> <thead> <tr> <th>方法</th> <th>估计什么</th> <th>输入要求</th> <th>典型用途</th> </tr> </thead> <tbody> <tr> <td><strong>MinHash</strong></td> <td>Jaccard (集合)</td> <td>shingle 集 (字符 / 词 n-gram)</td> <td>文档级去重</td> </tr> <tr> <td><strong>SimHash</strong></td> <td>Cosine (向量)</td> <td>特征向量 (TF-IDF 等)</td> <td>网页指纹、相似搜索</td> </tr> <tr> <td><strong>Embedding (BGE, OpenAI)</strong></td> <td>Cosine in 语义空间</td> <td>文本 → 向量</td> <td>语义去重、检索</td> </tr> <tr> <td><strong>Bloom Filter</strong></td> <td>完全相等</td> <td>任意</td> <td>精确查重</td> </tr> </tbody> </table> <p>LLM 预训练去重: <strong>先 MinHash + LSH 粗筛</strong>,再 embedding 模型在候选对上<strong>精筛</strong>(可选)。 RedPajama, FineWeb, SlimPajama 全都用这套。</p> <h3 id="高频追问">高频追问</h3> <table> <thead> <tr> <th>Q</th> <th>A</th> </tr> </thead> <tbody> <tr> <td>为什么 min-hash 碰撞概率 = Jaccard?</td> <td>见上面证明:U=A∪B 中的最小元素均匀分布,同时属于交集的概率 = |A∩B|/|A∪B|</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">num_perm</code> 怎么选?</td> <td>标准差 ≈ √(J(1-J)/K);128 ±4.4%,256 ±3.1%。代价是签名内存和比较时间</td> </tr> <tr> <td>MinHash 用什么 hash?</td> <td>工程上用 <code class="language-plaintext highlighter-rouge">(a·x + b) mod p</code> (universal hash);近似随机排列。<code class="language-plaintext highlighter-rouge">p</code> 通常 <code class="language-plaintext highlighter-rouge">2^31 - 1</code></td> </tr> <tr> <td>LSH banding 的 (b, r) 怎么调?</td> <td>想 J&gt;τ 的对都被召回 → 让 S 曲线在 τ 附近过 0.5: <code class="language-plaintext highlighter-rouge">τ ≈ (1/b)^(1/r)</code></td> </tr> <tr> <td>为什么 shingle 用 5-gram 字符?</td> <td>平衡: 太短(字符)→ 无信息;太长(句子)→ 微小差异就 miss。5-7 字符是经验最优</td> </tr> <tr> <td>MinHash vs SimHash?</td> <td>MinHash 估 <strong>Jaccard</strong>(集合相似),适合 shingle 集;SimHash 估 <strong>cosine</strong>(向量相似),适合特征向量</td> </tr> <tr> <td>为什么去重对 LLM 重要?</td> <td>(1) 重复 → 记忆而非泛化 (2) 评测数据泄露 (3) 浪费算力。GPT-3, LLaMA, Chinchilla 论文都强调</td> </tr> <tr> <td>工业级 dedup 流程?</td> <td>文档级 MinHash+LSH → 句子级 SimHash/Bloom → 评测集污染检查 (n-gram 重叠);三层都过</td> </tr> </tbody> </table> <hr/> <h2 id="2-tokenizer-三件套-bpe--wordpiece--unigram">2. Tokenizer 三件套: BPE / WordPiece / Unigram</h2> <blockquote> <p><strong>Key Insight</strong>: 都是 subword,<strong>唯一区别在”用什么准则合并/删除 token”</strong>。 BPE = 频率;WordPiece = 互信息;Unigram = 删除后 likelihood 下降最小。</p> </blockquote> <h3 id="为什么需要-subword">为什么需要 subword</h3> <table> <thead> <tr> <th>方案</th> <th>问题</th> </tr> </thead> <tbody> <tr> <td>Word-level</td> <td>词表巨大(50万+),OOV 严重</td> </tr> <tr> <td>Char-level</td> <td>序列太长,模型学不到语义</td> </tr> <tr> <td><strong>Subword(BPE/WP/Uni)</strong></td> <td>词表 30k~100k,常用词整 token,稀有词降到子词</td> </tr> </tbody> </table> <h3 id="实现层次-以-bpe-为例">实现层次 (以 BPE 为例)</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>══════════ 训练阶段 (Training) ══════════

原始 corpus (list[str])
   │
   ▼  get_word_freqs(corpus)            按空格切词 + 计数
{"low": 5, "lower": 2, ...}
   │
   ▼  get_splits(word)                  拆字符 + &lt;/w&gt;
{"low": [l,o,w,&lt;/w&gt;], "lower": [l,o,w,e,r,&lt;/w&gt;], ...}
   │
   ▼  ╔═════════════════════════════════════════════╗
      ║  loop merges_needed 次:                      ║
      ║    pairs = get_stats(splits)                ║
      ║    best  = argmax(pairs)        # 频率最高  ║
      ║    splits = merge_pair(splits, best)        ║
      ║    merges.append(best)                      ║
      ╚═════════════════════════════════════════════╝
   │
   ▼
final_vocab + merges (有序列表) + merges_lookup (pair→优先级)


══════════ 编码阶段 (Encoding) ══════════

word
   │
   ▼  get_splits(word)
[c1, c2, ..., &lt;/w&gt;]
   │
   ▼  ╔═════════════════════════════════════════════╗
      ║  apply_bpe:                                  ║
      ║    while True:                              ║
      ║      在相邻 pair 里找 merges_lookup          ║
      ║      优先级最高 (索引最小) 的那个            ║
      ║      没有 → break;有 → 合并所有出现位置     ║
      ╚═════════════════════════════════════════════╝
   │
   ▼  vocab 查询
[id1, id2, ...]
</code></pre></div></div> <h3 id="21-bpe--频率最高的-pair-合并">2.1 BPE — 频率最高的 pair 合并</h3> <p><strong>完整跑一遍</strong> <code class="language-plaintext highlighter-rouge">["low", "lower", "lowest"]</code>:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>init splits:
  low:    [l, o, w, &lt;/w&gt;]
  lower:  [l, o, w, e, r, &lt;/w&gt;]
  lowest: [l, o, w, e, s, t, &lt;/w&gt;]

第1轮 pair 频率: (l,o)=3, (o,w)=3, (w,e)=2, ...
  → 合并 (l, o)
  → splits 里 l,o 相邻处全变 lo

第2轮: (lo,w)=3 ← 最高 → 合并

第3轮: (low,e)=2 ← 最高 → 合并

... 直到攒够 vocab_size 个 token
</code></pre></div></div> <p><strong>编码代码</strong>:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">apply_bpe</span><span class="p">(</span><span class="n">word</span><span class="p">,</span> <span class="n">merges_lookup</span><span class="p">):</span>
    <span class="n">splits</span> <span class="o">=</span> <span class="nf">list</span><span class="p">(</span><span class="n">word</span><span class="p">)</span> <span class="o">+</span> <span class="p">[</span><span class="sh">"</span><span class="s">&lt;/w&gt;</span><span class="sh">"</span><span class="p">]</span>
    <span class="k">while</span> <span class="bp">True</span><span class="p">:</span>
        <span class="c1"># 找当前相邻 pair 里"优先级最高"(最早学到)的
</span>        <span class="n">best_pair</span><span class="p">,</span> <span class="n">best_pri</span> <span class="o">=</span> <span class="bp">None</span><span class="p">,</span> <span class="nf">float</span><span class="p">(</span><span class="sh">"</span><span class="s">inf</span><span class="sh">"</span><span class="p">)</span>
        <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="nf">len</span><span class="p">(</span><span class="n">splits</span><span class="p">)</span> <span class="o">-</span> <span class="mi">1</span><span class="p">):</span>
            <span class="n">pri</span> <span class="o">=</span> <span class="n">merges_lookup</span><span class="p">.</span><span class="nf">get</span><span class="p">((</span><span class="n">splits</span><span class="p">[</span><span class="n">i</span><span class="p">],</span> <span class="n">splits</span><span class="p">[</span><span class="n">i</span><span class="o">+</span><span class="mi">1</span><span class="p">]))</span>
            <span class="k">if</span> <span class="n">pri</span> <span class="ow">is</span> <span class="ow">not</span> <span class="bp">None</span> <span class="ow">and</span> <span class="n">pri</span> <span class="o">&lt;</span> <span class="n">best_pri</span><span class="p">:</span>
                <span class="n">best_pair</span><span class="p">,</span> <span class="n">best_pri</span> <span class="o">=</span> <span class="p">(</span><span class="n">splits</span><span class="p">[</span><span class="n">i</span><span class="p">],</span> <span class="n">splits</span><span class="p">[</span><span class="n">i</span><span class="o">+</span><span class="mi">1</span><span class="p">]),</span> <span class="n">pri</span>
        <span class="k">if</span> <span class="n">best_pair</span> <span class="ow">is</span> <span class="bp">None</span><span class="p">:</span>
            <span class="k">break</span>
        <span class="n">splits</span> <span class="o">=</span> <span class="nf">merge_in_place</span><span class="p">(</span><span class="n">splits</span><span class="p">,</span> <span class="n">best_pair</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">splits</span>
</code></pre></div></div> <p><strong>关键</strong>: 编码必须<strong>严格按训练时的合并顺序</strong>重放,否则同一个词可能被切成不同 token,模型会崩。</p> <h3 id="22-wordpiece-bert--互信息最大的-pair-合并">2.2 WordPiece (BERT) — 互信息最大的 pair 合并</h3> <p><strong>唯一的区别</strong>: 合并准则换成互信息</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>BPE:        argmax  count(A, B)
WordPiece:  argmax  count(A, B) / (count(A) × count(B))
</code></pre></div></div> <p>直觉: WordPiece 选「A 和 B 真的经常一起出现」而非「A 自己就常见」。</p> <p><strong>词边界标记</strong>: 用 <code class="language-plaintext highlighter-rouge">##</code> 表示”接续片段”:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>"playing" → ["play", "##ing"]
"unhappiness" → ["un", "##happy", "##ness"]
</code></pre></div></div> <p><strong>OOV 处理</strong>: 拼不出的输出 <code class="language-plaintext highlighter-rouge">[UNK]</code>(BPE 会用字符兜底,WP 不会 → 更脆)。</p> <h3 id="23-unigram-lm-t5-sentencepiece-默认--反向删词表">2.3 Unigram LM (T5, SentencePiece 默认) — 反向删词表</h3> <p><strong>自上而下删</strong>:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. 启发式构造一个超大候选词表(100万)
2. 训练 Unigram 语言模型: p(token)
3. 对每个词,Viterbi 找概率最高的切分
4. 计算每个 token 的"删除损失"(整个语料 likelihood 下降)
5. 删损失最小的 10%
6. 重复 2-5,直到词表 = vocab_size
</code></pre></div></div> <p><strong>关键</strong>: 一个词有<strong>多种切分方式</strong>,选概率最高的(或随机采样做数据增强)。</p> <h3 id="24-byte-level-bpe-gpt-234-llama-3">2.4 Byte-level BPE (GPT-2/3/4, LLaMA-3)</h3> <p><strong>不在字符层,在 UTF-8 字节层做 BPE</strong>:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>"你好" → UTF-8 字节 [228, 189, 160, 229, 165, 189] → 当作 6 个初始 token
</code></pre></div></div> <ul> <li>初始词表固定 <strong>256</strong>(所有字节值)</li> <li>任何 Unicode 都能编码 → <strong>永远不会 OOV</strong></li> <li>GPT-2 用 Ġ 等可见字符替代不可见字节(仅显示,不影响算法)</li> </ul> <h3 id="三者对比">三者对比</h3> <table> <thead> <tr> <th>维度</th> <th>BPE</th> <th>WordPiece</th> <th>Unigram</th> </tr> </thead> <tbody> <tr> <td>训练方向</td> <td>自下而上加</td> <td>自下而上加</td> <td>自上而下删</td> </tr> <tr> <td>合并/删除准则</td> <td>频率最高</td> <td>互信息最大</td> <td>删除后 likelihood 下降最小</td> </tr> <tr> <td>词边界标记</td> <td><code class="language-plaintext highlighter-rouge">&lt;/w&gt;</code> 后缀</td> <td><code class="language-plaintext highlighter-rouge">##</code> 续接前缀</td> <td><code class="language-plaintext highlighter-rouge">▁</code> 空格前缀(SP)</td> </tr> <tr> <td>编码</td> <td>按 merges 贪心</td> <td>最长前缀贪心</td> <td>Viterbi 最优切分</td> </tr> <tr> <td>OOV 处理</td> <td>字符级兜底</td> <td><code class="language-plaintext highlighter-rouge">[UNK]</code></td> <td>字符级兜底</td> </tr> <tr> <td>代表模型</td> <td>GPT, LLaMA, RoBERTa</td> <td>BERT, DistilBERT</td> <td>T5, ALBERT, XLNet</td> </tr> </tbody> </table> <p><strong>坑题</strong>: <strong>SentencePiece 不是算法,是 Google 的库</strong>,默认 Unigram,也支持 BPE。LLaMA 用 SentencePiece-BPE 模式。</p> <h3 id="高频追问-1">高频追问</h3> <table> <thead> <tr> <th>Q</th> <th>A</th> </tr> </thead> <tbody> <tr> <td>为什么 BPE 需要 <code class="language-plaintext highlighter-rouge">&lt;/w&gt;</code>?</td> <td>区分”词尾 e”(<code class="language-plaintext highlighter-rouge">est&lt;/w&gt;</code>)和”词中 e”,避免错误合并跨词边界</td> </tr> <tr> <td>编码是确定性的吗?</td> <td>BPE/WP 是;Unigram 默认是(选概率最高),训练时可采样做正则</td> </tr> <tr> <td>同一个词不同上下文切分会变吗?</td> <td>不会(BPE/WP);Unigram 在 subword regularization 下会</td> </tr> <tr> <td>为什么 GPT 用 byte-level?</td> <td>多语言通用 + 代码符号 + 永远无 OOV</td> </tr> <tr> <td>WordPiece 的 <code class="language-plaintext highlighter-rouge">[UNK]</code> 怎么来的?</td> <td>输入有训练时没见过的字符,且无法拼回 vocab 子串</td> </tr> </tbody> </table> <hr/> <h2 id="3-反向传播--softmax--attention-手推">3. 反向传播 — Softmax + Attention 手推</h2> <blockquote> <p><strong>Key Insight</strong>: Softmax 的 Jacobian = <code class="language-plaintext highlighter-rouge">diag(s) - s·sᵀ</code>,向量化后 = <code class="language-plaintext highlighter-rouge">s ⊙ (g − g·s)</code>。 Attention 反向 = 把前向的 4 个矩阵乘<strong>逆序求转置</strong>,中间夹一个 softmax_backward。</p> </blockquote> <h3 id="实现层次-1">实现层次</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>══════════ Forward ══════════
                                  ┌─ Q [N, D]
              S = Q·Kᵀ/√D         │
   ────────────────────────────── │  ↓ matmul + scale
              A = softmax(S)      ├─ K [M, D]
              out = A·V           │  ↓ softmax
   ────────────────────────────── │  ↓ matmul
                                  │
                                  └─ V [M, D]


══════════ Backward (反向链) ══════════

grad  ← upstream gradient on out
  │
  ├─►  dV = Aᵀ · grad                      [M, D]
  │           ▲
  │           └─ out = A·V → ∂out/∂V = Aᵀ
  │
  ├─►  dA = grad · Vᵀ                      [N, M]
  │           ▲
  │           └─ out = A·V → ∂out/∂A 走 V
  │
  ├─►  dS = softmax_backward(A, dA)        [N, M]
  │           │
  │           │  对每一行 s_i = A[i]:
  │           │  J = diag(s_i) - s_i·s_iᵀ
  │           │  向量化: s_i ⊙ (dA[i] - (dA[i]·s_i))
  │           ▼
  ├─►  dQ = dS · K / √D                    [N, D]
  │
  └─►  dK = dSᵀ · Q / √D                   [M, D]
              ▲
              └─ S 的列 ↔ K 的行,所以要转置
</code></pre></div></div> <h3 id="softmax-jacobian-的完整推导必背">Softmax Jacobian 的完整推导(必背)</h3> <p>对一行 <code class="language-plaintext highlighter-rouge">s = softmax(z)</code>,即 <code class="language-plaintext highlighter-rouge">s_i = exp(z_i) / Σ_k exp(z_k)</code>。</p> <p><strong>Case 1</strong>: <code class="language-plaintext highlighter-rouge">i == j</code></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>∂s_i/∂z_i = [exp(z_i) · Σ_k exp(z_k) - exp(z_i) · exp(z_i)] / (Σ_k exp(z_k))²
          = s_i - s_i² = s_i · (1 - s_i)
</code></pre></div></div> <p><strong>Case 2</strong>: <code class="language-plaintext highlighter-rouge">i ≠ j</code></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>∂s_i/∂z_j = [0 · Σ_k exp(z_k) - exp(z_i) · exp(z_j)] / (Σ_k exp(z_k))²
          = -s_i · s_j
</code></pre></div></div> <p><strong>合并</strong> 用 Kronecker δ:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>∂s_i/∂z_j = s_i · (δ_ij − s_j)
J = diag(s) − s · sᵀ           # 对称矩阵
</code></pre></div></div> <h3 id="jacobian-vector-product-化简为什么不用显式构造-j">Jacobian-Vector Product 化简(为什么不用显式构造 J)</h3> <p>显式构造 <code class="language-plaintext highlighter-rouge">J</code> 是 <code class="language-plaintext highlighter-rouge">O(M²)</code>,但 backward 只要 <code class="language-plaintext highlighter-rouge">g · J</code>(VJP):</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>(g · J)_j = Σ_i g_i · s_i · (δ_ij − s_j)
         = g_j · s_j − s_j · Σ_i g_i · s_i
         = s_j · (g_j − ⟨g, s⟩)
</code></pre></div></div> <p>→ 向量化: <strong><code class="language-plaintext highlighter-rouge">s ⊙ (g − ⟨g, s⟩)</code></strong>,只要 <code class="language-plaintext highlighter-rouge">O(M)</code> 乘加,不用建 J。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 显式 (慢, O(M²)):              vs       向量化 (快, O(M)):
</span><span class="n">J</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">diag</span><span class="p">(</span><span class="n">s</span><span class="p">)</span> <span class="o">-</span> <span class="n">np</span><span class="p">.</span><span class="nf">outer</span><span class="p">(</span><span class="n">s</span><span class="p">,</span> <span class="n">s</span><span class="p">)</span>          <span class="n">out</span> <span class="o">=</span> <span class="n">s</span> <span class="o">*</span> <span class="p">(</span><span class="n">g</span> <span class="o">-</span> <span class="p">(</span><span class="n">g</span> <span class="o">*</span> <span class="n">s</span><span class="p">).</span><span class="nf">sum</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="n">keepdims</span><span class="o">=</span><span class="bp">True</span><span class="p">))</span>
<span class="n">out</span> <span class="o">=</span> <span class="n">g</span> <span class="o">@</span> <span class="n">J</span>                              <span class="c1"># 直接对 batch 维广播
</span></code></pre></div></div> <h3 id="softmax--cross-entropy--p--y-经典化简">Softmax + Cross-Entropy = <code class="language-plaintext highlighter-rouge">p − y</code> (经典化简)</h3> <p>CE loss: <code class="language-plaintext highlighter-rouge">L = -Σ_i y_i · log(s_i)</code>,其中 <code class="language-plaintext highlighter-rouge">y</code> 是 one-hot。</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>∂L/∂z_j = ∂L/∂s · ∂s/∂z = (-y/s) · J
        = Σ_i (-y_i/s_i) · s_i · (δ_ij − s_j)
        = -y_j + s_j · Σ_i y_i        ← y 是 one-hot, Σ y_i = 1
        = s_j − y_j
</code></pre></div></div> <p><strong>所以 logits 上的梯度就是 <code class="language-plaintext highlighter-rouge">softmax(z) − onehot(target)</code></strong>,完全不用建 Jacobian。这就是为什么 deep learning 框架把 softmax + CE 融成一个 op (<code class="language-plaintext highlighter-rouge">F.cross_entropy(logits, targets)</code>),数值更稳、反向更快。</p> <h3 id="attention-反向链qkv-完整推导">Attention 反向链(QKV 完整推导)</h3> <p>前向: <code class="language-plaintext highlighter-rouge">S = QKᵀ/√D</code>, <code class="language-plaintext highlighter-rouge">A = softmax(S, dim=-1)</code>, <code class="language-plaintext highlighter-rouge">out = A·V</code>。</p> <p><strong>已知</strong>: <code class="language-plaintext highlighter-rouge">dout</code> (上游梯度, shape <code class="language-plaintext highlighter-rouge">[N, D]</code>)</p> <p><strong>反向 1: <code class="language-plaintext highlighter-rouge">out = A·V</code> (普通矩乘)</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>dV = Aᵀ · dout          ← shape [M, D]
dA = dout · Vᵀ          ← shape [N, M]
</code></pre></div></div> <p><strong>反向 2: <code class="language-plaintext highlighter-rouge">A = softmax(S, dim=-1)</code> (逐行 softmax)</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>dS[i] = softmax_backward(A[i], dA[i])     # 每行独立
     = A[i] ⊙ (dA[i] − ⟨dA[i], A[i]⟩)
</code></pre></div></div> <p><strong>反向 3: <code class="language-plaintext highlighter-rouge">S = Q·Kᵀ/√D</code> (matmul + scale)</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>dQ = dS · K / √D        ← shape [N, D]
dK = dSᵀ · Q / √D       ← shape [M, D]
</code></pre></div></div> <p><strong>记忆法</strong>: 反向是前向<strong>4 个矩阵乘的逆序求转置</strong>,中间夹一个 softmax_backward。<code class="language-plaintext highlighter-rouge">√D</code> 的位置和前向一样(标量,直接除)。</p> <h3 id="代码骨架-1">代码骨架</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">softmax_backward</span><span class="p">(</span><span class="n">s</span><span class="p">,</span> <span class="n">grad_s</span><span class="p">):</span>
    <span class="c1"># 向量化版: s * (grad_s - (grad_s * s).sum(-1, keepdims=True))
</span>    <span class="n">N</span><span class="p">,</span> <span class="n">M</span> <span class="o">=</span> <span class="n">s</span><span class="p">.</span><span class="n">shape</span>
    <span class="n">grad</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">zeros</span><span class="p">((</span><span class="n">N</span><span class="p">,</span> <span class="n">M</span><span class="p">),</span> <span class="n">dtype</span><span class="o">=</span><span class="n">np</span><span class="p">.</span><span class="n">float32</span><span class="p">)</span>
    <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">N</span><span class="p">):</span>
        <span class="n">J</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">diag</span><span class="p">(</span><span class="n">s</span><span class="p">[</span><span class="n">i</span><span class="p">])</span> <span class="o">-</span> <span class="n">s</span><span class="p">[</span><span class="n">i</span><span class="p">][:,</span> <span class="bp">None</span><span class="p">]</span> <span class="o">@</span> <span class="n">s</span><span class="p">[</span><span class="n">i</span><span class="p">][</span><span class="bp">None</span><span class="p">,</span> <span class="p">:]</span>
        <span class="n">grad</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">=</span> <span class="n">grad_s</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">@</span> <span class="n">J</span>
    <span class="k">return</span> <span class="n">grad</span>

<span class="k">def</span> <span class="nf">sdpa_backward</span><span class="p">(</span><span class="n">grad</span><span class="p">,</span> <span class="n">q</span><span class="p">,</span> <span class="n">k</span><span class="p">,</span> <span class="n">v</span><span class="p">):</span>
    <span class="n">D</span> <span class="o">=</span> <span class="n">q</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">1</span><span class="p">]</span>
    <span class="n">A</span> <span class="o">=</span> <span class="nf">softmax</span><span class="p">(</span><span class="n">q</span> <span class="o">@</span> <span class="n">k</span><span class="p">.</span><span class="n">T</span> <span class="o">/</span> <span class="n">np</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="n">D</span><span class="p">))</span>
    <span class="n">dv</span> <span class="o">=</span> <span class="n">A</span><span class="p">.</span><span class="n">T</span> <span class="o">@</span> <span class="n">grad</span>
    <span class="n">dA</span> <span class="o">=</span> <span class="n">grad</span> <span class="o">@</span> <span class="n">v</span><span class="p">.</span><span class="n">T</span>
    <span class="n">dS</span> <span class="o">=</span> <span class="nf">softmax_backward</span><span class="p">(</span><span class="n">A</span><span class="p">,</span> <span class="n">dA</span><span class="p">)</span>
    <span class="n">dq</span> <span class="o">=</span> <span class="n">dS</span> <span class="o">@</span> <span class="n">k</span> <span class="o">/</span> <span class="n">np</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="n">D</span><span class="p">)</span>
    <span class="n">dk</span> <span class="o">=</span> <span class="n">dS</span><span class="p">.</span><span class="n">T</span> <span class="o">@</span> <span class="n">q</span> <span class="o">/</span> <span class="n">np</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="n">D</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">dq</span><span class="p">,</span> <span class="n">dk</span><span class="p">,</span> <span class="n">dv</span>
</code></pre></div></div> <p><strong>验证</strong>: 和 PyTorch autograd 对比,<code class="language-plaintext highlighter-rouge">atol=1e-4</code> 内 close。</p> <h3 id="为什么除-d-方差分析">为什么除 √D? (方差分析)</h3> <p>设 <code class="language-plaintext highlighter-rouge">Q[i], K[j]</code> 各元素 i.i.d. ~ <code class="language-plaintext highlighter-rouge">N(0, 1)</code>, 维度 D。</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>S[i,j] = Σ_k Q[i,k] · K[j,k]
E[S]   = 0
Var[S] = Σ_k Var[Q·K] = D · 1 = D     ← 方差线性增长
Std[S] = √D
</code></pre></div></div> <p>D=64 时 <code class="language-plaintext highlighter-rouge">Std[S]≈8</code>,D=128 时 <code class="language-plaintext highlighter-rouge">≈11.3</code>。<strong>进 softmax 前 logits 太大 → softmax 退化为 one-hot → 梯度消失</strong>。</p> <p>除 <code class="language-plaintext highlighter-rouge">√D</code>: <code class="language-plaintext highlighter-rouge">Var[S/√D] = D/D = 1</code>,无论 D 多大方差稳定。 <strong>所以这个 <code class="language-plaintext highlighter-rouge">√D</code> 是为了梯度健康,不是为了 forward 数值稳定</strong>。</p> <h3 id="数值稳定的-softmax减-max-技巧">数值稳定的 softmax(减 max 技巧)</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">softmax</span><span class="p">(</span><span class="n">z</span><span class="p">):</span>
    <span class="n">z</span> <span class="o">=</span> <span class="n">z</span> <span class="o">-</span> <span class="n">z</span><span class="p">.</span><span class="nf">max</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="n">keepdims</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>   <span class="c1"># ★ 减 max
</span>    <span class="n">e</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">z</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">e</span> <span class="o">/</span> <span class="n">e</span><span class="p">.</span><span class="nf">sum</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="n">keepdims</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
</code></pre></div></div> <p><strong>为什么不影响结果</strong>: <code class="language-plaintext highlighter-rouge">softmax(z) = softmax(z - c)</code>,因为 <code class="language-plaintext highlighter-rouge">exp(z_i - c) / Σ exp(z_k - c) = exp(z_i)·exp(-c) / (exp(-c)·Σ exp(z_k))</code>,<code class="language-plaintext highlighter-rouge">exp(-c)</code> 上下消去。</p> <p><strong>为什么减最大值不溢出</strong>: 减完后所有元素 ≤ 0,<code class="language-plaintext highlighter-rouge">exp(≤0) ∈ (0, 1]</code>,绝不会溢出。</p> <h3 id="训练时-attention-反向的显存代价">训练时 attention 反向的显存代价</h3> <p>每一层 attention,正向产生:</p> <ul> <li><code class="language-plaintext highlighter-rouge">S [B, H, N, M]</code> 中间矩阵</li> <li><code class="language-plaintext highlighter-rouge">A [B, H, N, M]</code> softmax 输出</li> </ul> <p>反向都要用,所以必须保存 → <strong>显存 O(B·H·N²)</strong>。 对 LLaMA-7B (32 层, 32 头),序列 4k → ~16 GB 仅 attention 中间量。 <strong>这就是为什么 FlashAttention(§4)能让训练装下更大的模型</strong>。</p> <h3 id="高频追问-2">高频追问</h3> <table> <thead> <tr> <th>Q</th> <th>A</th> </tr> </thead> <tbody> <tr> <td>为什么 softmax 数值稳定要减 max?</td> <td><code class="language-plaintext highlighter-rouge">exp(z)</code> 大数会上溢;<code class="language-plaintext highlighter-rouge">softmax(z) == softmax(z - max(z))</code>(分子分母同乘常数)</td> </tr> <tr> <td>为什么 attention 要除 √D?</td> <td>点积方差 = D → softmax 饱和 → 梯度消失。除 √D 让 logit 方差稳定为 1</td> </tr> <tr> <td>softmax + cross-entropy 为什么简化成 <code class="language-plaintext highlighter-rouge">softmax - onehot</code>?</td> <td>见上面推导:<code class="language-plaintext highlighter-rouge">-y/s · J</code> 展开后 one-hot 把求和压到一个分量,剩 <code class="language-plaintext highlighter-rouge">p - y</code></td> </tr> <tr> <td>为什么不显式建 Jacobian?</td> <td>J 是 <code class="language-plaintext highlighter-rouge">O(M²)</code>,但 VJP 化简后 <code class="language-plaintext highlighter-rouge">s ⊙ (g − ⟨g,s⟩)</code> 只要 <code class="language-plaintext highlighter-rouge">O(M)</code></td> </tr> <tr> <td>反向能 inplace 写 dq, dk, dv 吗?</td> <td>不行,共用 A 的话会乱;PyTorch 是 save_for_backward 保留前向中间量</td> </tr> <tr> <td>训练时哪几个张量必须存?</td> <td>前向: Q, K, V, A(softmax 输出);反向重算 S 或直接复用 A。FlashAttention 只存 <code class="language-plaintext highlighter-rouge">(O, m, l)</code> 三个</td> </tr> <tr> <td>为什么 PyTorch autograd 默认是 VJP 不是 JVP?</td> <td>神经网络 loss 是标量,VJP (反向模式) 1 次反向得所有参数梯度;JVP 要 <code class="language-plaintext highlighter-rouge">dim(params)</code> 次正向</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">softmax(z/T)</code> 中 T 的作用?</td> <td>T → 0: one-hot(贪心);T → ∞: 均匀。LLM 推理用来控制采样随机性</td> </tr> <tr> <td>推导一下 <code class="language-plaintext highlighter-rouge">dz = s · (g - g·s)</code>,为什么不是 <code class="language-plaintext highlighter-rouge">s · g - s² · g</code>?</td> <td><code class="language-plaintext highlighter-rouge">(g · s)</code> 是<strong>内积标量</strong> Σ g_i·s_i,不是逐元素 g⊙s。是同一个标量减回 g 的每个分量</td> </tr> </tbody> </table> <hr/> <h2 id="4-memory-efficient-attention--online-softmax--flashattention">4. Memory-Efficient Attention — Online Softmax → FlashAttention</h2> <blockquote> <p><strong>Key Insight</strong>: 数值稳定的 safe softmax 本身<strong>最少要 2 pass</strong>(必须先扫一遍找 <code class="language-plaintext highlighter-rouge">m_N</code> 才能减 max)。 但 attention 的最终目标是 <code class="language-plaintext highlighter-rouge">O = A·V</code>,<strong>不是 A 本身</strong> —— 对 <code class="language-plaintext highlighter-rouge">O</code> 再施一次 “surrogate” 技巧,可以把整个 attention 压到 <strong>1 pass</strong>。这就是 FlashAttention 的全部魔法。</p> </blockquote> <blockquote> <p>参考: <a href="https://courses.cs.washington.edu/courses/cse599m/23sp/notes/flashattn.pdf">From Online Softmax to FlashAttention</a> (Zihao Ye, UW CSE 599M)</p> </blockquote> <h3 id="实现层次-3-pass--2-pass--1-pass">实现层次: 3 pass → 2 pass → 1 pass</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>══════════ Pass 1: Safe Softmax (3 passes) ══════════

   pass 1:  m_N = max(x_1, ..., x_N)          ← 扫一遍取全局 max
   pass 2:  d_N = Σ exp(x_i - m_N)             ← 再扫一遍算分母
   pass 3:  a_i = exp(x_i - m_N) / d_N         ← 第三遍得 softmax

   问题: 长序列里 logits {x_i} 装不下 SRAM,3 次 pass 就要 3 次重算 Q·Kᵀ


══════════ Pass 2: Online Softmax (Milakov 2018) ══════════

   关键: 用 surrogate d'_i 代替依赖 m_N 的 d_i

      d'_i := Σ_{j=1}^i exp(x_j - m_i)         ← 只用当前 max m_i,不用 m_N

   递推:
      m_i  = max(m_{i-1}, x_i)
      d'_i = d'_{i-1} · exp(m_{i-1} - m_i)     ← 旧累加用 exp(Δm) 重新缩放
             + exp(x_i - m_i)

   性质: 当 i = N 时 d'_N = d_N,所以可以用 d'_N 替换 d_N

   pass 1:  循环里同时算 m_i 和 d'_i
   pass 2:  a_i = exp(x_i - m_N) / d'_N

   但 softmax 本身**没法压到 1 pass** —— a_i 必须等 m_N 算完


══════════ Pass 3: FlashAttention (1 pass) ══════════

   关键观察: 我们不需要 a_i,只需要 O = Σ a_i · V[i,:]

   再施一次 surrogate,对 O 做递推:

      o'_i := Σ_{j=1}^i (exp(x_j - m_i) / d'_i) · V[j,:]

      o'_i = o'_{i-1} · (d'_{i-1} · exp(m_{i-1} - m_i)) / d'_i
                       + (exp(x_i - m_i) / d'_i) · V[i,:]

   性质: o'_N = O[k, :]

   一个循环里同时维护 (m_i, d'_i, o'_i) 三个状态,扫一遍 K, V 就完事
</code></pre></div></div> <h3 id="单-pass-算法-核心-5-行">单 pass 算法 (核心 5 行)</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">flash_attention_row</span><span class="p">(</span><span class="n">q_k</span><span class="p">,</span> <span class="n">K</span><span class="p">,</span> <span class="n">V</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">对 Q 的第 k 行计算 attention 输出 (single pass over K, V).</span><span class="sh">"""</span>
    <span class="n">D</span> <span class="o">=</span> <span class="n">V</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">1</span><span class="p">]</span>
    <span class="n">m</span><span class="p">,</span> <span class="n">d</span><span class="p">,</span> <span class="n">o</span> <span class="o">=</span> <span class="o">-</span><span class="n">np</span><span class="p">.</span><span class="n">inf</span><span class="p">,</span> <span class="mf">0.0</span><span class="p">,</span> <span class="n">np</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="n">D</span><span class="p">)</span>
    <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">K</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">0</span><span class="p">]):</span>
        <span class="n">x_i</span>   <span class="o">=</span> <span class="n">q_k</span> <span class="o">@</span> <span class="n">K</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">/</span> <span class="n">np</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="n">D</span><span class="p">)</span>                        <span class="c1"># logit
</span>        <span class="n">m_new</span> <span class="o">=</span> <span class="nf">max</span><span class="p">(</span><span class="n">m</span><span class="p">,</span> <span class="n">x_i</span><span class="p">)</span>
        <span class="n">d_new</span> <span class="o">=</span> <span class="n">d</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">m</span> <span class="o">-</span> <span class="n">m_new</span><span class="p">)</span> <span class="o">+</span> <span class="n">np</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">x_i</span> <span class="o">-</span> <span class="n">m_new</span><span class="p">)</span>
        <span class="n">o</span>     <span class="o">=</span> <span class="n">o</span> <span class="o">*</span> <span class="p">(</span><span class="n">d</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">m</span> <span class="o">-</span> <span class="n">m_new</span><span class="p">))</span> <span class="o">/</span> <span class="n">d_new</span> \
              <span class="o">+</span> <span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">x_i</span> <span class="o">-</span> <span class="n">m_new</span><span class="p">)</span> <span class="o">/</span> <span class="n">d_new</span><span class="p">)</span> <span class="o">*</span> <span class="n">V</span><span class="p">[</span><span class="n">i</span><span class="p">]</span>
        <span class="n">m</span><span class="p">,</span> <span class="n">d</span>  <span class="o">=</span> <span class="n">m_new</span><span class="p">,</span> <span class="n">d_new</span>
    <span class="k">return</span> <span class="n">o</span>   <span class="c1"># = O[k, :]
</span></code></pre></div></div> <h3 id="工程版-tiled-块大小-b-进-sram">工程版: tiled (块大小 b 进 SRAM)</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">flash_attention_tiled</span><span class="p">(</span><span class="n">Q</span><span class="p">,</span> <span class="n">K</span><span class="p">,</span> <span class="n">V</span><span class="p">,</span> <span class="n">b</span><span class="p">):</span>
    <span class="n">L</span><span class="p">,</span> <span class="n">D</span> <span class="o">=</span> <span class="n">Q</span><span class="p">.</span><span class="n">shape</span>
    <span class="n">O</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">zeros_like</span><span class="p">(</span><span class="n">Q</span><span class="p">)</span>
    <span class="k">for</span> <span class="n">k</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">L</span><span class="p">):</span>                                         <span class="c1"># 行间天然并行
</span>        <span class="n">m</span><span class="p">,</span> <span class="n">d</span><span class="p">,</span> <span class="n">o</span> <span class="o">=</span> <span class="o">-</span><span class="n">np</span><span class="p">.</span><span class="n">inf</span><span class="p">,</span> <span class="mf">0.0</span><span class="p">,</span> <span class="n">np</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="n">D</span><span class="p">)</span>
        <span class="k">for</span> <span class="n">start</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">L</span><span class="p">,</span> <span class="n">b</span><span class="p">):</span>                           <span class="c1"># 沿 K, V 滑动
</span>            <span class="n">Kc</span><span class="p">,</span> <span class="n">Vc</span>  <span class="o">=</span> <span class="n">K</span><span class="p">[</span><span class="n">start</span><span class="p">:</span><span class="n">start</span><span class="o">+</span><span class="n">b</span><span class="p">],</span> <span class="n">V</span><span class="p">[</span><span class="n">start</span><span class="p">:</span><span class="n">start</span><span class="o">+</span><span class="n">b</span><span class="p">]</span>       <span class="c1"># 一块进 SRAM
</span>            <span class="n">x</span>       <span class="o">=</span> <span class="n">Q</span><span class="p">[</span><span class="n">k</span><span class="p">]</span> <span class="o">@</span> <span class="n">Kc</span><span class="p">.</span><span class="n">T</span> <span class="o">/</span> <span class="n">np</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="n">D</span><span class="p">)</span>                 <span class="c1"># [b]
</span>            <span class="n">m_new</span>   <span class="o">=</span> <span class="nf">max</span><span class="p">(</span><span class="n">m</span><span class="p">,</span> <span class="n">x</span><span class="p">.</span><span class="nf">max</span><span class="p">())</span>
            <span class="n">scale</span>   <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">m</span> <span class="o">-</span> <span class="n">m_new</span><span class="p">)</span>
            <span class="n">e</span>       <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">x</span> <span class="o">-</span> <span class="n">m_new</span><span class="p">)</span>                        <span class="c1"># [b]
</span>            <span class="n">d_new</span>   <span class="o">=</span> <span class="n">d</span> <span class="o">*</span> <span class="n">scale</span> <span class="o">+</span> <span class="n">e</span><span class="p">.</span><span class="nf">sum</span><span class="p">()</span>
            <span class="n">o</span>       <span class="o">=</span> <span class="n">o</span> <span class="o">*</span> <span class="p">(</span><span class="n">d</span> <span class="o">*</span> <span class="n">scale</span><span class="p">)</span> <span class="o">/</span> <span class="n">d_new</span> <span class="o">+</span> <span class="p">(</span><span class="n">e</span> <span class="o">@</span> <span class="n">Vc</span><span class="p">)</span> <span class="o">/</span> <span class="n">d_new</span>
            <span class="n">m</span><span class="p">,</span> <span class="n">d</span>    <span class="o">=</span> <span class="n">m_new</span><span class="p">,</span> <span class="n">d_new</span>
        <span class="n">O</span><span class="p">[</span><span class="n">k</span><span class="p">]</span> <span class="o">=</span> <span class="n">o</span>
    <span class="k">return</span> <span class="n">O</span>
</code></pre></div></div> <h3 id="硬件视角-为什么省-hbm">硬件视角: 为什么省 HBM</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>朴素:    HBM ↔ S [N, M] ↔ HBM ↔ A = softmax(S) [N, M] ↔ HBM ↔ A·V ↔ HBM
         中间矩阵 S, A 反复读写 HBM  (这是真正的瓶颈)

Flash:   HBM → load Q[k], K[i:i+b], V[i:i+b] → SRAM tile 内算完 (m,d,o) → HBM 只写 O[k]
         中间结果 (S 的 tile, A 的 tile) 永远不出 SRAM
</code></pre></div></div> <p>H100 单个 SM 的 SRAM ≈ 228 KB(比 HBM 快 ~30×)。整体 SRAM footprint 只和 <code class="language-plaintext highlighter-rouge">b, D</code> 有关,<strong>和序列长度 L 无关</strong> → 这就是 FlashAttention 能撑 16k+ context 的原因。</p> <h3 id="反向传播-重算-recomputation-而非缓存">反向传播: 重算 (recomputation) 而非缓存</h3> <p>朴素 attention 的反向(见 §3) 需要保留 <code class="language-plaintext highlighter-rouge">[N, M]</code> 的 <code class="language-plaintext highlighter-rouge">A</code> 矩阵;长序列下显存爆炸。 FlashAttention 的解法: <strong>正向只保存每行的标量 <code class="language-plaintext highlighter-rouge">(m, l)</code>,反向时在每个 tile 内重新算一遍 softmax</strong>。</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>正向保存:                反向重算:
  O   [L, D]              for each Kⱼ, Vⱼ tile:
  m   [L]   (running max)   重算  S_ij = Qᵢ Kⱼᵀ / √D
  l   [L]   (running sum)   重算  A_ij = exp(S_ij - mᵢ) / lᵢ   ← 用保存的 mᵢ, lᵢ
                            标准 chain rule:
                              dV_j  += A_ijᵀ · dO_i
                              dA_ij  = dO_i · V_jᵀ
                              dS_ij  = softmax_backward(A_ij, dA_ij)
                                     = A_ij ⊙ (dA_ij - D_i)
                                     其中  D_i = (dO_i · O_iᵀ).sum(-1)    ← 一个标量
                              dQ_i  += dS_ij · K_j / √D
                              dK_j  += dS_ijᵀ · Q_i / √D
</code></pre></div></div> <p><strong>关键 trick: 那个 <code class="language-plaintext highlighter-rouge">D_i = (dO·Oᵀ).sum</code></strong> —— 朴素 softmax 反向要 <code class="language-plaintext highlighter-rouge">g·s</code> 内积,FlashAttention 里 <code class="language-plaintext highlighter-rouge">g = dA</code>, <code class="language-plaintext highlighter-rouge">s = A</code> 都不显式存。但代入 <code class="language-plaintext highlighter-rouge">O = A·V</code> 可以证明 <code class="language-plaintext highlighter-rouge">(dA·A).sum == (dO·O).sum</code> 是同一个标量, <strong>用已经存好的 <code class="language-plaintext highlighter-rouge">O, dO</code> 就能算出来</strong>,完全不需要 materialize <code class="language-plaintext highlighter-rouge">A</code>。</p> <h3 id="正反向显存对比">正反向显存对比</h3> <table> <thead> <tr> <th>方案</th> <th>正向显存</th> <th>反向额外显存</th> <th>总计</th> </tr> </thead> <tbody> <tr> <td>朴素 attention</td> <td><code class="language-plaintext highlighter-rouge">O(N²)</code> 存 <code class="language-plaintext highlighter-rouge">S</code>, <code class="language-plaintext highlighter-rouge">A</code></td> <td>0 (复用前向)</td> <td><code class="language-plaintext highlighter-rouge">O(N²)</code></td> </tr> <tr> <td>FlashAttention</td> <td><code class="language-plaintext highlighter-rouge">O(N·D)</code> 存 <code class="language-plaintext highlighter-rouge">O</code> + <code class="language-plaintext highlighter-rouge">O(N)</code> 存 <code class="language-plaintext highlighter-rouge">(m, l)</code></td> <td>tile 缓冲 <code class="language-plaintext highlighter-rouge">O(b²)</code></td> <td><strong><code class="language-plaintext highlighter-rouge">O(N·D)</code></strong></td> </tr> <tr> <td>序列 16k, D 128</td> <td>~256 MB</td> <td>—</td> <td>~8 MB (<code class="language-plaintext highlighter-rouge">32×</code>)</td> </tr> </tbody> </table> <h3 id="io-复杂度证明梗概为什么是-ondsram">IO 复杂度证明梗概(为什么是 O(N²·D²/SRAM))</h3> <p>FlashAttention 论文给出: 朴素 attention 的 HBM 访问量 <code class="language-plaintext highlighter-rouge">O(N·D + N²)</code>,FlashAttention <code class="language-plaintext highlighter-rouge">O(N²·D² / SRAM_size)</code>。 当 <code class="language-plaintext highlighter-rouge">D ≪ √SRAM</code> 时(典型 D=128, SRAM=100KB → √SRAM≈300),Flash 的 HBM 访问 <strong>少 1-2 个数量级</strong>。 这正是 FlashAttention 实际<strong>加速 2-4×</strong> 的来源(不是 FLOPs,而是 IO)。</p> <h3 id="高频追问-3">高频追问</h3> <table> <thead> <tr> <th>Q</th> <th>A</th> </tr> </thead> <tbody> <tr> <td>为什么 softmax 最少 2 pass?</td> <td>数值稳定要先有 <code class="language-plaintext highlighter-rouge">m_N</code> 才能减;<code class="language-plaintext highlighter-rouge">d'_i</code> 的 surrogate 让 <code class="language-plaintext highlighter-rouge">m_i</code> 替代 <code class="language-plaintext highlighter-rouge">m_N</code>,但 <code class="language-plaintext highlighter-rouge">a_i</code> 仍依赖 <code class="language-plaintext highlighter-rouge">m_N</code></td> </tr> <tr> <td>为什么 FlashAttention 能 1 pass?</td> <td>目标是 <code class="language-plaintext highlighter-rouge">O = A·V</code> 而非 <code class="language-plaintext highlighter-rouge">A</code>,对 <code class="language-plaintext highlighter-rouge">O</code> 再施一次 surrogate trick (<code class="language-plaintext highlighter-rouge">o'_i</code>) 消去 <code class="language-plaintext highlighter-rouge">m_N</code> 依赖</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">exp(m_{i-1} - m_i)</code> 在做什么?</td> <td>把”用旧 max 累加的量”<strong>修正成”用新 max 累加的”</strong>,保持数值等价</td> </tr> <tr> <td>FlashAttention 为什么快?(常见误解)</td> <td><strong>不是减 FLOPs,是减 HBM 读写</strong>。tiling 让中间矩阵留在 SRAM</td> </tr> <tr> <td>反向怎么实现?</td> <td>只存 <code class="language-plaintext highlighter-rouge">(m, d')</code>,反向重算 softmax + V 乘积;FLOPs 换显存</td> </tr> <tr> <td>FlashAttention 2 / 3 改了什么?</td> <td>v2: 交换内外循环 + 减 non-matmul FLOPs;v3: Hopper warp-specialization + FP8 异步</td> </tr> <tr> <td>为什么 Flash 对 long-context LLM 关键?</td> <td>SRAM 占用与 L 无关,只与 <code class="language-plaintext highlighter-rouge">b, D</code> 有关 → 64k context 也能稳跑</td> </tr> </tbody> </table> <hr/> <h2 id="5-reward-model--bradley-terry-pairwise">5. Reward Model — Bradley-Terry pairwise</h2> <blockquote> <p><strong>Key Insight</strong>: 不要让人打绝对分(尺度不一致),只让人选 A/B 哪个好。 Bradley-Terry: <code class="language-plaintext highlighter-rouge">P(y_w ≻ y_l) = σ(r_w − r_l)</code>,损失 = <code class="language-plaintext highlighter-rouge">−log σ(r_w − r_l)</code>。</p> </blockquote> <h3 id="实现层次-2">实现层次</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>            ┌──────── chosen 分支 ────────┐    ┌──────── rejected 分支 ────────┐
            │                              │    │                                │
input:      prompt + y_w  [B, T]                prompt + y_l  [B, T]
            │                                   │
            ▼ backbone (causal LM)              ▼ backbone (共享权重,前向 2 次)
            h_w [B, T, hidden]                  h_l [B, T, hidden]
            │                                   │
            ▼ 取最后一个 token (左 padding 保证)  ▼ 取最后一个 token
            h_w[:, -1, :] [B, hidden]           h_l[:, -1, :] [B, hidden]
            │                                   │
            ▼ reward_head (Linear → 1)          ▼ reward_head (同一个)
            r_chosen   [B]                      r_rejected [B]
                │                                   │
                └─────────────┬─────────────────────┘
                              ▼
                Bradley-Terry pairwise loss
                L = −log σ(r_chosen − r_rejected)
                              ▼
                          backward
                  (训练 backbone + reward_head)
</code></pre></div></div> <h3 id="公式">公式</h3> <p>每个回答 <code class="language-plaintext highlighter-rouge">y</code> 有隐式效用 <code class="language-plaintext highlighter-rouge">r(x, y)</code>,偏好概率:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>P(y_w ≻ y_l | x) = σ(r(x, y_w) − r(x, y_l))
</code></pre></div></div> <p>最大化对数似然 → 损失:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>L = -log σ(r(x, y_w) − r(x, y_l))
</code></pre></div></div> <h3 id="代码骨架-2">代码骨架</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">RewardModel</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">backbone</span><span class="p">,</span> <span class="n">hidden_dim</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">backbone</span> <span class="o">=</span> <span class="n">backbone</span>
        <span class="n">self</span><span class="p">.</span><span class="n">reward_head</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">hidden_dim</span><span class="p">,</span> <span class="mi">1</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">):</span>
        <span class="n">h</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">backbone</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>              <span class="c1"># [B, T, hidden]
</span>        <span class="n">last</span> <span class="o">=</span> <span class="n">h</span><span class="p">[:,</span> <span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="p">:]</span>                <span class="c1"># 最后一个 token(causal 模型看到整句)
</span>        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="nf">reward_head</span><span class="p">(</span><span class="n">last</span><span class="p">).</span><span class="nf">squeeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span>

<span class="k">def</span> <span class="nf">reward_model_loss</span><span class="p">(</span><span class="n">r_chosen</span><span class="p">,</span> <span class="n">r_rejected</span><span class="p">):</span>
    <span class="k">return</span> <span class="o">-</span><span class="n">torch</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nf">sigmoid</span><span class="p">(</span><span class="n">r_chosen</span> <span class="o">-</span> <span class="n">r_rejected</span><span class="p">)).</span><span class="nf">sum</span><span class="p">()</span>
</code></pre></div></div> <p><strong>工程细节</strong>:</p> <ul> <li>用<strong>左 padding</strong>(<code class="language-plaintext highlighter-rouge">encode_batch_left_pad</code>),保证 <code class="language-plaintext highlighter-rouge">h[:, -1, :]</code> 是真实结尾</li> <li>Backbone 通常 init from SFT checkpoint,reward_head 随机初始化</li> <li>训练时 chosen / rejected 共享 backbone 前向,只跑 2 次</li> </ul> <h3 id="bradley-terry-的等价-logistic-写法">Bradley-Terry 的等价 logistic 写法</h3> <p><code class="language-plaintext highlighter-rouge">-log σ(r_w - r_l)</code> 其实就是<strong>二分类 logistic loss</strong>,把”<code class="language-plaintext highlighter-rouge">y_w</code> ≻ <code class="language-plaintext highlighter-rouge">y_l</code>“当作 label = 1。 等价于在 reward 差上做二分类:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>σ(r_w - r_l)   = P(chosen wins)         ← 模型预测
target         = 1                        ← 数据标签
binary CE      = -log σ(r_w - r_l)       ← 标准 logistic loss
</code></pre></div></div> <p>→ 任何稳定的 logistic 损失 trick(如 label smoothing)都能直接套上。</p> <h3 id="奖励的尺度问题reward-scaling">奖励的尺度问题(reward scaling)</h3> <p>RM 训完后,raw reward 的<strong>尺度任意</strong>(取决于 backbone 初始化、训练步数等)。 不归一化直接送 PPO 会出问题:<strong>reward 数量级波动 → KL 系数失效 → policy 训练不稳</strong>。</p> <p>常用归一化:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 方法 1: 跑 RM 在大批 prompt 上算 mean/std,做 z-score (InstructGPT 做法)
</span><span class="n">rewards</span> <span class="o">=</span> <span class="p">(</span><span class="nf">rm</span><span class="p">(</span><span class="n">x</span><span class="p">)</span> <span class="o">-</span> <span class="n">reward_mean</span><span class="p">)</span> <span class="o">/</span> <span class="n">reward_std</span>

<span class="c1"># 方法 2: per-batch 归一化(简单)
</span><span class="n">rewards</span> <span class="o">=</span> <span class="p">(</span><span class="n">r</span> <span class="o">-</span> <span class="n">r</span><span class="p">.</span><span class="nf">mean</span><span class="p">())</span> <span class="o">/</span> <span class="p">(</span><span class="n">r</span><span class="p">.</span><span class="nf">std</span><span class="p">()</span> <span class="o">+</span> <span class="mf">1e-8</span><span class="p">)</span>

<span class="c1"># 方法 3: 取 baseline (减 SFT 模型在同 prompt 上的 reward)
</span><span class="n">shaped</span> <span class="o">=</span> <span class="nf">rm</span><span class="p">(</span><span class="n">policy_out</span><span class="p">)</span> <span class="o">-</span> <span class="nf">rm</span><span class="p">(</span><span class="n">sft_out</span><span class="p">)</span>
</code></pre></div></div> <h3 id="reward-hacking-是什么">Reward Hacking 是什么</h3> <p>policy 学会”骗”RM 拿高分但人类不喜欢的行为。常见模式:</p> <table> <thead> <tr> <th>模式</th> <th>例子</th> <th>修法</th> </tr> </thead> <tbody> <tr> <td><strong>长度作弊</strong></td> <td>RM 训练时 chosen 倾向更长 → policy 全写超长答案</td> <td>length penalty / SimPO</td> </tr> <tr> <td><strong>格式作弊</strong></td> <td>加 “Sure!” “Of course.” 这种 RM 喜欢的开头</td> <td>reward shaping / 多样化训练数据</td> </tr> <tr> <td><strong>拒答作弊</strong></td> <td>模糊回答避免被 RM 惩罚</td> <td>helpfulness vs harmlessness 多 RM</td> </tr> <tr> <td><strong>重复回应</strong></td> <td>复述 prompt 占 token</td> <td>repetition penalty in reward</td> </tr> <tr> <td><strong>数学幻觉</strong></td> <td>装作算了一长串 (RM 看格式不看正确性)</td> <td>用 verifier 替代 RM (GRPO 数学路线)</td> </tr> </tbody> </table> <p>→ 这就是为什么 PPO 要加 KL 惩罚 <code class="language-plaintext highlighter-rouge">β·KL(π‖π_ref)</code>:<strong>约束 policy 不要为了高 reward 偏离 SFT 太远</strong>。</p> <h3 id="多目标-reward-model-helpfulness-vs-harmlessness">多目标 Reward Model (Helpfulness vs Harmlessness)</h3> <p>Anthropic Constitutional AI / Claude 路线:<strong>两个 RM</strong>,加权合并:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>R = α · R_helpful + β · R_harmless
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">α, β</code> 是动态调的: 检测到不安全输入 → β 提高;一般问答 → α 主导。 也可以训成<strong>单个 RM 输出多维标量</strong>,然后融合层学加权。</p> <h3 id="pairwise-vs-pointwise-vs-listwise">Pairwise vs Pointwise vs Listwise</h3> <table> <thead> <tr> <th>范式</th> <th>数据</th> <th>Loss</th> <th>典型</th> </tr> </thead> <tbody> <tr> <td><strong>Pointwise</strong></td> <td>(prompt, response, score 1-5)</td> <td>MSE</td> <td>早期人工评分,人标不一致</td> </tr> <tr> <td><strong>Pairwise (BT)</strong></td> <td>(prompt, chosen, rejected)</td> <td><code class="language-plaintext highlighter-rouge">-log σ(r_w - r_l)</code></td> <td>InstructGPT, Claude</td> </tr> <tr> <td><strong>Listwise (Plackett-Luce)</strong></td> <td>(prompt, ranked list of k)</td> <td>k 选 1 的概率乘积</td> <td>排序信息更密集</td> </tr> </tbody> </table> <p>Pairwise 工业标配 —— 标注成本 / 一致性 / 算法成熟度三方面最优。</p> <h3 id="评估-rm-的指标">评估 RM 的指标</h3> <p><strong>Accuracy on held-out pairs</strong>: 给定 (chosen, rejected),RM 算出 <code class="language-plaintext highlighter-rouge">r_w &gt; r_l</code> 的比例。 经验值: 训得好的 RM 在新数据上 ~65-75%(注意:这个 ceiling 比想象低,因为偏好本身不一致)。</p> <p><strong>KL-controlled win rate</strong>: 让 PPO 训完的 policy 在固定 KL 距离 <code class="language-plaintext highlighter-rouge">β</code> 下, 对比 baseline (SFT 或上一版 policy) 的人类胜率。这才是 RM 真正的”线上指标”。</p> <h3 id="高频追问-4">高频追问</h3> <table> <thead> <tr> <th>Q</th> <th>A</th> </tr> </thead> <tbody> <tr> <td>为什么取最后一个 token?</td> <td>Causal mask 下,最后一个位置的 hidden 看到了完整 prompt + response</td> </tr> <tr> <td>Reward hacking 是什么?</td> <td>Policy 学会钻 RM 漏洞(长度/格式/拒答作弊),拿到高 reward 但人类不喜欢。修: 加 KL,length penalty,多目标 RM</td> </tr> <tr> <td>RM 和 DPO 的关系?</td> <td>DPO 在数学上等价于”训 RM 然后 PPO”,但消去 RM 这一步直接优化 policy(见 §10)</td> </tr> <tr> <td>多个 chosen 怎么处理?</td> <td>(a) 列出所有 pairwise (chosen, rejected) 对逐对算损失 (b) 用 listwise Plackett-Luce</td> </tr> <tr> <td>Reward 要不要归一化?</td> <td>必须。raw reward 尺度任意 → PPO 不稳。z-score 或减 SFT baseline</td> </tr> <tr> <td>RM 训多少数据?</td> <td>InstructGPT 用 ~33k pairs;开源 (Anthropic HH-RLHF) ~170k;实际工业线 100k-1M 量级</td> </tr> <tr> <td>为什么 BT 假设可疑但还能用?</td> <td>BT 假设 reward 是固定标量函数,实际人类偏好可能 cyclic。但平均下来够好</td> </tr> <tr> <td>RM 能直接做 inference rank 吗?</td> <td>可以(best-of-N 采样,挑 r 最高的)。计算成本: N 倍 inference</td> </tr> <tr> <td>Pairwise 数据怎么收集?</td> <td>同 prompt 不同 temperature 采 2 个 → 人标 chosen/rejected。或 GPT-4 当 judge (RLAIF)</td> </tr> <tr> <td>RM 会不会”偏向自己生成”?</td> <td>会。如果 RM 训练数据是 SFT 模型采样的,RM 会偏好 SFT 风格 → 限制了 PPO 的探索</td> </tr> </tbody> </table> <hr/> <h2 id="6-rl-fine-tuning--策略梯度--组内归一化">6. RL Fine-tuning — 策略梯度 + 组内归一化</h2> <blockquote> <p><strong>Key Insight</strong>: 策略梯度的方差太大,必须减 baseline。 <strong>组内均值</strong>当 baseline(同 prompt 才可比),既降方差又不用单独训 value 网络。</p> </blockquote> <h3 id="实现层次-3">实现层次</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>                  ┌─────────── 采样阶段 ───────────┐
                  │                                 │
   prompt [B] ────┤  policy.sample × G             │
                  │  (一个 prompt 采 G 个 rollout)  │
                  └────────────────┬───────────────┘
                                   ▼
              rollouts [B, G, T]   ←─── 每个回答的 token 序列
                                   │
                  ┌────────────────┼────────────────┐
                  ▼                                 ▼
       Reward Model 打分                  policy 重新前向 (要梯度)
              rewards [B, G]                  logits [B, G, T, V]
                  │                                 │
                  ▼ compute_group_advantage         ▼ gather + (-log_softmax)
       (r - mean(-1)) / (std(-1) + ε)      neg_logp [B, G, T]
       advantage [B, G]                            │
                  │                                 │
                  └──────────────┬──────────────────┘
                                 ▼
                policy_gradient_loss
                = (neg_logp * advantage[..., None]).sum(-1).mean(1).sum()
                  − entropy_weight · entropy
                                 │
                                 ▼
                            backward → 更新 policy
</code></pre></div></div> <h3 id="为什么要-advantage不直接用-reward">为什么要 advantage,不直接用 reward?</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>∇L = -E[ ∇logπ(a) · R ]    ← 方差大
∇L = -E[ ∇logπ(a) · (R − b) ]  ← 减 baseline b 不改变期望但降方差
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">b</code> 选什么?</p> <ul> <li>经典: value network <code class="language-plaintext highlighter-rouge">V(s)</code>(actor-critic)</li> <li><strong>GRPO 路线</strong>: 同 prompt 的<strong>组内均值</strong>(省一个 value 网络)</li> </ul> <h3 id="代码骨架-3">代码骨架</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">compute_group_advantage</span><span class="p">(</span><span class="n">rewards</span><span class="p">):</span>     <span class="c1"># [B, G]
</span>    <span class="nf">return </span><span class="p">(</span><span class="n">rewards</span> <span class="o">-</span> <span class="n">rewards</span><span class="p">.</span><span class="nf">mean</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="n">keepdim</span><span class="o">=</span><span class="bp">True</span><span class="p">))</span> <span class="o">/</span> \
           <span class="p">(</span><span class="n">rewards</span><span class="p">.</span><span class="nf">std</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="n">keepdim</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span> <span class="o">+</span> <span class="mf">1e-8</span><span class="p">)</span>

<span class="k">def</span> <span class="nf">policy_gradient_loss</span><span class="p">(</span><span class="n">neg_logp</span><span class="p">,</span> <span class="n">advantage</span><span class="p">,</span> <span class="n">entropy</span><span class="p">,</span> <span class="n">entropy_weight</span><span class="o">=</span><span class="mf">1e-3</span><span class="p">):</span>
    <span class="c1"># neg_logp: [B, G, T]  ; advantage: [B, G]
</span>    <span class="n">adv_loss</span> <span class="o">=</span> <span class="n">neg_logp</span> <span class="o">*</span> <span class="n">advantage</span><span class="p">.</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">adv_loss</span><span class="p">.</span><span class="nf">sum</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">).</span><span class="nf">mean</span><span class="p">(</span><span class="mi">1</span><span class="p">).</span><span class="nf">sum</span><span class="p">()</span> <span class="o">-</span> <span class="n">entropy_weight</span> <span class="o">*</span> <span class="n">entropy</span>
</code></pre></div></div> <p><strong>熵正则</strong>: 鼓励探索,防止过早坍缩到一种回答模式。</p> <h3 id="策略梯度的血脉-reinforce--a2c--ppo--grpo">策略梯度的”血脉” (REINFORCE → A2C → PPO → GRPO)</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1992  Williams  REINFORCE        ∇L = -E[∇logπ · R]            ← 纯朴素,方差爆炸
                                                              
1999  Sutton    Policy Gradient  ∇L = -E[∇logπ · A]            ← 引入 advantage
      Theorem                       A = R - b(s)                   降方差
                                                              
2016  Mnih      A2C/A3C          A = Σ γ^k r_{t+k} - V(s_t)    ← 用 value 网络当 baseline
                                                              
2017  Schulman  PPO              加 ratio + clip + KL            ← 防策略跑飞
                                                              
2024  DeepSeek  GRPO             去掉 value,组内 z-score 当 A   ← 省一个模型
</code></pre></div></div> <p>每代都在解决前一代的痛点。今天 RLHF 实际用的就是 <strong>PPO</strong> 和 <strong>GRPO</strong> 两条线。</p> <h3 id="完整训练循环-伪代码">完整训练循环 (伪代码)</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">rl_finetune</span><span class="p">(</span><span class="n">policy</span><span class="p">,</span> <span class="n">ref</span><span class="p">,</span> <span class="n">rm</span><span class="p">,</span> <span class="n">prompts</span><span class="p">,</span> <span class="n">epochs</span><span class="p">):</span>
    <span class="k">for</span> <span class="n">epoch</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">epochs</span><span class="p">):</span>
        <span class="k">for</span> <span class="n">batch_prompts</span> <span class="ow">in</span> <span class="n">prompts</span><span class="p">:</span>
            <span class="c1"># ===== 采样阶段 (no grad) =====
</span>            <span class="k">with</span> <span class="n">torch</span><span class="p">.</span><span class="nf">no_grad</span><span class="p">():</span>
                <span class="n">rollouts</span> <span class="o">=</span> <span class="p">[]</span>
                <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">group_size</span><span class="p">):</span>                <span class="c1"># G 个 rollout per prompt
</span>                    <span class="n">out</span> <span class="o">=</span> <span class="n">policy</span><span class="p">.</span><span class="nf">generate</span><span class="p">(</span><span class="n">batch_prompts</span><span class="p">)</span>
                    <span class="n">rollouts</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">out</span><span class="p">)</span>
                <span class="n">rewards</span> <span class="o">=</span> <span class="nf">rm</span><span class="p">(</span><span class="n">rollouts</span><span class="p">)</span>                     <span class="c1"># [B, G]
</span>
            <span class="c1"># ===== 优势归一化 =====
</span>            <span class="n">advantage</span> <span class="o">=</span> <span class="p">(</span><span class="n">rewards</span> <span class="o">-</span> <span class="n">rewards</span><span class="p">.</span><span class="nf">mean</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="n">keepdim</span><span class="o">=</span><span class="bp">True</span><span class="p">))</span> <span class="o">/</span> \
                        <span class="p">(</span><span class="n">rewards</span><span class="p">.</span><span class="nf">std</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="n">keepdim</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span> <span class="o">+</span> <span class="mf">1e-8</span><span class="p">)</span>

            <span class="c1"># ===== 训练阶段 (需要 grad) =====
</span>            <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">ppo_epochs</span><span class="p">):</span>                    <span class="c1"># PPO 多轮利用同一批 rollout
</span>                <span class="n">logits</span> <span class="o">=</span> <span class="nf">policy</span><span class="p">(</span><span class="n">rollouts</span><span class="p">)</span>                  <span class="c1"># 重新前向
</span>                <span class="n">neg_logp</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">cross_entropy</span><span class="p">(</span><span class="n">logits</span><span class="p">,</span> <span class="n">rollouts</span><span class="p">,</span> <span class="n">reduction</span><span class="o">=</span><span class="sh">'</span><span class="s">none</span><span class="sh">'</span><span class="p">)</span>
                <span class="n">entropy</span>  <span class="o">=</span> <span class="nf">compute_entropy</span><span class="p">(</span><span class="n">logits</span><span class="p">)</span>

                <span class="n">loss</span> <span class="o">=</span> <span class="p">(</span><span class="n">neg_logp</span> <span class="o">*</span> <span class="n">advantage</span><span class="p">.</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)).</span><span class="nf">sum</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">).</span><span class="nf">mean</span><span class="p">()</span> \
                     <span class="o">-</span> <span class="n">entropy_weight</span> <span class="o">*</span> <span class="n">entropy</span>

                <span class="c1"># 可选: KL 惩罚
</span>                <span class="n">kl</span> <span class="o">=</span> <span class="nf">compute_kl</span><span class="p">(</span><span class="n">policy</span><span class="p">,</span> <span class="n">ref</span><span class="p">,</span> <span class="n">rollouts</span><span class="p">)</span>
                <span class="n">loss</span> <span class="o">+=</span> <span class="n">kl_coef</span> <span class="o">*</span> <span class="n">kl</span>

                <span class="n">loss</span><span class="p">.</span><span class="nf">backward</span><span class="p">()</span>
                <span class="n">optimizer</span><span class="p">.</span><span class="nf">step</span><span class="p">()</span>
                <span class="n">optimizer</span><span class="p">.</span><span class="nf">zero_grad</span><span class="p">()</span>
</code></pre></div></div> <h3 id="on-policy-vs-off-policy-的微妙差别">On-Policy vs Off-Policy 的微妙差别</h3> <p><strong>On-policy</strong>: 用<strong>当前</strong> π 采样的数据更新 π。每次梯度步后,旧数据就<strong>作废</strong>。 <strong>Off-policy</strong>: 用<strong>别的</strong> π (历史 / SFT / 别人的) 采样的数据更新 π。需要 importance sampling 修正。</p> <table> <thead> <tr> <th>范式</th> <th>数据来源</th> <th>修正</th> <th>复杂度</th> <th>算法</th> </tr> </thead> <tbody> <tr> <td>纯 on-policy</td> <td>π_θ (当前)</td> <td>无</td> <td>慢 (一步一采)</td> <td>REINFORCE, vanilla PG</td> </tr> <tr> <td><strong>近 on-policy</strong></td> <td>π_θ_old (几步前)</td> <td>importance ratio</td> <td>中 (一批数据多步)</td> <td><strong>PPO, GRPO</strong></td> </tr> <tr> <td>纯 off-policy</td> <td>replay buffer / SFT</td> <td>强 IS / Q-learning</td> <td>难 (分布外严重)</td> <td>DDPG, SAC, <strong>DPO</strong></td> </tr> </tbody> </table> <p>PPO 和 GRPO 的妙处: 用 importance ratio 复用同一批 rollout 训多步,<strong>省了 90%+ 的采样成本</strong>。</p> <h3 id="为什么减-baseline-不改变期望严格证明">为什么减 baseline 不改变期望(严格证明)</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>E_π[∇log π(a|s) · b(s)] = b(s) · ∫ π(a|s) · ∇log π(a|s) da
                       = b(s) · ∫ ∇π(a|s) da                ← log 求导技巧
                       = b(s) · ∇ ∫ π(a|s) da
                       = b(s) · ∇ 1 = 0
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">b(s)</code> 只要不依赖 action <code class="language-plaintext highlighter-rouge">a</code>(可以依赖 state <code class="language-plaintext highlighter-rouge">s</code>)都不改期望;但<strong>会改方差</strong>。 <strong>最优 baseline</strong>: <code class="language-plaintext highlighter-rouge">b*(s) = E[(∇log π)² · R] / E[(∇log π)²]</code> (但实践用 V(s) 已经够好)。</p> <h3 id="熵正则的具体作用">熵正则的具体作用</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">loss</span> <span class="o">=</span> <span class="o">-</span><span class="n">E</span><span class="p">[</span><span class="n">logπ</span> <span class="err">·</span> <span class="n">A</span><span class="p">]</span> <span class="o">-</span> <span class="n">β_ent</span> <span class="err">·</span> <span class="nc">H</span><span class="p">(</span><span class="n">π</span><span class="p">)</span>        <span class="c1"># H 越大,π 越分散
</span></code></pre></div></div> <table> <thead> <tr> <th>β_ent</th> <th>效果</th> </tr> </thead> <tbody> <tr> <td>0</td> <td>容易 mode collapse,最后只剩 1-2 个高 reward 答案</td> </tr> <tr> <td>1e-3 ~ 1e-2</td> <td>典型范围 (InstructGPT 用 1e-2 量级)</td> </tr> <tr> <td>&gt; 0.1</td> <td>π 太接近均匀分布,根本学不到偏好</td> </tr> </tbody> </table> <p>对 LLM,熵指 token 级别 H = -Σ p_v · log p_v 在 vocab 维度上的均值。</p> <h3 id="高频追问-5">高频追问</h3> <table> <thead> <tr> <th>Q</th> <th>A</th> </tr> </thead> <tbody> <tr> <td>为什么减 baseline 不改变期望?</td> <td><code class="language-plaintext highlighter-rouge">E[∇logπ · b] = b · ∇∫π = 0</code>,只要 b 不依赖 action a(可依赖 state s)</td> </tr> <tr> <td>为什么用组内均值?</td> <td>同 prompt 下 reward 才可比;跨 prompt 平均无意义,reward 尺度差太大</td> </tr> <tr> <td>on-policy vs off-policy?</td> <td>on: 当前 π 采样,数据用 1 次;近 on: PPO/GRPO 用 IS ratio 重用;off: DPO 直接用离线偏好数据</td> </tr> <tr> <td>熵正则太强 / 太弱?</td> <td>太强 → 不收敛,policy 一直瞎采;太弱 → 早期 mode collapse</td> </tr> <tr> <td>和 GRPO 关系?</td> <td>这一节就是 GRPO 的”裸版”(没有 PPO 的 ratio + clip);加上后变成 §9</td> </tr> <tr> <td>REINFORCE 为什么不实用?</td> <td>没有 baseline → 方差极大;每个梯度步都要 fresh 采样 → 慢</td> </tr> <tr> <td>为什么 LLM 不用 actor-critic 的 V(s)?</td> <td>(a) state 是整个 token 序列,V(s) 很难训 (b) GRPO 用组内 baseline 已经够好</td> </tr> <tr> <td>group size G 的影响?</td> <td>G 太小 (≤2) 组内 std 不稳定;G 太大 (&gt;64) 显存爆且边际收益递减;典型 8-16</td> </tr> <tr> <td>奖励信号为什么稀疏(只在 EOS 给)?</td> <td>用 RM 打分时,RM 是 sequence-level 输出。token 级 reward 需要 step-wise RM 或 process RM</td> </tr> <tr> <td>token-level 还是 sequence-level loss?</td> <td>序列级 advantage 广播到所有 token 是主流 (GRPO/PPO);DPO 直接序列级 logp 差</td> </tr> </tbody> </table> <hr/> <h2 id="7-gae--广义优势估计">7. GAE — 广义优势估计</h2> <blockquote> <p><strong>Key Insight</strong>: 用 <code class="language-plaintext highlighter-rouge">λ</code> 在「<strong>低偏差高方差</strong>(蒙特卡洛回报)」和「<strong>高偏差低方差</strong>(一步 TD)」之间插值。 关键计算:<strong>从后往前递推</strong> <code class="language-plaintext highlighter-rouge">A_t = δ_t + γλ·A_{t+1}</code>。</p> </blockquote> <h3 id="实现层次-4">实现层次</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>   输入: rewards [B, T] , values [B, T] (来自 value head)
                                │
   ════════ 从后往前递推 ════════
                                │
   t = T-1 ──┐  next_V = 0      │
             │  δ = r_{T-1} - V_{T-1}
             │  A_{T-1} = δ
             ▼
   t = T-2 ──┐  next_V = V_{T-1}
             │  δ = r_{T-2} + γ·V_{T-1} - V_{T-2}
             │  A_{T-2} = δ + γλ·A_{T-1}
             ▼
   ...        ↓ 不断累乘 γλ
   t = 0   ──┐  next_V = V_1
             │  δ = r_0 + γ·V_1 - V_0
             │  A_0 = δ + γλ·A_1
             ▼
   advantages [B, T]
                                │
                                ▼
   returns = advantages + values   (用于 value head 的 MSE)
   
   极端情况:
     λ=0  ⇒  A_t = δ_t                   (一步 TD,低方差高偏差)
     λ=1  ⇒  A_t = Σ γ^k·δ_{t+k}         (≈MC return − V,高方差低偏差)
</code></pre></div></div> <h3 id="公式-1">公式</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>TD 残差:  δ_t = r_t + γ·V(s_{t+1}) − V(s_t)
GAE:      A_t = δ_t + γλ·A_{t+1}    ← 从后往前递推
</code></pre></div></div> <h3 id="代码骨架-4">代码骨架</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">compute_gae</span><span class="p">(</span><span class="n">rewards</span><span class="p">,</span> <span class="n">values</span><span class="p">,</span> <span class="n">gamma</span><span class="o">=</span><span class="mf">0.99</span><span class="p">,</span> <span class="n">lam</span><span class="o">=</span><span class="mf">0.95</span><span class="p">):</span>
    <span class="n">advantages</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">zeros_like</span><span class="p">(</span><span class="n">values</span><span class="p">)</span>
    <span class="n">lastgae</span> <span class="o">=</span> <span class="mf">0.0</span>
    <span class="n">T</span> <span class="o">=</span> <span class="n">rewards</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="o">-</span><span class="mi">1</span><span class="p">]</span>
    <span class="k">for</span> <span class="n">t</span> <span class="ow">in</span> <span class="nf">reversed</span><span class="p">(</span><span class="nf">range</span><span class="p">(</span><span class="n">T</span><span class="p">)):</span>
        <span class="n">next_value</span> <span class="o">=</span> <span class="n">values</span><span class="p">[:,</span> <span class="n">t</span><span class="o">+</span><span class="mi">1</span><span class="p">]</span> <span class="k">if</span> <span class="n">t</span> <span class="o">&lt;</span> <span class="n">T</span><span class="o">-</span><span class="mi">1</span> <span class="k">else</span> <span class="n">torch</span><span class="p">.</span><span class="nf">zeros_like</span><span class="p">(</span><span class="n">values</span><span class="p">[:,</span> <span class="n">t</span><span class="p">])</span>
        <span class="n">delta</span> <span class="o">=</span> <span class="n">rewards</span><span class="p">[:,</span> <span class="n">t</span><span class="p">]</span> <span class="o">+</span> <span class="n">gamma</span> <span class="o">*</span> <span class="n">next_value</span> <span class="o">-</span> <span class="n">values</span><span class="p">[:,</span> <span class="n">t</span><span class="p">]</span>
        <span class="n">lastgae</span> <span class="o">=</span> <span class="n">delta</span> <span class="o">+</span> <span class="n">gamma</span> <span class="o">*</span> <span class="n">lam</span> <span class="o">*</span> <span class="n">lastgae</span>
        <span class="n">advantages</span><span class="p">[:,</span> <span class="n">t</span><span class="p">]</span> <span class="o">=</span> <span class="n">lastgae</span>
    <span class="k">return</span> <span class="n">advantages</span>
</code></pre></div></div> <p><strong>数值验证</strong>: <code class="language-plaintext highlighter-rouge">γ = λ = 1</code> 时 GAE 应该等于 <code class="language-plaintext highlighter-rouge">cumsum(rewards, from end) - values</code>。</p> <h3 id="n-step-returns-复习-gae-的前身">n-step Returns 复习 (GAE 的前身)</h3> <p>回报的几种估计:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1-step TD:   Â^(1)_t = r_t + γ·V(s_{t+1}) − V(s_t)               (= δ_t)
2-step:      Â^(2)_t = r_t + γ·r_{t+1} + γ²·V(s_{t+2}) − V(s_t)
k-step:      Â^(k)_t = Σ_{l=0}^{k-1} γ^l·r_{t+l} + γ^k·V(s_{t+k}) − V(s_t)
∞-step (MC): Â^(∞)_t = Σ_{l=0}^∞ γ^l·r_{t+l} − V(s_t)
</code></pre></div></div> <p><strong>偏差-方差权衡</strong>:</p> <ul> <li>k 小: bootstrapping V 多,V 不准 → <strong>偏差大</strong>;但 Var 小,因为只用了 k 步真实 reward</li> <li>k 大: 用更多真实 reward → <strong>偏差小</strong>;但 Var 大,因为长 trajectory 累加随机性</li> </ul> <h3 id="gae--n-step-advantages-的指数加权平均">GAE = n-step Advantages 的指数加权平均</h3> <p>Schulman 2015 的 GAE 定义:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>A^GAE(γ,λ)_t = (1-λ) · Σ_{k=1}^∞ λ^{k-1} · Â^(k)_t       ← 各 k-step 的指数加权
</code></pre></div></div> <p>代入 Â^(k) 展开,<strong>化简后等于 δ_t 的指数加权</strong>:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>A^GAE_t = Σ_{l=0}^∞ (γλ)^l · δ_{t+l}                    ← 关键化简式!
        = δ_t + γλ·δ_{t+1} + (γλ)²·δ_{t+2} + ...
</code></pre></div></div> <p>这就是为什么 GAE 的递推这么简单: <strong><code class="language-plaintext highlighter-rouge">A_t = δ_t + γλ · A_{t+1}</code></strong>(一行)。</p> <h3 id="两个极端的严格推导">两个极端的严格推导</h3> <p><strong>λ = 0</strong>: 只保留 k=1 项</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>A^GAE_t = δ_t = r_t + γ·V(s_{t+1}) − V(s_t)             ← 一步 TD
</code></pre></div></div> <p><strong>bias 高</strong>(完全信任 V),<strong>variance 低</strong>(只有一项随机)。</p> <p><strong>λ = 1</strong>: 所有项加权</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>A^GAE_t = Σ_{l=0}^∞ γ^l · δ_{t+l}
        = Σ_{l=0}^∞ γ^l · r_{t+l} − V(s_t)              ← MC return − V
</code></pre></div></div> <p><strong>bias 低</strong>(用真实 reward),<strong>variance 高</strong>(累加无限项)。</p> <p>→ 选 <code class="language-plaintext highlighter-rouge">λ ∈ (0, 1)</code> 在两者间插值,典型 <code class="language-plaintext highlighter-rouge">λ = 0.95</code>(略偏蒙特卡洛)。</p> <h3 id="γ-和-λ-的实际差别">γ 和 λ 的实际差别</h3> <table> <thead> <tr> <th>超参</th> <th>含义</th> <th>典型</th> <th>调谁</th> </tr> </thead> <tbody> <tr> <td><strong>γ (折扣)</strong></td> <td>未来 reward 衰减率</td> <td>0.99</td> <td>任务相关:长 horizon 任务接近 1,短 horizon 可低些</td> </tr> <tr> <td><strong>λ (GAE)</strong></td> <td>n-step 加权</td> <td>0.95</td> <td>bias-variance 调优,通常固定 0.9-0.97</td> </tr> </tbody> </table> <p>LLM RLHF 里: γ ≈ 1(序列只到 EOS,无需折扣)、λ ≈ 0.95-1.0。</p> <h3 id="value-head-训练">Value Head 训练</h3> <p>Actor-critic 同时优化 policy 和 value:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">PolicyValueModel</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">backbone</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">backbone</span>   <span class="o">=</span> <span class="n">backbone</span>
        <span class="n">self</span><span class="p">.</span><span class="n">policy_head</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">d</span><span class="p">,</span> <span class="n">vocab_size</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">value_head</span>  <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">d</span><span class="p">,</span> <span class="mi">1</span><span class="p">)</span>          <span class="c1"># 标量 V(s)
</span>
    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">):</span>
        <span class="n">h</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">backbone</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="nf">policy_head</span><span class="p">(</span><span class="n">h</span><span class="p">),</span> <span class="n">self</span><span class="p">.</span><span class="nf">value_head</span><span class="p">(</span><span class="n">h</span><span class="p">).</span><span class="nf">squeeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span>

<span class="c1"># 训练
</span><span class="n">logits</span><span class="p">,</span> <span class="n">V</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">rollouts</span><span class="p">)</span>
<span class="n">A_gae</span>     <span class="o">=</span> <span class="nf">compute_gae</span><span class="p">(</span><span class="n">rewards</span><span class="p">,</span> <span class="n">V</span><span class="p">,</span> <span class="n">gamma</span><span class="o">=</span><span class="mf">0.99</span><span class="p">,</span> <span class="n">lam</span><span class="o">=</span><span class="mf">0.95</span><span class="p">)</span>
<span class="n">returns</span>   <span class="o">=</span> <span class="n">A_gae</span> <span class="o">+</span> <span class="n">V</span>                                 <span class="c1"># 即 V_target
</span>
<span class="n">policy_loss</span> <span class="o">=</span> <span class="o">-</span><span class="nf">mean</span><span class="p">(</span><span class="n">logp</span> <span class="o">*</span> <span class="n">A_gae</span><span class="p">.</span><span class="nf">detach</span><span class="p">())</span>            <span class="c1"># advantage detach!
</span><span class="n">value_loss</span>  <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">mse_loss</span><span class="p">(</span><span class="n">V</span><span class="p">,</span> <span class="n">returns</span><span class="p">.</span><span class="nf">detach</span><span class="p">())</span>         <span class="c1"># returns detach!
</span><span class="n">loss</span> <span class="o">=</span> <span class="n">policy_loss</span> <span class="o">+</span> <span class="mf">0.5</span> <span class="o">*</span> <span class="n">value_loss</span>
</code></pre></div></div> <p><strong>坑</strong>: A 和 returns 都要 <code class="language-plaintext highlighter-rouge">.detach()</code>,否则 value 梯度会污染 policy 梯度。</p> <h3 id="稀疏-reward-是怎么传回去的">稀疏 reward 是怎么传回去的</h3> <p>LLM RLHF 中,RM 只在 EOS 给一个 scalar reward。其他 token reward = 0。 GAE 把这个唯一的非零 reward 折扣回传:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>r = [0, 0, 0, ..., 0, R_final]              # 只有最后一步
γ = 1, λ = 1:
A_t = R_final - V(s_t)                       # 所有 t 都拿到 R_final 信号

γ &lt; 1, λ &lt; 1:
A_t = γ^(T-t-1) · R_final + bootstrap(V)
</code></pre></div></div> <p>→ 即使 reward 极稀疏,GAE + V 也能给每个 token 一个 advantage 信号。</p> <h3 id="高频追问-6">高频追问</h3> <table> <thead> <tr> <th>Q</th> <th>A</th> </tr> </thead> <tbody> <tr> <td>γ 和 λ 各管什么?</td> <td>γ 是回报折扣(管”未来 reward 看多远”);λ 管”信任 value 估计 vs 真实回报”的程度</td> </tr> <tr> <td>为什么从后往前?</td> <td>递推式 <code class="language-plaintext highlighter-rouge">A_t = δ_t + γλ·A_{t+1}</code> 依赖 t+1 的值,所以反向遍历</td> </tr> <tr> <td>GAE 怎么从 n-step 推出来?</td> <td>n-step advantages 的指数加权平均,化简得 <code class="language-plaintext highlighter-rouge">Σ (γλ)^l · δ_{t+l}</code>,等价于 <code class="language-plaintext highlighter-rouge">A_t = δ_t + γλ·A_{t+1}</code></td> </tr> <tr> <td>Actor 和 Critic 为什么共享 backbone?</td> <td>省显存 + 表征共享;value head 只是个 <code class="language-plaintext highlighter-rouge">Linear(dim, 1)</code></td> </tr> <tr> <td>Value loss 怎么算?</td> <td><code class="language-plaintext highlighter-rouge">MSE(V(s_t), returns_t)</code>,<code class="language-plaintext highlighter-rouge">returns = advantages + values</code></td> </tr> <tr> <td>value 和 reward 的区别?</td> <td>reward 是即时奖励 r_t;value V(s_t) 是从 s_t 起的预期总折扣回报</td> </tr> <tr> <td>advantage 为什么要 detach?</td> <td>进 policy loss 时要,否则 value 反向梯度会污染 policy</td> </tr> <tr> <td>稀疏 reward (只有最后一步)怎么办?</td> <td>GAE 会把最后一步的 reward 沿 γ^t 折扣回传到所有时间步</td> </tr> <tr> <td>GAE 在 LLM 里是 token 级还是序列级?</td> <td>token 级:每个 token 有自己的 A_t。但 reward 一般只在 EOS 给</td> </tr> <tr> <td>为什么 GRPO 不用 GAE?</td> <td>GRPO 砍掉 value 网络,直接用组内 z-score。代价: 没法 token 级精细化</td> </tr> </tbody> </table> <hr/> <h2 id="8-ppo--clipped-surrogate--kl">8. PPO — Clipped Surrogate + KL</h2> <blockquote> <p><strong>Key Insight</strong>: 用 importance sampling ratio 修正 off-policy + <strong>clip 防止策略跑飞</strong> + KL 约束别偏离 SFT 太远。 4 个模型 (policy / ref / reward / value) 是 PPO 工程痛点。</p> </blockquote> <h3 id="实现层次-5">实现层次</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>══════════ RLHF 三步走 ══════════
   Step 1: SFT             → π_SFT
   Step 2: 训 Reward Model → RM   (见 §5)
   Step 3: PPO 优化 π             ← 本节

══════════ PPO 一步更新的 4 个模型 ══════════

                        ┌──────────────────────────────┐
                        │  采样: π_old.sample(prompt)   │
                        │  得到 (a, logp_old) 缓存       │
                        └────────────┬──────────────────┘
                                     │ rollout
                                     ▼
   ┌───────────────┬─────────────────┴────────────────┬───────────────┐
   ▼               ▼                                  ▼               ▼
policy(πθ)      ref(π_SFT)                     reward(RM)         value(V)
要梯度           冻结                            冻结                要梯度
   │               │                                  │               │
logp_new         logp_ref                          rewards          V(s)
   │               │                                  │               │
   │     KL(π_θ ‖ π_ref) per token                    │       GAE(rewards, V)
   │               │                                  │               │
   │               └─── β·KL ───┐                     │               │
   │                            ▼                     │               ▼
   │                  shaped_reward = r − β·KL        │           advantages
   │                                                  │
   └────── ratio = exp(logp_new − logp_old) ──────────┘
                       │
            ┌──────────┴──────────┐
            ▼                     ▼
       ratio·A          clip(ratio, 1±ε)·A
            └───────── min() ─────┘
                       ▼
                   −mean = PPO loss
                       ▼
                   backward → 更新 policy + value
</code></pre></div></div> <h3 id="公式-2">公式</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ratio_t = exp(logπ_θ(a_t) - logπ_old(a_t))    # 重要性采样比
L = -E[ min( ratio · A, clip(ratio, 1-ε, 1+ε) · A ) ]
</code></pre></div></div> <p><strong>为什么 clip</strong>: 防止单步更新太大让策略跑飞。<code class="language-plaintext highlighter-rouge">ε = 0.2</code> 是经典值。</p> <h3 id="kl-惩罚">KL 惩罚</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>advantage = reward - β · KL(π_θ ‖ π_ref)
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">π_ref</code> = SFT 的冻结副本,<strong>全程不更新</strong>。这个 KL 防止 policy 偏离语言模型本能(避免 reward hacking)。</p> <h3 id="代码骨架-5">代码骨架</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">ppo_loss</span><span class="p">(</span><span class="n">logp_new</span><span class="p">,</span> <span class="n">logp_ref</span><span class="p">,</span> <span class="n">advantages</span><span class="p">,</span> <span class="n">clip</span><span class="o">=</span><span class="mf">0.2</span><span class="p">):</span>
    <span class="n">ratio</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">logp_new</span> <span class="o">-</span> <span class="n">logp_ref</span><span class="p">.</span><span class="nf">detach</span><span class="p">())</span>
    <span class="n">unclipped</span> <span class="o">=</span> <span class="n">ratio</span> <span class="o">*</span> <span class="n">advantages</span>
    <span class="n">clipped</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">clamp</span><span class="p">(</span><span class="n">ratio</span><span class="p">,</span> <span class="mf">1.0</span> <span class="o">-</span> <span class="n">clip</span><span class="p">,</span> <span class="mf">1.0</span> <span class="o">+</span> <span class="n">clip</span><span class="p">)</span> <span class="o">*</span> <span class="n">advantages</span>
    <span class="k">return</span> <span class="o">-</span><span class="n">torch</span><span class="p">.</span><span class="nf">mean</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nf">min</span><span class="p">(</span><span class="n">unclipped</span><span class="p">,</span> <span class="n">clipped</span><span class="p">))</span>

<span class="k">def</span> <span class="nf">apply_kl_penalty</span><span class="p">(</span><span class="n">rewards</span><span class="p">,</span> <span class="n">logp_new_dist</span><span class="p">,</span> <span class="n">logp_ref_dist</span><span class="p">,</span> <span class="n">kl_coef</span><span class="o">=</span><span class="mf">0.01</span><span class="p">):</span>
    <span class="n">kl</span> <span class="o">=</span> <span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">logp_new_dist</span><span class="p">)</span> <span class="o">*</span> <span class="p">(</span><span class="n">logp_new_dist</span> <span class="o">-</span> <span class="n">logp_ref_dist</span><span class="p">.</span><span class="nf">detach</span><span class="p">())).</span><span class="nf">sum</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">rewards</span> <span class="o">-</span> <span class="n">kl_coef</span> <span class="o">*</span> <span class="n">kl</span>
</code></pre></div></div> <h3 id="clip-的可视化理解-min-的含义">Clip 的可视化(理解 min 的含义)</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>                Advantage &gt; 0 (好动作,想拉高 logp)
                ┌──────────────────────────────┐
                │  ratio 增大方向                │
                │                              │
   ratio·A      │      ╱                       │ ← unclipped:无限抬升
                │     ╱                        │
                │    ╱                         │
                │   ╱                          │
   1+ε ─────────│──╱──────────── clipped       │ ← clipped:被天花板限制
                │ ╱            ────────────    │
                │╱                             │
   ratio=1 ─────┼──────────────────────────────│
                ↑                              ↓
                │  ratio 减小方向                │
                │                              │
              对 A &gt; 0: 选 min → 取被 clip 的(保守,防过度拉高)


                Advantage &lt; 0 (差动作,想压低 logp)
                ┌──────────────────────────────┐
   ratio=1 ─────┼──────────────────────────────│
                │\                             │
                │ \                            │
                │  \                           │
   1-ε ─────────│---\---────────── clipped     │
                │    \           ─────────     │
                │     \                        │
                │      \                       │
   ratio·A      │       \                      │ ← unclipped:无限下压
                │        \                     │
                └──────────────────────────────┘

              对 A &lt; 0: 选 min → 取没被 clip 的(继续压低,不饶过坏动作)
</code></pre></div></div> <p><strong>核心直觉</strong>: 对正 A,<strong>限上界防止过度乐观</strong>;对负 A,<strong>不限下界保留惩罚力度</strong>。 这是 PPO 比 TRPO 简洁但等效的关键。</p> <h3 id="multi-epoch-训练-为什么能复用同一批-rollout">Multi-Epoch 训练: 为什么能复用同一批 rollout</h3> <p>朴素 on-policy: 一批数据用 1 个梯度步,扔掉,重新采样 — 极慢。 PPO: 一批数据用 <strong>k 个 epoch</strong>(典型 k=4)的梯度更新,通过 <code class="language-plaintext highlighter-rouge">ratio</code> 修正 off-policy 偏差:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span> <span class="n">batch</span> <span class="ow">in</span> <span class="n">batches</span><span class="p">:</span>
    <span class="n">rollouts</span><span class="p">,</span> <span class="n">logp_old</span> <span class="o">=</span> <span class="nf">sample</span><span class="p">(</span><span class="n">policy</span><span class="p">)</span>          <span class="c1"># 采一次
</span>    <span class="k">for</span> <span class="n">ppo_epoch</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">4</span><span class="p">):</span>                    <span class="c1"># ← 复用 4 次
</span>        <span class="n">logp_new</span> <span class="o">=</span> <span class="nf">policy</span><span class="p">(</span><span class="n">rollouts</span><span class="p">)</span>
        <span class="n">ratio</span> <span class="o">=</span> <span class="nf">exp</span><span class="p">(</span><span class="n">logp_new</span> <span class="o">-</span> <span class="n">logp_old</span><span class="p">)</span>         <span class="c1"># ratio 起飞表示 policy 漂太远
</span>        <span class="n">loss</span> <span class="o">=</span> <span class="nf">clipped_surrogate</span><span class="p">(</span><span class="n">ratio</span><span class="p">,</span> <span class="n">A</span><span class="p">)</span>
        <span class="n">loss</span><span class="p">.</span><span class="nf">backward</span><span class="p">();</span> <span class="nf">step</span><span class="p">()</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">ratio</code> 越接近 1 → 策略和 old 没差多远 → IS 估计准;<code class="language-plaintext highlighter-rouge">ratio</code> 离 1 远 → clip 生效避免崩。 <strong>多 epoch 是 PPO 比 REINFORCE 快 10× 的根本原因</strong>。</p> <h3 id="token-level-vs-sequence-level-ppo">Token-level vs Sequence-level PPO</h3> <p>LLM 里的 a_t 是 token,不是动作。两种实现:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Token-level (主流):
   logp(a_t)        每个 token 单独算 logπ(token | prefix)
   ratio_t          per-token ratio
   loss = mean_t mean_seq min(ratio_t · A_t, clip)

Sequence-level (DPO 风格):
   logp(seq) = Σ_t logp(a_t)
   ratio = exp(logp_new(seq) - logp_old(seq))
   loss = min(ratio · A_seq, clip)
</code></pre></div></div> <p>Token-level 更细致,能给 GAE 提供 per-token A_t;sequence-level 更稳定,梯度信号简洁。</p> <h3 id="4-个模型-ppo-工程痛点">4 个模型 (PPO 工程痛点)</h3> <table> <thead> <tr> <th>模型</th> <th>角色</th> <th>是否更新</th> <th>显存代价</th> </tr> </thead> <tbody> <tr> <td><strong>policy</strong></td> <td>主角,被 RL 训练</td> <td>✅</td> <td>1× + 优化器 ~5×</td> </tr> <tr> <td><strong>ref</strong> (= SFT 冻结)</td> <td>算 KL 惩罚</td> <td>❌</td> <td>1× (inference only)</td> </tr> <tr> <td><strong>reward</strong></td> <td>RM 给分</td> <td>❌</td> <td>1× (inference only)</td> </tr> <tr> <td><strong>value</strong></td> <td>GAE baseline (actor-critic)</td> <td>✅ (共享 backbone)</td> <td>0.1× (只是个 head)</td> </tr> </tbody> </table> <p>总显存 ≈ <strong>3-4 倍模型大小</strong>。这就是为什么 RLHF PPO 难做 70B+ 模型,GRPO 把 value 砍了缓解。</p> <h3 id="ppo-完整-loss">PPO 完整 loss</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">ppo_total_loss</span><span class="p">(</span><span class="n">logp_new</span><span class="p">,</span> <span class="n">logp_old</span><span class="p">,</span> <span class="n">A</span><span class="p">,</span> <span class="n">V</span><span class="p">,</span> <span class="n">returns</span><span class="p">,</span> <span class="n">dist_new</span><span class="p">,</span> <span class="n">dist_ref</span><span class="p">):</span>
    <span class="c1"># 1. Policy loss (clipped surrogate)
</span>    <span class="n">ratio</span>   <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">logp_new</span> <span class="o">-</span> <span class="n">logp_old</span><span class="p">)</span>
    <span class="n">policy</span>  <span class="o">=</span> <span class="o">-</span><span class="n">torch</span><span class="p">.</span><span class="nf">min</span><span class="p">(</span><span class="n">ratio</span> <span class="o">*</span> <span class="n">A</span><span class="p">,</span>
                          <span class="n">torch</span><span class="p">.</span><span class="nf">clamp</span><span class="p">(</span><span class="n">ratio</span><span class="p">,</span> <span class="mi">1</span><span class="o">-</span><span class="n">ε</span><span class="p">,</span> <span class="mi">1</span><span class="o">+</span><span class="n">ε</span><span class="p">)</span> <span class="o">*</span> <span class="n">A</span><span class="p">).</span><span class="nf">mean</span><span class="p">()</span>

    <span class="c1"># 2. Value loss
</span>    <span class="n">value</span>   <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">mse_loss</span><span class="p">(</span><span class="n">V</span><span class="p">,</span> <span class="n">returns</span><span class="p">)</span>

    <span class="c1"># 3. Entropy bonus
</span>    <span class="n">entropy</span> <span class="o">=</span> <span class="o">-</span><span class="p">(</span><span class="n">dist_new</span><span class="p">.</span><span class="nf">exp</span><span class="p">()</span> <span class="o">*</span> <span class="n">dist_new</span><span class="p">).</span><span class="nf">sum</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">).</span><span class="nf">mean</span><span class="p">()</span>

    <span class="c1"># 4. KL penalty (optional, 也可以放在 reward shaping 里)
</span>    <span class="n">kl</span>      <span class="o">=</span> <span class="p">(</span><span class="n">dist_new</span><span class="p">.</span><span class="nf">exp</span><span class="p">()</span> <span class="o">*</span> <span class="p">(</span><span class="n">dist_new</span> <span class="o">-</span> <span class="n">dist_ref</span><span class="p">)).</span><span class="nf">sum</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">).</span><span class="nf">mean</span><span class="p">()</span>

    <span class="k">return</span> <span class="n">policy</span> <span class="o">+</span> <span class="n">c_v</span> <span class="o">*</span> <span class="n">value</span> <span class="o">-</span> <span class="n">c_ent</span> <span class="o">*</span> <span class="n">entropy</span> <span class="o">+</span> <span class="n">β_kl</span> <span class="o">*</span> <span class="n">kl</span>
</code></pre></div></div> <p>InstructGPT 经验值: <code class="language-plaintext highlighter-rouge">c_v = 0.5, c_ent = 0.01, β_kl = 0.1, ε = 0.2</code>。</p> <h3 id="高频追问-7">高频追问</h3> <table> <thead> <tr> <th>Q</th> <th>A</th> </tr> </thead> <tbody> <tr> <td>为什么要 clip?</td> <td>单步更新太大会让 ratio 离 1 太远 → importance sampling 估计崩;clip 提供”信任域”</td> </tr> <tr> <td>ε 太大/太小?</td> <td>太大失去 clip 意义,等同朴素 PG;太小更新太保守,收敛慢。0.2 是经验最优</td> </tr> <tr> <td>为什么需要 KL?</td> <td>RM 是不完美的代理,policy 全力对抗 RM 会 reward hacking;KL 限制偏离 SFT</td> </tr> <tr> <td>Min(unclipped, clipped) 的直觉?</td> <td>选<strong>保守</strong>的那一边:正 A 限上界、负 A 不限下界(本来就要压)</td> </tr> <tr> <td>Ratio 用 logp_new - logp_old 不是 logp_ref?</td> <td><code class="language-plaintext highlighter-rouge">old</code> 是<strong>采样时的 policy 快照</strong> (每个 minibatch 之前 detach);ref 是 SFT 副本,做 KL 用</td> </tr> <tr> <td>PPO 为什么能多 epoch?</td> <td>用 ratio 修正 off-policy,只要 ratio 没漂太远(被 clip 控住),数据可复用 4-8 次</td> </tr> <tr> <td>token-level 还是 sequence-level?</td> <td>LLM 通常 token-level,提供 per-token advantage;sequence-level 适合稀疏 reward</td> </tr> <tr> <td>value head 怎么初始化?</td> <td>(a) 从 policy 复用 backbone + 随机初始 head (b) 单独训几个 epoch 让 value 先 warm up</td> </tr> <tr> <td>PPO vs TRPO?</td> <td>TRPO 用二阶 + KL 信任域 (严格但贵);PPO 用一阶 + clip 近似 (便宜 90% 效果)</td> </tr> <tr> <td>4 个模型显存怎么压?</td> <td>(a) ref 用 LoRA delta (b) value 共享 backbone (c) reward 量化 (d) 用 GRPO 砍 value</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">ratio.mean()</code> 应该等于多少?</td> <td>应该 ≈ 1。&gt; 1.5 或 &lt; 0.5 表示 policy 漂太远,该早停或减小 lr</td> </tr> </tbody> </table> <hr/> <h2 id="9-grpo--ppo-砍掉-value-网络">9. GRPO — PPO 砍掉 Value 网络</h2> <blockquote> <p><strong>Key Insight</strong>: 同 prompt 的 G 个 rollout 组内 z-score 就是个<strong>很好的 baseline</strong>,不再需要 value 网络。 省一个模型 = 省 25% 显存,适合<strong>可验证 reward</strong>(数学/代码)。</p> </blockquote> <h3 id="实现层次-6">实现层次</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>══════════ PPO vs GRPO 差在哪 ══════════

PPO:    policy + ref + reward + VALUE  (4 个模型, GAE 算 A)
GRPO:   policy + ref + reward          (3 个模型, 组内归一化算 A)
                              ↑
                       省掉的就是 value


══════════ GRPO 一步更新 ══════════

  prompt
     │
     ▼ policy.sample × G             一个 prompt 采 G 个 rollout
  rollouts [B, G]
     │
     ├──► RM 打分 ─────► rewards [B, G]
     │                       │
     │                       ▼  组内 z-score (不需要 value!)
     │             advantage = (r - r.mean(-1)) / (r.std(-1) + ε)
     │                       │
     │                       ▼
     │             advantage [B, G]
     │
     ├──► policy 重新前向 ─► logp_policy [B, G]
     │
     └──► ref (本轮 deepcopy) ─► logp_ref [B, G]
                                     │
                  ratio = exp(logp_policy - logp_ref)
                                     │
                  ┌──────────────────┴──────────────────┐
                  ▼                                     ▼
            ratio · A                       clip(ratio, 1±ε) · A
                  └──────────── min() ──────────────────┘
                                ▼
                          −mean = GRPO loss
                                ▼
                          backward → 更新 policy
</code></pre></div></div> <h3 id="公式-3">公式</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>A_i = (r_i - mean(r)) / (std(r) + eps)     ← 组内 z-score
ratio_i = exp(logπ_θ(rollout_i) - logπ_ref(rollout_i))
L = -E[ min(ratio_i · A_i, clip(ratio_i, 1-ε, 1+ε) · A_i) ]
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">π_ref</code> 不再是 SFT 副本,而是<strong>这一轮 RL step 开始时 deepcopy 的快照</strong>(每步刷新)。</p> <h3 id="代码骨架-6">代码骨架</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">grpo_loss</span><span class="p">(</span><span class="n">logp_policy</span><span class="p">,</span> <span class="n">logp_ref</span><span class="p">,</span> <span class="n">rewards</span><span class="p">,</span> <span class="n">epsilon</span><span class="o">=</span><span class="mf">0.2</span><span class="p">):</span>
    <span class="n">advantage</span> <span class="o">=</span> <span class="p">(</span><span class="n">rewards</span> <span class="o">-</span> <span class="n">rewards</span><span class="p">.</span><span class="nf">mean</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="n">keepdim</span><span class="o">=</span><span class="bp">True</span><span class="p">))</span> <span class="o">/</span> \
                <span class="p">(</span><span class="n">rewards</span><span class="p">.</span><span class="nf">std</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="n">keepdim</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span> <span class="o">+</span> <span class="mf">1e-8</span><span class="p">)</span>
    <span class="n">ratio</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">logp_policy</span> <span class="o">-</span> <span class="n">logp_ref</span><span class="p">.</span><span class="nf">detach</span><span class="p">())</span>
    <span class="n">unclipped</span> <span class="o">=</span> <span class="n">ratio</span> <span class="o">*</span> <span class="n">advantage</span>
    <span class="n">clipped</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">clamp</span><span class="p">(</span><span class="n">ratio</span><span class="p">,</span> <span class="mi">1</span> <span class="o">-</span> <span class="n">epsilon</span><span class="p">,</span> <span class="mi">1</span> <span class="o">+</span> <span class="n">epsilon</span><span class="p">)</span> <span class="o">*</span> <span class="n">advantage</span>
    <span class="k">return</span> <span class="o">-</span><span class="n">torch</span><span class="p">.</span><span class="nf">minimum</span><span class="p">(</span><span class="n">unclipped</span><span class="p">,</span> <span class="n">clipped</span><span class="p">).</span><span class="nf">mean</span><span class="p">()</span>
</code></pre></div></div> <h3 id="grpo-vs-ppo-对比表">GRPO vs PPO 对比表</h3> <table> <thead> <tr> <th>维度</th> <th>PPO</th> <th>GRPO</th> </tr> </thead> <tbody> <tr> <td>Value 网络</td> <td>必须</td> <td>砍掉</td> </tr> <tr> <td>Advantage</td> <td>GAE(rewards, V)</td> <td>组内归一化 reward</td> </tr> <tr> <td>Ref policy</td> <td>SFT 副本(冻结)</td> <td>每步 deepcopy 的快照</td> </tr> <tr> <td>显存</td> <td>4 模型</td> <td>3 模型(policy + ref + RM/verifier)</td> </tr> <tr> <td>适合任务</td> <td>通用 RLHF</td> <td>有可验证 reward(数学 / 代码)</td> </tr> <tr> <td>代表</td> <td>InstructGPT, ChatGPT 早期</td> <td><strong>DeepSeek-R1, DeepSeek-Math</strong></td> </tr> </tbody> </table> <h3 id="grpo-vs-rloo-vs-vanilla-pg">GRPO vs RLOO vs Vanilla PG</h3> <table> <thead> <tr> <th>算法</th> <th>Baseline</th> <th>Off-policy 修正</th> <th>代表</th> </tr> </thead> <tbody> <tr> <td><strong>REINFORCE</strong></td> <td>无</td> <td>无</td> <td>教科书</td> </tr> <tr> <td><strong>Vanilla PG with mean</strong></td> <td>全 batch 均值</td> <td>无</td> <td>早期</td> </tr> <tr> <td><strong>RLOO (REINFORCE Leave-One-Out)</strong></td> <td>同 prompt <strong>其他 G-1 个</strong> rollout 的均值</td> <td>无 (on-policy)</td> <td>Cohere 用</td> </tr> <tr> <td><strong>GRPO</strong></td> <td>组内 z-score (mean + std 归一化)</td> <td>ratio + clip</td> <td>DeepSeek</td> </tr> <tr> <td><strong>PPO with value</strong></td> <td>V(s)</td> <td>ratio + clip</td> <td>InstructGPT</td> </tr> </tbody> </table> <p>GRPO 比 RLOO 多了 std 归一化(让 advantage 尺度稳定)+ PPO 的 clip(允许 multi-epoch)。</p> <h3 id="deepseek-r1-的具体配置">DeepSeek-R1 的具体配置</h3> <p>DeepSeek-R1 论文 (2025) 用 GRPO 训了一个<strong>仅靠 RL 就能 reasoning</strong> 的模型:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>配置:
  Group size G = 64        ← 比 InstructGPT 大很多
  ε_clip      = 0.2
  β_KL        = 0          ← 不加 KL!直接让 policy 探索
  Reward      = 0/1 数学正确性 (verifier, 不用 RM)
  Ref         = 每个 RL step 之前 deepcopy 的快照

为什么能 work?
  (1) 数学/代码任务有 verifiable reward,不会 reward hacking → 不需要 KL
  (2) 大 G 让组内 z-score 估计稳定
  (3) 长 CoT 自动涌现:奖励正确就行,policy 学会"先想后答"
</code></pre></div></div> <p>观察: SFT 不需要长 CoT 数据,RL 自己会涌现”先思考再答案”的链。</p> <h3 id="长度归一化问题-length-normalization">长度归一化问题 (length normalization)</h3> <p>GRPO 序列级 logp 会偏向短回答(每多一个 token 多一个 logp 项,绝对值变大):</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>logp_policy(seq_short) = log p(t1) + log p(t2)              # 2 项
logp_policy(seq_long)  = log p(t1) + ... + log p(t50)       # 50 项, 绝对值偏大
</code></pre></div></div> <p>→ ratio 受序列长度影响。修法:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Token-mean normalization (DeepSeek-Math 用)
</span><span class="n">logp_seq</span> <span class="o">=</span> <span class="n">neg_logp</span><span class="p">.</span><span class="nf">sum</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span> <span class="o">/</span> <span class="n">mask</span><span class="p">.</span><span class="nf">sum</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span>        <span class="c1"># 除以序列长度
</span>
<span class="c1"># Or 算 per-token 的 ratio 和 advantage,然后逐 token 加权
</span><span class="n">ratio_t</span> <span class="o">=</span> <span class="nf">exp</span><span class="p">(</span><span class="n">logp_new_t</span> <span class="o">-</span> <span class="n">logp_old_t</span><span class="p">)</span>            <span class="c1"># 每个 token 独立
</span><span class="n">loss_t</span>  <span class="o">=</span> <span class="nf">min</span><span class="p">(</span><span class="n">ratio_t</span> <span class="err">·</span> <span class="n">A</span><span class="p">,</span> <span class="nf">clip</span><span class="p">(</span><span class="n">ratio_t</span><span class="p">,</span> <span class="mi">1</span><span class="err">±</span><span class="n">ε</span><span class="p">)</span> <span class="err">·</span> <span class="n">A</span><span class="p">)</span>
<span class="n">loss</span>    <span class="o">=</span> <span class="n">loss_t</span><span class="p">.</span><span class="nf">sum</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">).</span><span class="nf">mean</span><span class="p">()</span>                   <span class="c1"># 沿 token 加和
</span></code></pre></div></div> <p>DeepSeek-V3/R1 实际用的是 <strong>token-level loss + length normalization</strong> 的组合。</p> <h3 id="verifier-vs-reward-model">Verifier vs Reward Model</h3> <p>GRPO 不一定要 RM,可以用<strong>可验证的 verifier</strong>:</p> <table> <thead> <tr> <th>任务</th> <th>Verifier</th> <th>优势</th> </tr> </thead> <tbody> <tr> <td>数学题</td> <td>答案正则匹配 / SymPy 化简</td> <td>无 reward hacking</td> </tr> <tr> <td>代码题</td> <td>跑测试用例,通过率</td> <td>客观</td> </tr> <tr> <td>自然语言</td> <td>用 RM (LLM-as-judge 或学习的 RM)</td> <td>灵活但有 hacking 风险</td> </tr> </tbody> </table> <p>DeepSeek-R1 用了<strong>纯 verifier</strong>(<code class="language-plaintext highlighter-rouge">0/1</code> 准确率),所以才能砍掉 KL 还稳定。</p> <h3 id="完整-grpo-训练循环">完整 GRPO 训练循环</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">grpo_train_step</span><span class="p">(</span><span class="n">policy</span><span class="p">,</span> <span class="n">prompts</span><span class="p">,</span> <span class="n">verifier</span><span class="p">,</span> <span class="n">G</span><span class="o">=</span><span class="mi">16</span><span class="p">,</span> <span class="n">eps</span><span class="o">=</span><span class="mf">0.2</span><span class="p">):</span>
    <span class="c1"># === 采样阶段 ===
</span>    <span class="n">ref</span> <span class="o">=</span> <span class="n">copy</span><span class="p">.</span><span class="nf">deepcopy</span><span class="p">(</span><span class="n">policy</span><span class="p">).</span><span class="nf">eval</span><span class="p">()</span>          <span class="c1"># 本轮 ref 快照
</span>    <span class="k">with</span> <span class="n">torch</span><span class="p">.</span><span class="nf">no_grad</span><span class="p">():</span>
        <span class="n">rollouts</span> <span class="o">=</span> <span class="p">[</span><span class="n">policy</span><span class="p">.</span><span class="nf">generate</span><span class="p">(</span><span class="n">p</span><span class="p">)</span> <span class="k">for</span> <span class="n">p</span> <span class="ow">in</span> <span class="n">prompts</span> <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">G</span><span class="p">)]</span>
        <span class="n">rewards</span>  <span class="o">=</span> <span class="nf">verifier</span><span class="p">(</span><span class="n">rollouts</span><span class="p">)</span>            <span class="c1"># [B, G],可验证 reward
</span>        <span class="n">logp_old</span> <span class="o">=</span> <span class="nf">compute_logp</span><span class="p">(</span><span class="n">ref</span><span class="p">,</span> <span class="n">rollouts</span><span class="p">)</span>   <span class="c1"># ref = policy snapshot
</span>
    <span class="c1"># === 组内归一化 ===
</span>    <span class="n">A</span> <span class="o">=</span> <span class="p">(</span><span class="n">rewards</span> <span class="o">-</span> <span class="n">rewards</span><span class="p">.</span><span class="nf">mean</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="n">keepdim</span><span class="o">=</span><span class="bp">True</span><span class="p">))</span> <span class="o">/</span> \
        <span class="p">(</span><span class="n">rewards</span><span class="p">.</span><span class="nf">std</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="n">keepdim</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span> <span class="o">+</span> <span class="mf">1e-8</span><span class="p">)</span>

    <span class="c1"># === 多 epoch PPO 风格更新 ===
</span>    <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">ppo_epochs</span><span class="p">):</span>
        <span class="n">logp_new</span> <span class="o">=</span> <span class="nf">compute_logp</span><span class="p">(</span><span class="n">policy</span><span class="p">,</span> <span class="n">rollouts</span><span class="p">)</span>
        <span class="n">ratio</span>    <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">logp_new</span> <span class="o">-</span> <span class="n">logp_old</span><span class="p">)</span>
        <span class="n">loss</span>     <span class="o">=</span> <span class="o">-</span><span class="n">torch</span><span class="p">.</span><span class="nf">min</span><span class="p">(</span><span class="n">ratio</span> <span class="o">*</span> <span class="n">A</span><span class="p">,</span>
                              <span class="n">torch</span><span class="p">.</span><span class="nf">clamp</span><span class="p">(</span><span class="n">ratio</span><span class="p">,</span> <span class="mi">1</span><span class="o">-</span><span class="n">eps</span><span class="p">,</span> <span class="mi">1</span><span class="o">+</span><span class="n">eps</span><span class="p">)</span> <span class="o">*</span> <span class="n">A</span><span class="p">).</span><span class="nf">mean</span><span class="p">()</span>
        <span class="n">loss</span><span class="p">.</span><span class="nf">backward</span><span class="p">();</span> <span class="nf">step</span><span class="p">()</span>
</code></pre></div></div> <h3 id="高频追问-8">高频追问</h3> <table> <thead> <tr> <th>Q</th> <th>A</th> </tr> </thead> <tbody> <tr> <td>为什么组内归一化能替代 value baseline?</td> <td>同 prompt 下 reward 才可比,组内均值是无偏 baseline 估计</td> </tr> <tr> <td>Group size G 怎么选?</td> <td>InstructGPT 8;DeepSeek-R1 用 64。大 G 估计准但显存爆;小 G 方差大</td> </tr> <tr> <td>没 value 网络怎么处理 token-level?</td> <td>序列级 A 广播到所有 token (typical);或 token-level loss + length norm</td> </tr> <tr> <td>为什么 ref 每步 deepcopy 而不是固定 SFT?</td> <td>GRPO 的 ratio 主要做 off-policy 修正(单批数据多轮更新);不需要长期 KL 约束</td> </tr> <tr> <td>GRPO vs RLOO 区别?</td> <td>RLOO 用 leave-one-out 均值;GRPO 多了 std 归一化 + PPO clip 支持多 epoch</td> </tr> <tr> <td>DeepSeek-R1 为什么能不加 KL?</td> <td>数学/代码用 verifier (0/1 正确性),不会 reward hacking</td> </tr> <tr> <td>长 CoT 怎么自然涌现?</td> <td>大 G 探索充足 + 正确答案被加权 → policy 学会先想后答(无监督)</td> </tr> <tr> <td>长度偏置怎么处理?</td> <td>Token-mean normalization (logp / 序列长度) 或 per-token loss</td> </tr> <tr> <td>GRPO 能 multi-epoch 吗?</td> <td>能,和 PPO 一样靠 ratio + clip 控住 off-policy 漂移</td> </tr> <tr> <td>GRPO 适合什么、不适合什么?</td> <td>适合: 数学/代码/有 verifier 的任务。不适合: 主观对话 (需要 RM 的偏好)</td> </tr> </tbody> </table> <hr/> <h2 id="10-dpo--把-rm--rl-合成一个分类损失">10. DPO — 把 RM + RL 合成一个分类损失</h2> <blockquote> <p><strong>Key Insight</strong>: 「最大化 reward + KL 约束」的最优解 → 反解出 <code class="language-plaintext highlighter-rouge">r = β·log(π/π_ref)</code> → 代入 Bradley-Terry → <strong>RM 这一步消失</strong>,变成一个 sigmoid 分类损失。</p> </blockquote> <h3 id="实现层次-7">实现层次</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>══════════ 数据 ══════════
   (prompt, y_w (chosen tokens), y_l (rejected tokens))


══════════ 4 次前向 = 2 (policy/ref) × 2 (chosen/rejected) ══════════

                ┌──────────────────── policy(πθ) ────────────────────┐
                │                                                     │
   y_w ────────►│ logits_w ──► seq_logprob ──► π_w  (要梯度)         │
                │                                                     │
   y_l ────────►│ logits_l ──► seq_logprob ──► π_l  (要梯度)         │
                └─────────────────────────────────────────────────────┘

                ┌──────────────────── ref(π_SFT) ─────────────────────┐
                │  (冻结, with torch.no_grad())                       │
   y_w ────────►│ logits  ──► seq_logprob ──► r_w                    │
                │                                                     │
   y_l ────────►│ logits  ──► seq_logprob ──► r_l                    │
                └─────────────────────────────────────────────────────┘

══════════ 损失 ══════════

   margin = (π_w − r_w) − (π_l − r_l)        ← chosen 相对 ref 的 log-ratio
                                                减 rejected 相对 ref 的 log-ratio
   logits = β · margin
   loss   = −log σ(logits) = −F.logsigmoid(logits).sum()
                              │
                              ▼ backward → 只更新 policy

══════════ 模型数 ══════════
   policy (训练中) + ref (冻结) = 2 个    ← 比 PPO 的 4 个少一半
</code></pre></div></div> <h3 id="公式-4">公式</h3> <p>把隐式 reward <code class="language-plaintext highlighter-rouge">r(x,y) = β·log(π_θ/π_ref)</code> 代入 Bradley-Terry:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>L = -log σ( β · [ (logπ_θ(y_w) - logπ_ref(y_w))  -  (logπ_θ(y_l) - logπ_ref(y_l)) ] )
</code></pre></div></div> <h3 id="直觉">直觉</h3> <ul> <li>抬高 chosen 相对 ref 的 log-ratio,压低 rejected 的</li> <li><code class="language-plaintext highlighter-rouge">β</code> 控制偏离 ref 的强度(≈ PPO 里的 KL 系数)</li> <li>不需要 RM,不需要 RL,<strong>纯监督训练</strong></li> </ul> <h3 id="代码骨架-7">代码骨架</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">sequence_logprob</span><span class="p">(</span><span class="n">logits</span><span class="p">,</span> <span class="n">tokens</span><span class="p">):</span>
    <span class="n">logp</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">log_softmax</span><span class="p">(</span><span class="n">logits</span><span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>
    <span class="c1"># t 时刻 logits 预测 t+1 的 token
</span>    <span class="n">chosen</span> <span class="o">=</span> <span class="n">logp</span><span class="p">[:,</span> <span class="p">:</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="p">:].</span><span class="nf">gather</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="n">tokens</span><span class="p">[:,</span> <span class="mi">1</span><span class="p">:].</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)).</span><span class="nf">squeeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">chosen</span><span class="p">.</span><span class="nf">sum</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span>

<span class="k">def</span> <span class="nf">dpo_loss</span><span class="p">(</span><span class="n">policy_logits_w</span><span class="p">,</span> <span class="n">policy_logits_l</span><span class="p">,</span>
             <span class="n">ref_logits_w</span><span class="p">,</span> <span class="n">ref_logits_l</span><span class="p">,</span>
             <span class="n">tokens_w</span><span class="p">,</span> <span class="n">tokens_l</span><span class="p">,</span> <span class="n">beta</span><span class="o">=</span><span class="mf">0.2</span><span class="p">):</span>
    <span class="n">pi_w</span> <span class="o">=</span> <span class="nf">sequence_logprob</span><span class="p">(</span><span class="n">policy_logits_w</span><span class="p">,</span> <span class="n">tokens_w</span><span class="p">)</span>
    <span class="n">pi_l</span> <span class="o">=</span> <span class="nf">sequence_logprob</span><span class="p">(</span><span class="n">policy_logits_l</span><span class="p">,</span> <span class="n">tokens_l</span><span class="p">)</span>
    <span class="k">with</span> <span class="n">torch</span><span class="p">.</span><span class="nf">no_grad</span><span class="p">():</span>
        <span class="n">ref_w</span> <span class="o">=</span> <span class="nf">sequence_logprob</span><span class="p">(</span><span class="n">ref_logits_w</span><span class="p">,</span> <span class="n">tokens_w</span><span class="p">)</span>
        <span class="n">ref_l</span> <span class="o">=</span> <span class="nf">sequence_logprob</span><span class="p">(</span><span class="n">ref_logits_l</span><span class="p">,</span> <span class="n">tokens_l</span><span class="p">)</span>
    <span class="n">logits</span> <span class="o">=</span> <span class="n">beta</span> <span class="o">*</span> <span class="p">((</span><span class="n">pi_w</span> <span class="o">-</span> <span class="n">ref_w</span><span class="p">)</span> <span class="o">-</span> <span class="p">(</span><span class="n">pi_l</span> <span class="o">-</span> <span class="n">ref_l</span><span class="p">))</span>
    <span class="k">return</span> <span class="o">-</span><span class="n">F</span><span class="p">.</span><span class="nf">logsigmoid</span><span class="p">(</span><span class="n">logits</span><span class="p">).</span><span class="nf">sum</span><span class="p">()</span>
</code></pre></div></div> <h3 id="dpo-损失从-rlhf-目标的完整推导必背">DPO 损失从 RLHF 目标的完整推导(必背)</h3> <p><strong>Step 1</strong>: RLHF 的优化目标 = “最大化 reward + KL 不偏离 ref”:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>π* = argmax_π  E_{x, y~π}[ r(x,y) ]  − β · KL(π ‖ π_ref)
</code></pre></div></div> <p><strong>Step 2</strong>: 这个约束优化有<strong>闭式最优解</strong>(Lagrangian 配方法):</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>π*(y|x) ∝ π_ref(y|x) · exp(r(x,y) / β)
π*(y|x) = (1/Z(x)) · π_ref(y|x) · exp(r(x,y) / β)
</code></pre></div></div> <p>其中 <code class="language-plaintext highlighter-rouge">Z(x) = Σ_y π_ref(y|x)·exp(r(x,y)/β)</code> 是归一化常数。</p> <p><strong>Step 3</strong>: <strong>反解出 reward</strong>(从最优 policy 反推 reward):</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>r(x,y) = β · log(π*(y|x) / π_ref(y|x)) + β · log Z(x)
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">β·log Z(x)</code> 是 prompt 相关的常数项,在 Bradley-Terry 的<strong>差</strong>里<strong>消掉了</strong>。</p> <p><strong>Step 4</strong>: 代入 Bradley-Terry: <code class="language-plaintext highlighter-rouge">P(y_w ≻ y_l) = σ(r(x,y_w) - r(x,y_l))</code>:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>P(y_w ≻ y_l) = σ( β · log(π*(y_w)/π_ref(y_w)) − β · log(π*(y_l)/π_ref(y_l)) )
            = σ( β · [(logπ*(y_w) − logπ_ref(y_w)) − (logπ*(y_l) − logπ_ref(y_l))] )
</code></pre></div></div> <p><strong>Step 5</strong>: 最大化对数似然 → DPO loss:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>L = -log σ( β · [(logπ_θ(y_w) − logπ_ref(y_w)) − (logπ_θ(y_l) − logπ_ref(y_l))] )
</code></pre></div></div> <p><strong>关键观察</strong>: 这个推导<strong>完全等价于</strong> RM + PPO,但<strong>消去了 RM 这一步</strong>。 代价: PPO 是 on-policy 自己采样,DPO 是 off-policy 用固定偏好数据。</p> <h3 id="dpo-梯度的直觉看公式知道在干啥">DPO 梯度的直觉(看公式知道在干啥)</h3> <p>对 DPO loss 求 <code class="language-plaintext highlighter-rouge">θ</code> 梯度:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>∇_θ L = -β · σ(-β·margin) · ∇_θ [logπ_θ(y_w) - logπ_θ(y_l)]
                ↑                          ↑
           当前模型预测错的概率           "推高 chosen, 压低 rejected"
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">σ(-β·margin)</code> 是个<strong>置信度调节子</strong>: 模型已经把 chosen 拉高很多了,这个 σ 很小,梯度自动减弱。 <strong>自动 hard mining</strong>: 关注那些当前还分不清的 pair。</p> <h3 id="长度偏置-dpo-的著名痛点">长度偏置 (DPO 的著名痛点)</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>logπ(seq) = Σ_t log π(t_t | t_&lt;t)         # 项数 = 序列长度
</code></pre></div></div> <p>如果 chosen 平均比 rejected 长 50 tokens,<code class="language-plaintext highlighter-rouge">logπ(chosen) - logπ(rejected)</code> 会有 <strong>~50 个额外 logp 项贡献</strong>,模型学到”输出长 = 高 reward”的<strong>虚假关联</strong>。</p> <p>实测: 普通 DPO 训完后,输出长度比 SFT 长 2-3 倍,但人类胜率提升有限。</p> <p><strong>修法</strong>:</p> <ul> <li><strong>SimPO</strong>: 用 <code class="language-plaintext highlighter-rouge">logπ / |seq|</code> 归一化,去掉 ref 模型</li> <li><strong>Length-normalized DPO</strong>: 加 length penalty 项</li> <li><strong>R-DPO</strong>: 在 reward 里减 length term</li> </ul> <h3 id="dpo-的几个进化版本">DPO 的几个进化版本</h3> <table> <thead> <tr> <th>算法</th> <th>改了什么</th> <th>解决了什么</th> </tr> </thead> <tbody> <tr> <td><strong>DPO (Rafailov 2023)</strong></td> <td>RM + PPO → 直接分类 loss</td> <td>简化 RLHF</td> </tr> <tr> <td><strong>IPO (Azar 2023)</strong></td> <td>sigmoid → identity</td> <td>防止过拟合 / 偏好概率饱和</td> </tr> <tr> <td><strong>KTO (Ethayarajh 2024)</strong></td> <td>pairwise → 单样本 like/dislike</td> <td>不需要成对数据,只要 thumbs up/down</td> </tr> <tr> <td><strong>SimPO (Meng 2024)</strong></td> <td>去掉 ref 模型 + length normalize</td> <td>省显存 + 解长度偏置</td> </tr> <tr> <td><strong>RPO (Pal 2024)</strong></td> <td>DPO + 显式 SFT loss 项</td> <td>防 logp 整体下降的坍塌</td> </tr> <tr> <td><strong>R-DPO</strong></td> <td>加 length penalty 到 reward</td> <td>解长度偏置</td> </tr> <tr> <td><strong>β-DPO</strong></td> <td>动态调 β</td> <td>不同样本难度自适应</td> </tr> </tbody> </table> <h3 id="dpo-训练中的logp-双降现象-常见-bug">DPO 训练中的”logp 双降”现象 (常见 bug)</h3> <p>实际训完 DPO 后画图,常发现:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>logπ_θ(chosen):    -25   (训前)  →   -40   (训后)    ← 居然降了!
logπ_θ(rejected):  -27   (训前)  →   -55   (训后)    ← 降得更多
margin:              2                  15            ← margin 增大了
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">margin</code> 在涨,所以 DPO loss 在降,<strong>但 chosen 的概率也降了</strong>!原因: DPO 只关心 margin,不关心绝对概率。</p> <p><strong>修法 (RPO)</strong>: 加一个 SFT loss 项强制 <code class="language-plaintext highlighter-rouge">logπ_θ(chosen)</code> 不要下降:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">L_RPO</span> <span class="o">=</span> <span class="n">L_DPO</span> <span class="o">+</span> <span class="n">λ</span> <span class="err">·</span> <span class="p">(</span><span class="o">-</span><span class="n">logπ_θ</span><span class="p">(</span><span class="n">y_w</span><span class="p">))</span>     <span class="c1"># 第二项就是普通 SFT loss
</span></code></pre></div></div> <h3 id="dpo-vs-ppo-实操对比">DPO vs PPO 实操对比</h3> <table> <thead> <tr> <th>维度</th> <th>PPO</th> <th>DPO</th> </tr> </thead> <tbody> <tr> <td>训练阶段</td> <td>SFT → RM → PPO</td> <td>SFT → DPO(一步到位)</td> </tr> <tr> <td>Reward model</td> <td>显式训</td> <td>隐式</td> </tr> <tr> <td>RL 采样</td> <td>on-policy 采</td> <td>用离线偏好数据</td> </tr> <tr> <td>显存</td> <td>4 模型 (policy + ref + reward + value)</td> <td>2 模型 (policy + ref)</td> </tr> <tr> <td>实现复杂度</td> <td>高 (4 模型 + GAE + clip)</td> <td>低 (一个 sigmoid loss)</td> </tr> <tr> <td>训练稳定性</td> <td>难调</td> <td>稳定(分类损失)</td> </tr> <tr> <td>数据分布敏感性</td> <td>低 (自己采样)</td> <td>高 (OOD 偏好学不好)</td> </tr> <tr> <td>工业线落地</td> <td>Anthropic, OpenAI 早期</td> <td>Llama 3, Mistral, Zephyr</td> </tr> <tr> <td>性能上限</td> <td>略高</td> <td>接近但有差距</td> </tr> </tbody> </table> <h3 id="完整-dpo-训练代码">完整 DPO 训练代码</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">dpo_train_step</span><span class="p">(</span><span class="n">policy</span><span class="p">,</span> <span class="n">ref</span><span class="p">,</span> <span class="n">batch</span><span class="p">,</span> <span class="n">beta</span><span class="o">=</span><span class="mf">0.1</span><span class="p">):</span>
    <span class="c1"># 4 次前向: 2 (policy/ref) × 2 (chosen/rejected)
</span>    <span class="n">logp_pol_w</span> <span class="o">=</span> <span class="nf">sequence_logprob</span><span class="p">(</span><span class="nf">policy</span><span class="p">(</span><span class="n">batch</span><span class="p">[</span><span class="sh">'</span><span class="s">chosen</span><span class="sh">'</span><span class="p">]),</span> <span class="n">batch</span><span class="p">[</span><span class="sh">'</span><span class="s">chosen</span><span class="sh">'</span><span class="p">])</span>
    <span class="n">logp_pol_l</span> <span class="o">=</span> <span class="nf">sequence_logprob</span><span class="p">(</span><span class="nf">policy</span><span class="p">(</span><span class="n">batch</span><span class="p">[</span><span class="sh">'</span><span class="s">rejected</span><span class="sh">'</span><span class="p">]),</span> <span class="n">batch</span><span class="p">[</span><span class="sh">'</span><span class="s">rejected</span><span class="sh">'</span><span class="p">])</span>

    <span class="k">with</span> <span class="n">torch</span><span class="p">.</span><span class="nf">no_grad</span><span class="p">():</span>
        <span class="n">logp_ref_w</span> <span class="o">=</span> <span class="nf">sequence_logprob</span><span class="p">(</span><span class="nf">ref</span><span class="p">(</span><span class="n">batch</span><span class="p">[</span><span class="sh">'</span><span class="s">chosen</span><span class="sh">'</span><span class="p">]),</span> <span class="n">batch</span><span class="p">[</span><span class="sh">'</span><span class="s">chosen</span><span class="sh">'</span><span class="p">])</span>
        <span class="n">logp_ref_l</span> <span class="o">=</span> <span class="nf">sequence_logprob</span><span class="p">(</span><span class="nf">ref</span><span class="p">(</span><span class="n">batch</span><span class="p">[</span><span class="sh">'</span><span class="s">rejected</span><span class="sh">'</span><span class="p">]),</span> <span class="n">batch</span><span class="p">[</span><span class="sh">'</span><span class="s">rejected</span><span class="sh">'</span><span class="p">])</span>

    <span class="c1"># margin: chosen 的 log-ratio 减 rejected 的 log-ratio
</span>    <span class="n">chosen_logratio</span>   <span class="o">=</span> <span class="n">logp_pol_w</span> <span class="o">-</span> <span class="n">logp_ref_w</span>
    <span class="n">rejected_logratio</span> <span class="o">=</span> <span class="n">logp_pol_l</span> <span class="o">-</span> <span class="n">logp_ref_l</span>
    <span class="n">margin</span>            <span class="o">=</span> <span class="n">chosen_logratio</span> <span class="o">-</span> <span class="n">rejected_logratio</span>

    <span class="n">loss</span> <span class="o">=</span> <span class="o">-</span><span class="n">F</span><span class="p">.</span><span class="nf">logsigmoid</span><span class="p">(</span><span class="n">beta</span> <span class="o">*</span> <span class="n">margin</span><span class="p">).</span><span class="nf">mean</span><span class="p">()</span>

    <span class="c1"># 监控指标
</span>    <span class="n">chosen_reward</span>   <span class="o">=</span> <span class="n">beta</span> <span class="o">*</span> <span class="n">chosen_logratio</span><span class="p">.</span><span class="nf">detach</span><span class="p">()</span>
    <span class="n">rejected_reward</span> <span class="o">=</span> <span class="n">beta</span> <span class="o">*</span> <span class="n">rejected_logratio</span><span class="p">.</span><span class="nf">detach</span><span class="p">()</span>
    <span class="n">reward_gap</span>      <span class="o">=</span> <span class="p">(</span><span class="n">chosen_reward</span> <span class="o">-</span> <span class="n">rejected_reward</span><span class="p">).</span><span class="nf">mean</span><span class="p">()</span>    <span class="c1"># 应该上涨
</span>    <span class="n">accuracy</span>        <span class="o">=</span> <span class="p">(</span><span class="n">chosen_reward</span> <span class="o">&gt;</span> <span class="n">rejected_reward</span><span class="p">).</span><span class="nf">float</span><span class="p">().</span><span class="nf">mean</span><span class="p">()</span>  <span class="c1"># 训练 accuracy
</span>
    <span class="k">return</span> <span class="n">loss</span><span class="p">,</span> <span class="p">{</span><span class="sh">'</span><span class="s">reward_gap</span><span class="sh">'</span><span class="p">:</span> <span class="n">reward_gap</span><span class="p">,</span> <span class="sh">'</span><span class="s">acc</span><span class="sh">'</span><span class="p">:</span> <span class="n">accuracy</span><span class="p">}</span>
</code></pre></div></div> <h3 id="高频追问-9">高频追问</h3> <table> <thead> <tr> <th>Q</th> <th>A</th> </tr> </thead> <tbody> <tr> <td>DPO 为什么不需要 RM?</td> <td>反解 RLHF 最优解: <code class="language-plaintext highlighter-rouge">r = β·log(π/π_ref) + const</code>,代入 BT,const 抵消,直接得分类 loss</td> </tr> <tr> <td>β 怎么选?</td> <td>0.1-0.5 常见;太大 → 几乎不动 ref;太小 → 偏离 ref 太远易崩</td> </tr> <tr> <td>为什么还要 ref?</td> <td>限制 policy 不要离 SFT 太远;ref 同时给 chosen / rejected 提供归一化基准</td> </tr> <tr> <td>DPO 的缺点?</td> <td>(a) Off-policy → OOD 偏好学不好 (b) 长度偏置 (c) chosen logp 可能整体下降</td> </tr> <tr> <td>怎么改进 DPO?</td> <td>IPO (防过拟合), KTO (单点偏好), SimPO (去 ref + length norm), RPO (加 SFT)</td> </tr> <tr> <td>DPO 训练中 chosen logp 应该上升吗?</td> <td>不一定!DPO 只优化 margin,实测 chosen 和 rejected 经常一起下降 (用 RPO 修)</td> </tr> <tr> <td>DPO 能 multi-epoch 吗?</td> <td>能,但易过拟合;经验 1-3 epoch 最佳。IPO 提出就是为了缓解</td> </tr> <tr> <td>为什么 SimPO 去掉 ref 还 work?</td> <td>用 <code class="language-plaintext highlighter-rouge">logπ/|seq|</code> 自带 length norm + ref 的归一化作用被 length scale 替代</td> </tr> <tr> <td>DPO 数据从哪来?</td> <td>同 prompt 不同 temperature 采样的两个回复 → 人标 chosen/rejected;或 GPT-4 当 judge</td> </tr> <tr> <td>DPO 训练时 ref 要不要也 grad?</td> <td><strong>绝对不要</strong>!ref 必须 <code class="language-plaintext highlighter-rouge">with torch.no_grad()</code>,否则梯度全乱</td> </tr> </tbody> </table> <hr/> <h2 id="11-一图全景-四种对齐算法对比">11. 一图全景: 四种对齐算法对比</h2> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>              数据形式                    需要 Reward Model?           是否需要在线采样?
──────────────────────────────────────────────────────────────────────────────────
SFT       (prompt, response)              ─                            ─
RM        (prompt, chosen, rejected)      训出来 ←                     ─
PPO       (prompt) + RM 打分              是 (独立训)                  是 (on-policy)
GRPO      (prompt) + RM 或 verifier       是 或 否 (可验证 reward)      是 (组内采样)
DPO       (prompt, chosen, rejected)      ─                            ─ (纯监督)
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>                            ┌─ value baseline ─► PPO  (4 模型)
            策略梯度 + RM ──┤
                            └─ 组内归一化 ────► GRPO (3 模型, 无 value)

            Bradley-Terry + 最优策略解析解 ───► DPO  (2 模型, 无 RM 无 RL)
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>显存:  PPO  ≫  GRPO  &gt;  DPO  ≫  SFT
难度:  PPO  ≫  GRPO  ≈  DPO  &gt;  SFT
质量:  PPO  &gt;  GRPO  ≳  DPO    (DPO 在 OOD 数据上掉得快)
</code></pre></div></div> <hr/> <h2 id="附-复习自检清单">附: 复习自检清单</h2> <ul class="task-list"> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/><strong>MinHash</strong>: 能口述”为什么 min h(A) = min h(B) 的概率 = Jaccard”</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/><strong>BPE</strong>: 能在白纸上跑完一个 3 词例子的前 3 轮合并</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/><strong>BPE vs WordPiece vs Unigram</strong>: 三个准则、三种方向、三种 OOV 处理</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/><strong>Byte-level BPE</strong>: GPT-2 的 Ġ 是什么,为什么用字节</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/><strong>Softmax 反向</strong>: 写出 <code class="language-plaintext highlighter-rouge">s ⊙ (g - (g·s))</code>,解释为什么</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/><strong>Attention 反向</strong>: dq/dk/dv 三个公式 + √D 的位置</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/><strong>Memory-Efficient Attention</strong>: 分块 num/den 累加;online softmax 的 running max 怎么维护;FlashAttention 为什么快(HBM,不是 FLOPs)</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/><strong>RM loss</strong>: <code class="language-plaintext highlighter-rouge">-log σ(r_w - r_l)</code>,从 Bradley-Terry 推</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/><strong>GAE</strong>: 写出递推式 + γ/λ 极值含义</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/><strong>PPO</strong>: clipped surrogate + KL,4 个模型</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/><strong>GRPO</strong>: 比 PPO 少了什么(value),advantage 怎么算</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/><strong>DPO</strong>: 写出损失,解释为什么不需要 RM</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/><strong>四者对比</strong>: 数据形式 / RM / on-policy / 显存 4 个维度</li> </ul> <hr/>]]></content><author><name></name></author><category term="notes"/><category term="post-training"/><category term="rlhf"/><category term="tokenizer"/><category term="ppo"/><category term="grpo"/><category term="dpo"/><category term="flashattention"/><summary type="html"><![CDATA[从 MinHash 去重到 BPE/WordPiece/Unigram、Softmax 手推、FlashAttention、RM/PPO/GRPO/DPO，每节给出 Key Insight、实现层次图、代码骨架、推导和高频追问。]]></summary></entry><entry><title type="html">From PPO to Search-R1: The Design Space of Reasoning and Agentic RL</title><link href="https://brucexu09.github.io/blog/2026/from-ppo-to-search-r1/" rel="alternate" type="text/html" title="From PPO to Search-R1: The Design Space of Reasoning and Agentic RL"/><published>2026-04-17T00:00:00+00:00</published><updated>2026-04-17T00:00:00+00:00</updated><id>https://brucexu09.github.io/blog/2026/from-ppo-to-search-r1</id><content type="html" xml:base="https://brucexu09.github.io/blog/2026/from-ppo-to-search-r1/"><![CDATA[<div class="lang-content lang-en"> <h2 id="preface">Preface</h2> <p>The major advances in reasoning LLMs over the past year — o1, DeepSeek-R1, Search-R1 — share a common RL foundation, but each makes different choices at a few critical design points. Laying these choices side by side makes the design space much clearer than reading any single paper on its own.</p> <p>That is what this post does. The main thread is Search-R1<sup id="fnref:sr1"><a href="#fn:sr1" class="footnote" rel="footnote" role="doc-noteref">1</a></sup>: it is a clean instance of Agentic RL, and it is simple enough to naturally tie together PPO, GRPO, verifiers, and token masking.</p> <p>Readers are assumed to be familiar with transformers, SFT, and basic deep learning. Familiarity with RL internals is <strong>not</strong> assumed.</p> <p>Not covered: RLHF preference-data construction, inference-time scaling (best-of-N, tree search), multi-agent, low-level hardware optimizations.</p> <hr/> <h2 id="problem-setup-evolution-of-the-post-training-stack">1. Problem Setup: Evolution of the Post-training Stack</h2> <p>Moving from a pretrained LLM to an agent that can autonomously call tools goes through several technical jumps. Each jump solves a limitation of the previous one:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Pretrain
   │ knows a lot, but doesn't follow instructions
   ↓
SFT (Supervised Fine-Tuning)
   │ imitates only; can't actively suppress wrong behavior
   ↓
RLHF (PPO + Reward Model)
   │ RM gets hacked; preference data is expensive; weak reasoning signal
   ↓
Reasoning RL (GRPO + Verifier)
   │ reward only comes from the model's own outputs; no external interaction
   ↓
Agentic RL (+ Tool / Environment)
</code></pre></div> </div> <p>The technical core of each jump:</p> <ul> <li><strong>SFT</strong>: cross entropy + supervised learning</li> <li><strong>RLHF</strong>: PPO + neural reward model</li> <li><strong>Reasoning RL</strong>: GRPO + rule-based verifier; drops the reward model</li> <li><strong>Agentic RL</strong>: on top of Reasoning RL, embeds environment interaction into the rollout</li> </ul> <p>“Agentic RL” is not a standalone new algorithm but a training paradigm that “introduces environment interaction into the rollout.” The algorithmic core is still PPO or GRPO. The key change is <strong>extension of the environment</strong>: tool calls are inserted mid-rollout, and execution / retrieval results are injected back into the sequence, becoming context for the next generation.</p> <p>Search-R1 is the cleanest instance of this paradigm: it uses a search engine as the environment and lets the LLM autonomously decide when and what to search during reasoning.</p> <p>Off this main thread there is a side branch: <strong>DPO</strong> turns RLHF directly into supervised learning — no RL rollout, no reward model. §7 discusses it separately.</p> <p>The following sections unfold each component in turn.</p> <hr/> <h2 id="from-cross-entropy-to-policy-gradient">2. From Cross Entropy to Policy Gradient</h2> <p>This section establishes the equivalence between RL loss and SFT loss, as groundwork for understanding PPO later.</p> <h3 id="simplifying-cross-entropy-under-next-token-prediction">2.1 Simplifying Cross Entropy under Next-Token Prediction</h3> <p>General definition:</p> \[H(p, q) = -\sum_i p_i \log q_i\] <p>In the next-token setting, the ground-truth label \(p\) is one-hot with \(p_{y_t} = 1\) and zero elsewhere. Summing over the vocabulary leaves a single term:</p> \[H(p, q) = -\log \pi_\theta(y_t \mid y_{&lt;t})\] <p>SFT sums over the whole sequence:</p> \[\mathcal{L}^{SFT} = -\sum_{t=1}^{T} \log \pi_\theta(y_t \mid y_{&lt;t})\] <h3 id="the-subtle-distinction-between-ce-and-kl">2.2 The Subtle Distinction Between CE and KL</h3> <p>The identity:</p> \[H(p, q) = H(p) + D_{KL}(p \| q)\] <p>In supervised learning \(p\) is the fixed ground truth, so \(H(p)\) is a constant with respect to \(\theta\) — meaning <strong>optimizing CE and optimizing KL have identical gradients</strong>. We default to CE only because it is cheaper.</p> <p>In RL this equivalence breaks down: both sides of the KL are models (\(\pi_\theta\) vs \(\pi_{ref}\)), neither is fixed. The distinction between CE and KL becomes genuinely important here. We will use this below when discussing PPO’s KL penalty.</p> <h3 id="from-sft-to-reinforce">2.3 From SFT to REINFORCE</h3> <p>The SFT loss unconditionally pushes up the probability of every token — every token in the demo data is assumed to be good. That assumption is fine for imitation learning, but RL wants more: <strong>push up good behaviors, and actively suppress bad ones</strong>.</p> <p>REINFORCE multiplies the SFT loss by a weight \(A_t\):</p> \[\mathcal{L}^{REINFORCE} = -\mathbb{E}\Big[\sum_t \log \pi_\theta(y_t \mid y_{&lt;t}) \cdot A_t\Big]\] <ul> <li>\(A_t &gt; 0\): good token, push probability up</li> <li>\(A_t &lt; 0\): bad token, push probability down</li> <li>The larger \(\lvert A_t \rvert\) is, the larger the adjustment</li> </ul> <p>The three losses in unified form:</p> <table> <thead> <tr> <th>Loss</th> <th>Per-token form</th> <th>Meaning of the weight</th> </tr> </thead> <tbody> <tr> <td>SFT</td> <td>\(-\log \pi_\theta(y_t)\)</td> <td>1 (push up every token)</td> </tr> <tr> <td>REINFORCE</td> <td>\(-\log \pi_\theta(y_t) \cdot A_t\)</td> <td>advantage-weighted</td> </tr> <tr> <td>PPO</td> <td>\(-\min(r_t A_t, \text{clip}(r_t) A_t)\)</td> <td>ratio replaces log (next section)</td> </tr> </tbody> </table> <p><strong>Key insight</strong>: the RL loss is structurally “signed, weighted cross entropy.” With \(A_t \equiv 1\), SFT and RL coincide in <strong>gradient form</strong>. Note this is only a formal analogy — SFT samples from demo data while RL samples from \(\pi_\theta\); the sample distributions are different.</p> <h3 id="code-comparison">2.4 Code Comparison</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># SFT loss
</span><span class="k">def</span> <span class="nf">sft_loss</span><span class="p">(</span><span class="n">logits</span><span class="p">,</span> <span class="n">actions</span><span class="p">,</span> <span class="n">mask</span><span class="p">):</span>
    <span class="n">log_probs</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">log_softmax</span><span class="p">(</span><span class="n">logits</span><span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>
    <span class="n">token_logprobs</span> <span class="o">=</span> <span class="n">log_probs</span><span class="p">.</span><span class="nf">gather</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="n">actions</span><span class="p">.</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)).</span><span class="nf">squeeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span>
    <span class="k">return</span> <span class="o">-</span><span class="p">(</span><span class="n">token_logprobs</span> <span class="o">*</span> <span class="n">mask</span><span class="p">).</span><span class="nf">sum</span><span class="p">()</span> <span class="o">/</span> <span class="n">mask</span><span class="p">.</span><span class="nf">sum</span><span class="p">()</span>

<span class="c1"># REINFORCE loss: only differs by * advantages
</span><span class="k">def</span> <span class="nf">reinforce_loss</span><span class="p">(</span><span class="n">logits</span><span class="p">,</span> <span class="n">actions</span><span class="p">,</span> <span class="n">advantages</span><span class="p">,</span> <span class="n">mask</span><span class="p">):</span>
    <span class="n">log_probs</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">log_softmax</span><span class="p">(</span><span class="n">logits</span><span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>
    <span class="n">token_logprobs</span> <span class="o">=</span> <span class="n">log_probs</span><span class="p">.</span><span class="nf">gather</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="n">actions</span><span class="p">.</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)).</span><span class="nf">squeeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span>
    <span class="k">return</span> <span class="o">-</span><span class="p">(</span><span class="n">token_logprobs</span> <span class="o">*</span> <span class="n">advantages</span> <span class="o">*</span> <span class="n">mask</span><span class="p">).</span><span class="nf">sum</span><span class="p">()</span> <span class="o">/</span> <span class="n">mask</span><span class="p">.</span><span class="nf">sum</span><span class="p">()</span>
</code></pre></div> </div> <p>The only difference is one <code class="language-plaintext highlighter-rouge">* advantages</code>. That one multiplication opens the gap between SFT and RL: <strong>RL can suppress bad behavior with a negative weight; SFT cannot.</strong></p> <hr/> <h2 id="ppo-design-motivation-and-the-role-of-each-component">3. PPO: Design Motivation and the Role of Each Component</h2> <p>PPO<sup id="fnref:ppo"><a href="#fn:ppo" class="footnote" rel="footnote" role="doc-noteref">2</a></sup> is the de-facto standard for LLM RL training today. This section breaks it down component by component, ordered as “component → what problem does it solve.”</p> <p>Below is the LLM-RLHF version of PPO — it adds a \(\pi_{ref}\) KL penalty that the original PPO paper did not have. This addition is an engineering convention inherited from InstructGPT<sup id="fnref:instructgpt"><a href="#fn:instructgpt" class="footnote" rel="footnote" role="doc-noteref">3</a></sup>, not part of the original PPO:</p> \[\mathcal{L}^{PPO}(\theta) = -\mathbb{E}_t\Big[\min\big(r_t(\theta) A_t,\ \text{clip}(r_t(\theta), 1-\epsilon, 1+\epsilon) A_t\big)\Big] + \beta \cdot D_{KL}\big[\pi_\theta \| \pi_{ref}\big]\] <h3 id="starting-point-two-pathologies-of-reinforce">3.1 Starting Point: Two Pathologies of REINFORCE</h3> <p><strong>Pathology 1: high variance; a single update may blow up.</strong> \(A_t\) is estimated from a single trajectory, which is high variance. Occasional high-reward samples can blow up \(A_t\) for certain tokens, and one gradient step can push \(\pi_\theta\) into a weird region.</p> <p><strong>Pathology 2: sample inefficiency.</strong> REINFORCE is strictly on-policy: once policy updates, the previous rollouts are useless. Unacceptable for LLMs where rollouts are extremely expensive.</p> <p>Each component of PPO cures these two pathologies.</p> <h3 id="importance-ratio-reusing-the-same-batch-of-rollouts">3.2 Importance Ratio: Reusing the Same Batch of Rollouts</h3> \[r_t(\theta) = \frac{\pi_\theta(y_t \mid y_{&lt;t})}{\pi_{old}(y_t \mid y_{&lt;t})}\] <p>\(\pi_{old}\) is a snapshot of the policy at the time this batch of rollouts was sampled. The training loop becomes:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span> <span class="n">iteration</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">N</span><span class="p">):</span>
    <span class="c1"># snapshot current policy
</span>    <span class="n">pi_old</span> <span class="o">=</span> <span class="nf">copy</span><span class="p">(</span><span class="n">pi_theta</span><span class="p">).</span><span class="nf">detach</span><span class="p">()</span>

    <span class="c1"># collect rollouts using pi_old
</span>    <span class="n">rollouts</span> <span class="o">=</span> <span class="nf">sample_rollouts</span><span class="p">(</span><span class="n">pi_old</span><span class="p">,</span> <span class="n">batch_size</span><span class="o">=</span><span class="n">B</span><span class="p">)</span>
    <span class="n">advantages</span> <span class="o">=</span> <span class="nf">compute_gae</span><span class="p">(</span><span class="n">rollouts</span><span class="p">)</span>

    <span class="c1"># multiple gradient updates on the same rollouts
</span>    <span class="k">for</span> <span class="n">epoch</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">K</span><span class="p">):</span>  <span class="c1"># typically K = 2~4
</span>        <span class="k">for</span> <span class="n">minibatch</span> <span class="ow">in</span> <span class="nf">split</span><span class="p">(</span><span class="n">rollouts</span><span class="p">):</span>
            <span class="n">loss</span> <span class="o">=</span> <span class="nf">ppo_loss</span><span class="p">(</span><span class="n">pi_theta</span><span class="p">,</span> <span class="n">pi_old</span><span class="p">,</span> <span class="n">minibatch</span><span class="p">,</span> <span class="n">advantages</span><span class="p">)</span>
            <span class="n">loss</span><span class="p">.</span><span class="nf">backward</span><span class="p">()</span>
            <span class="n">optimizer</span><span class="p">.</span><span class="nf">step</span><span class="p">()</span>
</code></pre></div> </div> <p>The same batch is reused \(K\) times. By the \(k\)-th epoch \(\pi_\theta \neq \pi_{old}\) — that is off-policy. The importance ratio mathematically corrects this mismatch (standard application of importance sampling).</p> <p><strong>Notational pitfall</strong>: \(r_t\) here is the importance ratio, not the reward. Reward becomes \(A_t\) via GAE in PPO; the reward itself does not appear in the loss.</p> <h3 id="clip-single-step-magnitude-constraint">3.3 Clip: Single-step Magnitude Constraint</h3> <p>The ratio alone is not enough. If \(\pi_\theta\) drifts far from \(\pi_{old}\) (e.g. \(r_t = 5\)), importance sampling variance explodes. Clip hard-caps it:</p> \[\text{clip}(r_t, 1-\epsilon, 1+\epsilon)\] <p>Typically \(\epsilon = 0.2\), meaning the single-token probability ratio can change by at most ±20%.</p> <p><strong>But clip alone has a trap.</strong> Say \(A_t = +1\) (a good token) and \(r_t = 0.5\) (the policy has been pushing it down — an error state). If we directly used \(\text{clip}(r_t) \cdot A_t = 0.8\), the gradient gets <strong>artificially inflated</strong>, encouraging the policy to keep going the wrong way.</p> <h3 id="min-let-clip-only-brake-not-block-corrections">3.4 Min: Let Clip Only Brake, Not Block Corrections</h3> <p>The full policy term:</p> \[L_t^{CLIP} = \min\big(r_t A_t,\ \text{clip}(r_t, 1-\epsilon, 1+\epsilon) \cdot A_t\big)\] <p>With \(\epsilon = 0.2\), four cases:</p> <table> <thead> <tr> <th>Case</th> <th>\(A_t\)</th> <th>\(r_t\)</th> <th>\(r_t A_t\)</th> <th>\(\text{clip}(r_t) A_t\)</th> <th>min picks</th> <th>Meaning</th> </tr> </thead> <tbody> <tr> <td>B</td> <td>\(+1\)</td> <td>\(1.5\) (above upper bound)</td> <td>\(1.5\)</td> <td>\(1.2\)</td> <td>clip term</td> <td>good token pushed too far, <strong>saturated</strong></td> </tr> <tr> <td>C</td> <td>\(+1\)</td> <td>\(0.5\) (below lower bound)</td> <td>\(0.5\)</td> <td>\(0.8\)</td> <td><strong>raw term</strong></td> <td>good token being pushed down, <strong>correction allowed</strong></td> </tr> <tr> <td>D</td> <td>\(-1\)</td> <td>\(0.5\) (below lower bound)</td> <td>\(-0.5\)</td> <td>\(-0.8\)</td> <td>clip term</td> <td>bad token already suppressed enough, <strong>saturated</strong></td> </tr> <tr> <td>E</td> <td>\(-1\)</td> <td>\(1.5\) (above upper bound)</td> <td>\(-1.5\)</td> <td>\(-1.2\)</td> <td><strong>raw term</strong></td> <td>bad token being pushed up, <strong>correction allowed</strong></td> </tr> </tbody> </table> <p>The pattern:</p> <ul> <li>Going too far in the <strong>right</strong> direction (B, D) → min picks the clip term, gradient saturates</li> <li>Going in the <strong>wrong</strong> direction (C, E) → min picks the raw term, gradient preserved, correction continues</li> </ul> <p>Schulman calls this the <strong>pessimistic bound</strong>: take the smaller value as a conservative estimate. Two birds one stone: restrict single-step magnitude (B, D) without blocking error correction (C, E).</p> <h3 id="kl-penalty-long-term-anchor-against-drift">3.5 KL Penalty: Long-term Anchor Against Drift</h3> <p>Clip handles <strong>single-step</strong> stability. But even if every step stays within \([1-\epsilon, 1+\epsilon]\), accumulation over many iterations may still drift \(\pi_\theta\) to a completely different distribution. The classic failure mode is <strong>reward hacking</strong>: the model discovers a generation pattern that reliably gets high reward while language quality collapses; clip never triggers at any single step, but the cumulative drift is catastrophic.</p> <p>PPO adds the KL term:</p> \[\beta \cdot D_{KL}\big[\pi_\theta(\cdot \mid s_t) \| \pi_{ref}(\cdot \mid s_t)\big]\] <p>\(\pi_{ref}\) is the policy at the <strong>start of training</strong> (usually the post-SFT model), frozen throughout RL. It is not the same as \(\pi_{old}\):</p> <table> <thead> <tr> <th>Dimension</th> <th>Clip on \(r_t = \pi_\theta / \pi_{old}\)</th> <th>KL on \(\pi_\theta | \pi_{ref}\)</th> </tr> </thead> <tbody> <tr> <td>Comparison target</td> <td>policy at last rollout</td> <td>policy at training start</td> </tr> <tr> <td>Time scale</td> <td>single iteration</td> <td>entire training run</td> </tr> <tr> <td>Form</td> <td>hard constraint (saturating clip)</td> <td>soft constraint (continuous penalty)</td> </tr> <tr> <td>Problem solved</td> <td>over-aggressive single step</td> <td>cumulative drift</td> </tr> </tbody> </table> <p>The two are <strong>complementary, not redundant</strong>. Clip only, no KL → long-run reward hacking. KL only, no clip → a single big update can crash training.</p> <p><strong>The k3 estimator.</strong> Token-level KL by definition iterates over the whole vocabulary, which is expensive. In practice people use Schulman’s k3<sup id="fnref:k3"><a href="#fn:k3" class="footnote" rel="footnote" role="doc-noteref">4</a></sup>:</p> \[\text{KL}_{k3} = \exp(\log\pi_{ref} - \log\pi_\theta) - (\log\pi_{ref} - \log\pi_\theta) - 1\] <p>Strictly speaking, k3 is a low-variance unbiased estimator of \(D_{KL}[p \| q]\) <strong>when samples come from \(p\)</strong>. In PPO we actually sample from \(\pi_{old}\) (because the rollout uses \(\pi_{old}\)), so this is technically an approximation. But \(\pi_{old} \approx \pi_\theta\) under the clip constraint, and in practice the approximation is good enough.</p> <p>One-liner in code:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">log_ratio</span> <span class="o">=</span> <span class="n">ref_logprobs</span> <span class="o">-</span> <span class="n">new_logprobs</span>
<span class="n">kl</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">log_ratio</span><span class="p">)</span> <span class="o">-</span> <span class="n">log_ratio</span> <span class="o">-</span> <span class="mi">1</span>  <span class="c1"># k3 estimator
</span></code></pre></div> </div> <h3 id="full-code">3.6 Full Code</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">compute_ppo_loss</span><span class="p">(</span>
    <span class="n">logits</span><span class="p">,</span>          <span class="c1"># [B, T, V] current policy outputs
</span>    <span class="n">old_logprobs</span><span class="p">,</span>    <span class="c1"># [B, T] log prob from pi_old (detached)
</span>    <span class="n">ref_logprobs</span><span class="p">,</span>    <span class="c1"># [B, T] log prob from pi_ref (frozen)
</span>    <span class="n">actions</span><span class="p">,</span>         <span class="c1"># [B, T] sampled tokens
</span>    <span class="n">advantages</span><span class="p">,</span>      <span class="c1"># [B, T] from GAE (Section 4)
</span>    <span class="n">action_mask</span><span class="p">,</span>     <span class="c1"># [B, T] 1 for policy-generated positions
</span>    <span class="n">clip_eps</span><span class="o">=</span><span class="mf">0.2</span><span class="p">,</span>
    <span class="n">kl_coef</span><span class="o">=</span><span class="mf">0.001</span><span class="p">,</span>
<span class="p">):</span>
    <span class="n">log_probs</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">log_softmax</span><span class="p">(</span><span class="n">logits</span><span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>
    <span class="n">new_logprobs</span> <span class="o">=</span> <span class="n">log_probs</span><span class="p">.</span><span class="nf">gather</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="n">actions</span><span class="p">.</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)).</span><span class="nf">squeeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span>

    <span class="c1"># === importance ratio (3.2) ===
</span>    <span class="n">ratio</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">new_logprobs</span> <span class="o">-</span> <span class="n">old_logprobs</span><span class="p">)</span>

    <span class="c1"># === clip + min (3.3, 3.4) ===
</span>    <span class="n">surr1</span> <span class="o">=</span> <span class="n">ratio</span> <span class="o">*</span> <span class="n">advantages</span>
    <span class="n">surr2</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">clamp</span><span class="p">(</span><span class="n">ratio</span><span class="p">,</span> <span class="mi">1</span> <span class="o">-</span> <span class="n">clip_eps</span><span class="p">,</span> <span class="mi">1</span> <span class="o">+</span> <span class="n">clip_eps</span><span class="p">)</span> <span class="o">*</span> <span class="n">advantages</span>
    <span class="n">policy_loss</span> <span class="o">=</span> <span class="o">-</span><span class="n">torch</span><span class="p">.</span><span class="nf">min</span><span class="p">(</span><span class="n">surr1</span><span class="p">,</span> <span class="n">surr2</span><span class="p">)</span>

    <span class="c1"># === KL penalty (3.5, k3 estimator) ===
</span>    <span class="n">log_ratio_ref</span> <span class="o">=</span> <span class="n">ref_logprobs</span> <span class="o">-</span> <span class="n">new_logprobs</span>
    <span class="n">kl</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">log_ratio_ref</span><span class="p">)</span> <span class="o">-</span> <span class="n">log_ratio_ref</span> <span class="o">-</span> <span class="mi">1</span>

    <span class="n">total</span> <span class="o">=</span> <span class="p">(</span><span class="n">policy_loss</span> <span class="o">+</span> <span class="n">kl_coef</span> <span class="o">*</span> <span class="n">kl</span><span class="p">)</span> <span class="o">*</span> <span class="n">action_mask</span>
    <span class="k">return</span> <span class="n">total</span><span class="p">.</span><span class="nf">sum</span><span class="p">()</span> <span class="o">/</span> <span class="n">action_mask</span><span class="p">.</span><span class="nf">sum</span><span class="p">()</span>
</code></pre></div> </div> <h3 id="five-regularization-mechanisms-at-a-glance">3.7 Five Regularization Mechanisms at a Glance</h3> <table> <thead> <tr> <th>Mechanism</th> <th>Target</th> <th>Problem it solves</th> </tr> </thead> <tbody> <tr> <td>Importance ratio</td> <td>data / policy mismatch</td> <td>allows rollouts to be reused</td> </tr> <tr> <td>Clip on ratio</td> <td>single-token single-step update</td> <td>single step drifting too far from \(\pi_{old}\)</td> </tr> <tr> <td>Min on clip</td> <td>side effects of clip</td> <td>preserves correction gradient in the wrong direction</td> </tr> <tr> <td>KL on \(\pi_{ref}\)</td> <td>cumulative drift</td> <td>reward hacking</td> </tr> <tr> <td>Value baseline</td> <td>advantage estimation</td> <td>variance reduction, credit assignment</td> </tr> </tbody> </table> <p>PPO’s engineering stability is the sum of all five. Remove any one and some setting will start breaking.</p> <hr/> <h2 id="sparse-reward-and-credit-assignment">4. Sparse Reward and Credit Assignment</h2> <h3 id="problem-definition">4.1 Problem Definition</h3> <p>In LLM RL, reward typically only has a nonzero value at the end of the episode. A typical Search-R1 episode:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;think&gt;...&lt;/think&gt;&lt;search&gt;...&lt;/search&gt;&lt;information&gt;...&lt;/information&gt;
&lt;think&gt;...&lt;/think&gt;&lt;search&gt;...&lt;/search&gt;&lt;information&gt;...&lt;/information&gt;
&lt;think&gt;...&lt;/think&gt;&lt;answer&gt;McComb, Mississippi&lt;/answer&gt;   ← reward=1 only here
</code></pre></div> </div> <p>500 tokens, only the last one has a nonzero reward. Using the terminal reward as \(A_t\) for every token causes two problems: all tokens get equal weight (can’t distinguish the critical ones), and gradient variance is high (each episode contributes only one scalar signal).</p> <p>This is the <strong>credit assignment</strong> problem.</p> <h3 id="value-function">4.2 Value Function</h3> <p>The standard fix introduces a value function \(V_\phi\):</p> \[V_\phi(s_t) = \mathbb{E}\Big[\sum_{k=0}^{T-t} \gamma^k r_{t+k}\Big]\] <p>The advantage is defined as:</p> \[A_t = R_t - V_\phi(s_t)\] <ul> <li>\(A_t &gt; 0\): actual return exceeds expectation → good choice</li> <li>\(A_t &lt; 0\): below expectation → bad choice</li> <li>\(A_t \approx 0\): not critical</li> </ul> <p>The value function <strong>spreads</strong> the sparse terminal reward across every token position.</p> <h3 id="monte-carlo-vs-td">4.3 Monte Carlo vs TD</h3> <p>Two ways to estimate \(R_t\):</p> <table> <thead> <tr> <th>Method</th> <th>Formula</th> <th>Property</th> </tr> </thead> <tbody> <tr> <td>Monte Carlo</td> <td>\(R_t^{MC} = \sum_{k=0}^{T-t} \gamma^k r_{t+k}\)</td> <td>unbiased, high variance</td> </tr> <tr> <td>TD</td> <td>\(R_t^{TD} = r_t + \gamma V_\phi(s_{t+1})\)</td> <td>biased, low variance</td> </tr> </tbody> </table> <h3 id="gae-exponential-interpolation-of-the-two">4.4 GAE: Exponential Interpolation of the Two</h3> <p>GAE<sup id="fnref:gae"><a href="#fn:gae" class="footnote" rel="footnote" role="doc-noteref">5</a></sup> uses \(\lambda \in [0, 1]\) to interpolate:</p> \[A_t^{GAE} = \sum_{l=0}^{T-t} (\gamma \lambda)^l \delta_{t+l}, \quad \delta_t = r_t + \gamma V_\phi(s_{t+1}) - V_\phi(s_t)\] <p>Recursive form:</p> \[A_t^{GAE} = \delta_t + \gamma \lambda A_{t+1}^{GAE}\] <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">compute_gae</span><span class="p">(</span><span class="n">rewards</span><span class="p">,</span> <span class="n">values</span><span class="p">,</span> <span class="n">gamma</span><span class="o">=</span><span class="mf">1.0</span><span class="p">,</span> <span class="n">lam</span><span class="o">=</span><span class="mf">1.0</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    rewards: [T] per-step rewards (mostly zero in LLM setting)
    values:  [T+1] value estimates; values[T] is terminal bootstrap
    </span><span class="sh">"""</span>
    <span class="n">T</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">rewards</span><span class="p">)</span>
    <span class="n">advantages</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="n">T</span><span class="p">)</span>
    <span class="n">gae</span> <span class="o">=</span> <span class="mf">0.0</span>

    <span class="c1"># iterate in reverse: GAE is an exponentially weighted sum of future deltas
</span>    <span class="k">for</span> <span class="n">t</span> <span class="ow">in</span> <span class="nf">reversed</span><span class="p">(</span><span class="nf">range</span><span class="p">(</span><span class="n">T</span><span class="p">)):</span>
        <span class="n">delta</span> <span class="o">=</span> <span class="n">rewards</span><span class="p">[</span><span class="n">t</span><span class="p">]</span> <span class="o">+</span> <span class="n">gamma</span> <span class="o">*</span> <span class="n">values</span><span class="p">[</span><span class="n">t</span> <span class="o">+</span> <span class="mi">1</span><span class="p">]</span> <span class="o">-</span> <span class="n">values</span><span class="p">[</span><span class="n">t</span><span class="p">]</span>
        <span class="n">gae</span> <span class="o">=</span> <span class="n">delta</span> <span class="o">+</span> <span class="n">gamma</span> <span class="o">*</span> <span class="n">lam</span> <span class="o">*</span> <span class="n">gae</span>
        <span class="n">advantages</span><span class="p">[</span><span class="n">t</span><span class="p">]</span> <span class="o">=</span> <span class="n">gae</span>

    <span class="n">returns</span> <span class="o">=</span> <span class="n">advantages</span> <span class="o">+</span> <span class="n">values</span><span class="p">[:</span><span class="n">T</span><span class="p">]</span>
    <span class="k">return</span> <span class="n">advantages</span><span class="p">,</span> <span class="n">returns</span>
</code></pre></div> </div> <p>Search-R1 uses \(\lambda = \gamma = 1\). Under this setting the GAE recursion degenerates to \(A_t = \sum_l \delta_{t+l}\), which telescopes to \(R_t - V(s_t)\) — exactly the MC advantage.</p> <h3 id="why-sparse-reward-works-on-llms">4.5 Why Sparse Reward Works on LLMs</h3> <ol> <li><strong>Pretraining provides a strong prior</strong>: RL doesn’t start from scratch — it only does fine-grained adjustment</li> <li><strong>Causal token structure</strong>: early good choices correlate highly with final success</li> <li><strong>Smoothing from the value function</strong>: spreads terminal reward to each position</li> <li><strong>Batch-size statistical power</strong>: Search-R1 uses batch = 512, gradient is relatively stable</li> </ol> <hr/> <h2 id="reward-source-reward-model-vs-verifier">5. Reward Source: Reward Model vs Verifier</h2> <h3 id="two-orthogonal-dimensions">5.1 Two Orthogonal Dimensions</h3> <p>The design space of LLM RL has two orthogonal axes:</p> <p><strong>Axis 1: Where does reward come from</strong> (optimize what)</p> <ul> <li>Neural Reward Model (RM)</li> <li>Rule-based Verifier</li> <li>Generative Verifier</li> </ul> <p><strong>Axis 2: How to do credit assignment</strong> (propagate how)</p> <ul> <li>Critic + GAE (PPO)</li> <li>Group Normalization (GRPO)</li> <li>Offline data (DPO<sup id="fnref:dpo"><a href="#fn:dpo" class="footnote" rel="footnote" role="doc-noteref">6</a></sup>)</li> </ul> <table> <thead> <tr> <th>Reward source</th> <th>Credit Assignment</th> <th>Representative work</th> </tr> </thead> <tbody> <tr> <td>Neural RM</td> <td>Critic + GAE</td> <td>Classic RLHF (InstructGPT)</td> </tr> <tr> <td>Rule Verifier</td> <td>Critic + GAE</td> <td>Search-R1</td> </tr> <tr> <td>Rule Verifier</td> <td>Group Norm</td> <td>DeepSeek-R1<sup id="fnref:r1"><a href="#fn:r1" class="footnote" rel="footnote" role="doc-noteref">7</a></sup></td> </tr> </tbody> </table> <h3 id="reward-model-vs-critic-two-concepts-frequently-confused">5.2 Reward Model vs Critic: Two Concepts Frequently Confused</h3> <table> <thead> <tr> <th>Dimension</th> <th>Reward Model</th> <th>Critic (Value Model)</th> </tr> </thead> <tbody> <tr> <td>What it evaluates</td> <td>the <strong>absolute quality</strong> of the full response</td> <td>the <strong>expected future return</strong> of the current state</td> </tr> <tr> <td>Input</td> <td>(prompt, response)</td> <td>current state \(s_t\)</td> </tr> <tr> <td>Output</td> <td>a single scalar \(r\)</td> <td>one \(V_\phi(s_t)\) per position</td> </tr> <tr> <td>Training signal</td> <td>human preference data (or rules)</td> <td>bootstrap from reward</td> </tr> <tr> <td>When trained</td> <td><strong>before</strong> RL, frozen during RL</td> <td>trained <strong>together</strong> with the policy</td> </tr> </tbody> </table> <p>Common misconception: “with an RM you don’t need a Critic.” In fact the two have completely different jobs: <strong>the RM compresses (prompt, response) into a sparse scalar; the Critic spreads that scalar across each token.</strong> One tells you “good or bad overall,” the other tells you “good where, bad where.”</p> <h3 id="why-reasoning-loves-rule-based-verifiers">5.3 Why Reasoning Loves Rule-based Verifiers</h3> <p>Four reasons:</p> <p><strong>1. Hack-resistant.</strong> The classic failure of a neural RM: policy learns to “fool” the RM. A rule-based verifier is deterministic — it cannot be hacked.</p> <p><strong>2. Scalable.</strong> Training an RM needs a lot of annotation. Writing a verifier function takes a few lines:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">math_verifier</span><span class="p">(</span><span class="n">pred</span><span class="p">,</span> <span class="n">gt</span><span class="p">):</span>
    <span class="k">return</span> <span class="n">sympy</span><span class="p">.</span><span class="nf">simplify</span><span class="p">(</span><span class="nf">parse</span><span class="p">(</span><span class="n">pred</span><span class="p">)</span> <span class="o">-</span> <span class="nf">parse</span><span class="p">(</span><span class="n">gt</span><span class="p">))</span> <span class="o">==</span> <span class="mi">0</span>

<span class="k">def</span> <span class="nf">code_verifier</span><span class="p">(</span><span class="n">pred</span><span class="p">,</span> <span class="n">tests</span><span class="p">):</span>
    <span class="k">return</span> <span class="nf">all</span><span class="p">(</span><span class="nf">run_test</span><span class="p">(</span><span class="n">pred</span><span class="p">,</span> <span class="n">t</span><span class="p">)</span> <span class="k">for</span> <span class="n">t</span> <span class="ow">in</span> <span class="n">tests</span><span class="p">)</span>
</code></pre></div> </div> <p><strong>3. Signal is more direct.</strong> An RM is a proxy indicator; the verifier is the true target. The more direct the training objective, the better RL converges.</p> <p><strong>4. Matches the new outcome-only reward paradigm.</strong> DeepSeek-R1 showed that outcome reward alone can elicit complex reasoning. Rule-based verifiers naturally fit this paradigm.</p> <h3 id="limitations-of-verifiers">5.4 Limitations of Verifiers</h3> <p>Not every task admits a good verifier:</p> <table> <thead> <tr> <th>Task</th> <th>Easy to write a verifier?</th> </tr> </thead> <tbody> <tr> <td>Math</td> <td>Yes, easy</td> </tr> <tr> <td>Code</td> <td>Yes, easy</td> </tr> <tr> <td>Short-answer QA</td> <td>Yes, moderate</td> </tr> <tr> <td>Long-form writing</td> <td>No, hard</td> </tr> <tr> <td>Dialogue quality</td> <td>No, hard</td> </tr> </tbody> </table> <p>This explains why the breakthroughs of reasoning LLMs are concentrated in math / code / QA — they have natural verifiers.</p> <hr/> <h2 id="grpo-the-trade-off-of-dropping-the-critic">6. GRPO: The Trade-off of Dropping the Critic</h2> <p>GRPO<sup id="fnref:grpo"><a href="#fn:grpo" class="footnote" rel="footnote" role="doc-noteref">8</a></sup> is proposed by DeepSeek and is the RL algorithm used by DeepSeek-R1. <strong>Core innovation: drop the Critic.</strong></p> <h3 id="motivation">6.1 Motivation</h3> <p>In LLM settings, the Critic is not cheap: it is typically the same scale as the policy and needs its own forward / backward, and its value estimates are inaccurate at cold-start. When reward is already outcome-level (one scalar per response), the token-level granularity a Critic provides may not be worth the compute.</p> <h3 id="group-normalization">6.2 Group Normalization</h3> <p>Sample \(G\) responses for the same prompt, and compute advantage via <strong>intra-group normalization</strong>:</p> \[\hat{A}_i = \frac{r_i - \text{mean}(r_1, ..., r_G)}{\text{std}(r_1, ..., r_G)}\] <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">compute_grpo_advantages</span><span class="p">(</span><span class="n">rewards</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    rewards: [G] rewards for G responses to the same prompt
    </span><span class="sh">"""</span>
    <span class="n">mean</span> <span class="o">=</span> <span class="n">rewards</span><span class="p">.</span><span class="nf">mean</span><span class="p">()</span>
    <span class="n">std</span> <span class="o">=</span> <span class="n">rewards</span><span class="p">.</span><span class="nf">std</span><span class="p">()</span> <span class="o">+</span> <span class="mf">1e-8</span>
    <span class="nf">return </span><span class="p">(</span><span class="n">rewards</span> <span class="o">-</span> <span class="n">mean</span><span class="p">)</span> <span class="o">/</span> <span class="n">std</span>
</code></pre></div> </div> <p>Intuition: among \(G\) responses to the same prompt, a response is “good” if it scores above the group mean. You don’t need the Critic’s absolute estimate — relative comparison within the group is enough.</p> <h3 id="trade-off">6.3 Trade-off</h3> <p><strong>Benefits</strong>:</p> <ul> <li>No Critic, about half the compute</li> <li>Simpler training pipeline</li> <li>Reasoning tasks already have outcome-level reward; token-level advantage isn’t needed</li> </ul> <p><strong>Costs</strong>:</p> <ul> <li>Token-level information lost: can’t distinguish “which tokens are critical within the same response”</li> <li>Depends on \(G\) being large enough: GRPO does not work at \(G=1\)</li> <li>Reward collapse risk: late in training, when all \(G\) responses are correct (or all wrong), std → 0</li> </ul> <p>Search-R1’s PPO vs GRPO comparison (Qwen2.5-7B):</p> <table> <thead> <tr> <th>Method</th> <th>Convergence speed</th> <th>Stability</th> <th>Final avg EM</th> </tr> </thead> <tbody> <tr> <td>PPO</td> <td>slow (Critic needs warmup)</td> <td>stable</td> <td>0.431</td> </tr> <tr> <td>GRPO</td> <td>fast</td> <td>tends to collapse late</td> <td>0.350–0.396</td> </tr> </tbody> </table> <h3 id="how-to-choose">6.4 How to Choose</h3> <ul> <li><strong>Reasoning + short training</strong>: GRPO. reward is already outcome-level</li> <li><strong>Long training + chasing peak performance</strong>: PPO. the Critic pays for itself</li> <li><strong>Compute-constrained</strong>: GRPO. saves a model</li> </ul> <hr/> <h2 id="dpo-skipping-the-reward-model-and-rl-altogether">7. DPO: Skipping the Reward Model and RL Altogether</h2> <p>DPO<sup id="fnref:dpo:1"><a href="#fn:dpo" class="footnote" rel="footnote" role="doc-noteref">6</a></sup> (Direct Preference Optimization) takes a completely different path: <strong>no reward model, no RL rollout</strong>. Preference pairs \((x, y_w, y_l)\) (where \(y_w\) is the chosen response and \(y_l\) the rejected one) are treated as plain supervised-learning data.</p> <h3 id="motivation-1">7.1 Motivation</h3> <p>Classic RLHF has a long pipeline: preference data → train RM → PPO rollout → policy update. Each step has its own failure modes: RM gets hacked, PPO is unstable, rollouts are expensive. The DPO question is: can we short-circuit this chain?</p> <h3 id="key-derivation-the-rlhf-optimum-has-a-closed-form">7.2 Key Derivation: The RLHF Optimum Has a Closed Form</h3> <p>The RLHF objective:</p> \[\max_\pi \mathbb{E}_{x, y \sim \pi}[r(x, y)] - \beta D_{KL}[\pi \| \pi_{ref}]\] <p>This problem has a closed-form optimal solution:</p> \[\pi^*(y \mid x) = \frac{1}{Z(x)} \pi_{ref}(y \mid x) \exp\Big(\frac{1}{\beta} r(x, y)\Big)\] <p>where \(Z(x) = \sum_y \pi_{ref}(y \mid x) \exp(r(x, y)/\beta)\) is the normalization constant. Solving for the reward:</p> \[r(x, y) = \beta \log \frac{\pi^*(y \mid x)}{\pi_{ref}(y \mid x)} + \beta \log Z(x)\] <p><strong>Key observation</strong>: reward can be written as a “log probability ratio.” This is the pivot of the entire DPO derivation.</p> <h3 id="from-preference-probability-to-the-dpo-loss">7.3 From Preference Probability to the DPO Loss</h3> <p>Plug into the Bradley-Terry preference model:</p> \[P(y_w \succ y_l \mid x) = \sigma\big(r(x, y_w) - r(x, y_l)\big)\] <p>The subtraction kills the intractable \(\log Z(x)\) term. What remains only involves log probability ratios of the policy and the reference:</p> \[\mathcal{L}^{DPO} = -\mathbb{E}_{(x, y_w, y_l)}\Big[\log \sigma\Big(\beta \log \frac{\pi_\theta(y_w \mid x)}{\pi_{ref}(y_w \mid x)} - \beta \log \frac{\pi_\theta(y_l \mid x)}{\pi_{ref}(y_l \mid x)}\Big)\Big]\] <p>This is just a binary classification loss (logistic regression on preference pairs) — no reward model, no rollout, no PPO.</p> <h3 id="code">7.4 Code</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">compute_dpo_loss</span><span class="p">(</span>
    <span class="n">policy_chosen_logps</span><span class="p">,</span>    <span class="c1"># [B] log prob of y_w under pi_theta (sequence-level)
</span>    <span class="n">policy_rejected_logps</span><span class="p">,</span>  <span class="c1"># [B] log prob of y_l under pi_theta
</span>    <span class="n">ref_chosen_logps</span><span class="p">,</span>       <span class="c1"># [B] log prob of y_w under pi_ref
</span>    <span class="n">ref_rejected_logps</span><span class="p">,</span>     <span class="c1"># [B] log prob of y_l under pi_ref
</span>    <span class="n">beta</span><span class="o">=</span><span class="mf">0.1</span><span class="p">,</span>
<span class="p">):</span>
    <span class="c1"># log ratio of policy to reference for both chosen and rejected
</span>    <span class="n">chosen_ratio</span> <span class="o">=</span> <span class="n">policy_chosen_logps</span> <span class="o">-</span> <span class="n">ref_chosen_logps</span>
    <span class="n">rejected_ratio</span> <span class="o">=</span> <span class="n">policy_rejected_logps</span> <span class="o">-</span> <span class="n">ref_rejected_logps</span>

    <span class="c1"># how much more the policy prefers y_w over y_l, scaled by beta
</span>    <span class="n">logits</span> <span class="o">=</span> <span class="n">beta</span> <span class="o">*</span> <span class="p">(</span><span class="n">chosen_ratio</span> <span class="o">-</span> <span class="n">rejected_ratio</span><span class="p">)</span>

    <span class="c1"># binary classification: push chosen above rejected
</span>    <span class="k">return</span> <span class="o">-</span><span class="n">F</span><span class="p">.</span><span class="nf">logsigmoid</span><span class="p">(</span><span class="n">logits</span><span class="p">).</span><span class="nf">mean</span><span class="p">()</span>
</code></pre></div> </div> <p><strong>Implementation detail</strong>: <code class="language-plaintext highlighter-rouge">logps</code> is the sum of log probabilities over the whole response (sequence-level), not per-token. This is the fundamental difference from PPO’s token-level advantage.</p> <h3 id="dpo-vs-ppo-trade-off">7.5 DPO vs PPO: Trade-off</h3> <table> <thead> <tr> <th>Dimension</th> <th>PPO</th> <th>DPO</th> </tr> </thead> <tbody> <tr> <td>Data source</td> <td>online rollout</td> <td>offline preference pairs</td> </tr> <tr> <td>Reward model</td> <td>required</td> <td>not required</td> </tr> <tr> <td>Training stability</td> <td>need to tune clip, KL, GAE, etc.</td> <td>almost as simple as SFT</td> </tr> <tr> <td>Data efficiency</td> <td>rollouts are expensive</td> <td>preferences collected once</td> </tr> <tr> <td>OOD generalization</td> <td>good (rollout enables exploration)</td> <td>bounded by preference coverage</td> </tr> <tr> <td>Granularity</td> <td>token-level</td> <td>sequence-level</td> </tr> <tr> <td>Best fit</td> <td>verifier available or online interaction</td> <td>static preference data available</td> </tr> </tbody> </table> <p><strong>Key insight</strong>: DPO reduces RL to supervised learning. The cost is losing “online exploration” — the policy can only learn from a static set of preference pairs and cannot self-improve via new rollouts. This is a genuine disadvantage for OOD generalization and reasoning tasks.</p> <h3 id="when-to-choose-dpo">7.6 When to Choose DPO</h3> <ul> <li><strong>Preference data already exists and rollouts are expensive</strong>: engineering-friendly substitute for classic RLHF</li> <li><strong>Tasks like dialogue style or writing preference where verifiers are hard to write</strong></li> <li><strong>Engineering simplicity</strong>: no desire to maintain an RM + PPO stack</li> </ul> <p>Not a good fit:</p> <ul> <li><strong>Reasoning tasks with rule verifiers</strong>: online exploration in PPO / GRPO is stronger</li> <li><strong>Agentic tasks requiring multi-turn environment interaction</strong>: DPO has no concept of rollout and cannot handle this</li> </ul> <p>DPO is complementary to PPO / GRPO, not a replacement. With this in mind, when you get to Search-R1 you won’t ask “why not use DPO?” — Search-R1 fundamentally requires rollouts and environment interaction.</p> <hr/> <h2 id="search-r1-a-complete-instance-of-agentic-rl">8. Search-R1: A Complete Instance of Agentic RL</h2> <p>All the pieces above come together in this section.</p> <h3 id="task-formalization">8.1 Task Formalization</h3> <p>A Search-R1 rollout is an alternating interaction between the LLM and a search engine:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;think&gt;reasoning about what to search&lt;/think&gt;
&lt;search&gt;query string&lt;/search&gt;
&lt;information&gt;[retrieved passages injected by environment]&lt;/information&gt;
&lt;think&gt;reasoning about retrieved info&lt;/think&gt;
&lt;search&gt;follow-up query&lt;/search&gt;
&lt;information&gt;[more retrieved passages]&lt;/information&gt;
...
&lt;answer&gt;final answer&lt;/answer&gt;
</code></pre></div> </div> <p>The template constrains only <strong>structure</strong>, not <strong>strategy</strong> — RL itself discovers the search policy.</p> <h3 id="rollout-process">8.2 Rollout Process</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">rollout_with_search</span><span class="p">(</span><span class="n">model</span><span class="p">,</span> <span class="n">tokenizer</span><span class="p">,</span> <span class="n">search_engine</span><span class="p">,</span> <span class="n">prompt</span><span class="p">,</span> <span class="n">max_turns</span><span class="o">=</span><span class="mi">4</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    Search-R1 rollout: interleave LLM generation with search engine calls.
    </span><span class="sh">"""</span>
    <span class="n">tokens</span> <span class="o">=</span> <span class="n">tokenizer</span><span class="p">.</span><span class="nf">encode</span><span class="p">(</span><span class="n">prompt</span><span class="p">)</span>
    <span class="n">action_mask</span> <span class="o">=</span> <span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">*</span> <span class="nf">len</span><span class="p">(</span><span class="n">tokens</span><span class="p">)</span>  <span class="c1"># prompt tokens are not LLM-generated
</span>
    <span class="k">for</span> <span class="n">turn</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">max_turns</span><span class="p">):</span>
        <span class="n">new_tokens</span><span class="p">,</span> <span class="n">stop</span> <span class="o">=</span> <span class="n">model</span><span class="p">.</span><span class="nf">generate</span><span class="p">(</span>
            <span class="n">tokens</span><span class="p">,</span>
            <span class="n">stop_sequences</span><span class="o">=</span><span class="p">[</span><span class="sh">"</span><span class="s">&lt;/search&gt;</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">&lt;/answer&gt;</span><span class="sh">"</span><span class="p">],</span>
            <span class="n">max_new_tokens</span><span class="o">=</span><span class="mi">500</span><span class="p">,</span>
        <span class="p">)</span>
        <span class="n">tokens</span><span class="p">.</span><span class="nf">extend</span><span class="p">(</span><span class="n">new_tokens</span><span class="p">)</span>
        <span class="n">action_mask</span><span class="p">.</span><span class="nf">extend</span><span class="p">([</span><span class="mi">1</span><span class="p">]</span> <span class="o">*</span> <span class="nf">len</span><span class="p">(</span><span class="n">new_tokens</span><span class="p">))</span>       <span class="c1"># LLM-generated
</span>
        <span class="k">if</span> <span class="n">stop</span> <span class="o">==</span> <span class="sh">"</span><span class="s">&lt;/answer&gt;</span><span class="sh">"</span><span class="p">:</span>
            <span class="k">break</span>
        <span class="k">elif</span> <span class="n">stop</span> <span class="o">==</span> <span class="sh">"</span><span class="s">&lt;/search&gt;</span><span class="sh">"</span><span class="p">:</span>
            <span class="n">query</span> <span class="o">=</span> <span class="nf">extract_search_query</span><span class="p">(</span><span class="n">tokens</span><span class="p">)</span>
            <span class="n">retrieved</span> <span class="o">=</span> <span class="n">search_engine</span><span class="p">.</span><span class="nf">retrieve</span><span class="p">(</span><span class="n">query</span><span class="p">,</span> <span class="n">top_k</span><span class="o">=</span><span class="mi">3</span><span class="p">)</span>
            <span class="n">retrieved_text</span> <span class="o">=</span> <span class="sa">f</span><span class="sh">"</span><span class="s">&lt;information&gt;</span><span class="si">{</span><span class="nf">format_passages</span><span class="p">(</span><span class="n">retrieved</span><span class="p">)</span><span class="si">}</span><span class="s">&lt;/information&gt;</span><span class="sh">"</span>
            <span class="n">retrieved_tokens</span> <span class="o">=</span> <span class="n">tokenizer</span><span class="p">.</span><span class="nf">encode</span><span class="p">(</span><span class="n">retrieved_text</span><span class="p">)</span>
            <span class="n">tokens</span><span class="p">.</span><span class="nf">extend</span><span class="p">(</span><span class="n">retrieved_tokens</span><span class="p">)</span>
            <span class="n">action_mask</span><span class="p">.</span><span class="nf">extend</span><span class="p">([</span><span class="mi">0</span><span class="p">]</span> <span class="o">*</span> <span class="nf">len</span><span class="p">(</span><span class="n">retrieved_tokens</span><span class="p">))</span>  <span class="c1"># NOT LLM-generated
</span>
    <span class="k">return</span> <span class="n">tokens</span><span class="p">,</span> <span class="n">action_mask</span>
</code></pre></div> </div> <h3 id="core-technical-contribution-retrieved-token-masking">8.3 Core Technical Contribution: Retrieved Token Masking</h3> <p><strong>Problem</strong>: the rollout sequence mixes in tokens not produced by the LLM (retrieved passages). If you don’t handle them, policy gradient will compute gradients on those tokens too — which is wrong: those tokens are not actions of the policy.</p> <p>Worse, failing to mask lets the loss treat “the stylistic features of search-engine output” as something the policy should imitate. The policy ends up learning to mimic the retrieval text style instead of learning “how to search.”</p> <p><strong>Solution</strong>: only compute loss over LLM-generated tokens:</p> \[\mathcal{L}^{Search\text{-}R1} = \frac{\sum_t I(y_t) \cdot \mathcal{L}^{PPO}_t}{\sum_t I(y_t)}, \quad I(y_t) = \begin{cases} 1 &amp; y_t \text{ is LLM-generated} \\ 0 &amp; y_t \text{ is retrieved} \end{cases}\] <p>In implementation, <code class="language-plaintext highlighter-rouge">action_mask</code> just gets passed into the PPO loss:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">search_r1_loss</span><span class="p">(</span><span class="n">logits</span><span class="p">,</span> <span class="n">old_logprobs</span><span class="p">,</span> <span class="n">ref_logprobs</span><span class="p">,</span> <span class="n">actions</span><span class="p">,</span>
                   <span class="n">advantages</span><span class="p">,</span> <span class="n">action_mask</span><span class="p">,</span> <span class="n">clip_eps</span><span class="o">=</span><span class="mf">0.2</span><span class="p">,</span> <span class="n">kl_coef</span><span class="o">=</span><span class="mf">0.001</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    Identical to standard PPO loss, except action_mask now also
    excludes retrieved tokens in addition to padding.
    </span><span class="sh">"""</span>
    <span class="n">log_probs</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">log_softmax</span><span class="p">(</span><span class="n">logits</span><span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>
    <span class="n">new_logprobs</span> <span class="o">=</span> <span class="n">log_probs</span><span class="p">.</span><span class="nf">gather</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="n">actions</span><span class="p">.</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)).</span><span class="nf">squeeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span>

    <span class="n">ratio</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">new_logprobs</span> <span class="o">-</span> <span class="n">old_logprobs</span><span class="p">)</span>
    <span class="n">surr1</span> <span class="o">=</span> <span class="n">ratio</span> <span class="o">*</span> <span class="n">advantages</span>
    <span class="n">surr2</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">clamp</span><span class="p">(</span><span class="n">ratio</span><span class="p">,</span> <span class="mi">1</span> <span class="o">-</span> <span class="n">clip_eps</span><span class="p">,</span> <span class="mi">1</span> <span class="o">+</span> <span class="n">clip_eps</span><span class="p">)</span> <span class="o">*</span> <span class="n">advantages</span>
    <span class="n">policy_loss</span> <span class="o">=</span> <span class="o">-</span><span class="n">torch</span><span class="p">.</span><span class="nf">min</span><span class="p">(</span><span class="n">surr1</span><span class="p">,</span> <span class="n">surr2</span><span class="p">)</span>

    <span class="n">log_ratio_ref</span> <span class="o">=</span> <span class="n">ref_logprobs</span> <span class="o">-</span> <span class="n">new_logprobs</span>
    <span class="n">kl</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">log_ratio_ref</span><span class="p">)</span> <span class="o">-</span> <span class="n">log_ratio_ref</span> <span class="o">-</span> <span class="mi">1</span>

    <span class="c1"># action_mask = 0 for padding OR retrieved tokens
</span>    <span class="n">total</span> <span class="o">=</span> <span class="p">(</span><span class="n">policy_loss</span> <span class="o">+</span> <span class="n">kl_coef</span> <span class="o">*</span> <span class="n">kl</span><span class="p">)</span> <span class="o">*</span> <span class="n">action_mask</span>
    <span class="k">return</span> <span class="n">total</span><span class="p">.</span><span class="nf">sum</span><span class="p">()</span> <span class="o">/</span> <span class="n">action_mask</span><span class="p">.</span><span class="nf">sum</span><span class="p">()</span>
</code></pre></div> </div> <p><strong>Effect</strong> (Qwen2.5-7B, avg EM over 7 QA datasets):</p> <table> <thead> <tr> <th>Setting</th> <th>Avg EM</th> </tr> </thead> <tbody> <tr> <td>Do not mask retrieved tokens</td> <td>0.343</td> </tr> <tr> <td>Mask retrieved tokens</td> <td><strong>0.431</strong></td> </tr> </tbody> </table> <p>This single change gives ~25% relative improvement. <strong>The right definition of the action space can have a larger effect on RL training than the algorithmic changes themselves.</strong></p> <p>More abstractly: <strong>RL should only optimize variables the agent controls.</strong> Retrieved content is the environment’s response — the agent doesn’t control it, so it shouldn’t be shaped by policy gradient. This is a basic RL principle; it is easy to overlook in “pure LLM generation” settings, and Agentic RL makes it matter again.</p> <h3 id="experimental-observations">8.4 Experimental Observations</h3> <p><strong>Main results</strong> (avg EM over 7 QA datasets, Qwen2.5-7B):</p> <table> <thead> <tr> <th>Method</th> <th>Avg EM</th> </tr> </thead> <tbody> <tr> <td>Direct Inference</td> <td>0.181</td> </tr> <tr> <td>RAG</td> <td>0.304</td> </tr> <tr> <td>R1 (RL without search)</td> <td>0.276</td> </tr> <tr> <td>Rejection Sampling + SFT</td> <td>0.348</td> </tr> <tr> <td><strong>Search-R1 (PPO)</strong></td> <td><strong>0.431</strong></td> </tr> </tbody> </table> <p><strong>Emergent behaviors</strong>: the trained model exhibits patterns not explicitly taught by the paper:</p> <ol> <li><strong>Self-verification</strong>: after reaching an answer, issue one more query to confirm</li> <li><strong>Query refinement</strong>: if the first search fails, rewrite the query and retry</li> <li><strong>Problem decomposition</strong>: complex questions get automatically decomposed into sub-queries</li> </ol> <p>Table 9 of the paper shows a case: asking “In which city and state was the singer of the perfume Curious born?” Search-R1 proceeds as:</p> <ol> <li><code class="language-plaintext highlighter-rouge">&lt;search&gt;Curious fragrance information&lt;/search&gt;</code> → finds it’s Britney Spears’s</li> <li><code class="language-plaintext highlighter-rouge">&lt;search&gt;Britney Spears birthplace&lt;/search&gt;</code> → McComb, Mississippi</li> <li><code class="language-plaintext highlighter-rouge">&lt;search&gt;McComb, Mississippi location&lt;/search&gt;</code> → verification</li> <li><code class="language-plaintext highlighter-rouge">&lt;answer&gt;McComb, Mississippi&lt;/answer&gt;</code></li> </ol> <p>Compare R1 without search: it guesses “Houston” (wrongly attributing Curious to Beyoncé).</p> <hr/> <h2 id="a-panoramic-summary-of-the-design-space">9. A Panoramic Summary of the Design Space</h2> <h3 id="two-orthogonal-dimensions-1">9.1 Two Orthogonal Dimensions</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>                    │ Critic + GAE      │ Group Norm         │
                    │ (token-level adv) │ (response-level)   │
────────────────────┼───────────────────┼────────────────────┤
  Neural RM         │ classic RLHF      │ uncommon           │
────────────────────┼───────────────────┼────────────────────┤
  Rule Verifier     │ Search-R1         │ DeepSeek-R1        │
────────────────────┼───────────────────┼────────────────────┤
  Gen Verifier      │ emerging          │ emerging           │
</code></pre></div> </div> <p><strong>The horizontal axis governs cost and stability.</strong> Critic + GAE needs one more model but is more stable; Group Norm saves a model at the cost of coarser granularity.</p> <p><strong>The vertical axis governs applicable scope.</strong> Neural RM covers more tasks but can be hacked; rule verifiers are hack-resistant but cover less; generative verifiers sit between the two.</p> <h3 id="the-essence-of-agentic-rl">9.2 The Essence of Agentic RL</h3> <p>Agentic RL is not a new algorithm — it is an <strong>extension of the environment</strong>. The environment of traditional RL for LLM has only prompt and terminal reward; the environment of Agentic RL also includes tools, intermediate feedback, and state changes.</p> <p>The algorithm layer is still PPO or GRPO. The key change is in the rollout — no longer a one-shot generation but alternating interaction between policy and environment. This brings two new challenges:</p> <ol> <li><strong>Non-policy tokens mixed into the rollout sequence</strong> → need retrieved token masking</li> <li><strong>Higher rollout cost</strong> → higher bar for sample efficiency</li> </ol> <h3 id="current-limitations-and-open-problems">9.3 Current Limitations and Open Problems</h3> <ol> <li><strong>Reward design for open-ended tasks</strong>: rule verifiers only work for tasks with clear right and wrong. Writing, dialogue, and similar tasks still need neural RMs or human feedback.</li> <li><strong>Long-horizon reasoning</strong>: when the reasoning chain runs to tens of thousands of tokens, both Critic and Group Norm face challenges.</li> <li><strong>Multi-tool coordination</strong>: how to attribute reward across tools, how to manage the rollout of complex tool-call graphs — both still open.</li> <li><strong>Theoretical explanation of emergent behaviors under outcome reward</strong>: why do behaviors like self-verification emerge spontaneously? Under what conditions? No systematic answer yet.</li> </ol> <hr/> <h2 id="closing-and-references">10. Closing and References</h2> <h3 id="quick-index">Quick Index</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Cross Entropy                  -log π(y_t)              per-token base loss
SFT Loss                       Σ CE                     unweighted
REINFORCE                      Σ CE · A_t               advantage-weighted
PPO                            + clip + min + KL        stabilized policy gradient
GRPO                           + group norm             drops the Critic
DPO                            logistic on pref pairs   offline path skipping RL
Search-R1                      + retrieved token mask   Agentic RL instance

Policy (π_θ)                   LLM being trained
Reference (π_ref)              LLM at training start, frozen
Old (π_old)                    snapshot at this rollout
Critic (V_φ)                   estimates future return
Reward Model                   scalar on (prompt, response)
Verifier                       general concept: function/model judging correctness
</code></pre></div> </div> </div> <div class="lang-content lang-zh"> <h2 id="section">前言</h2> <p>过去一年 reasoning LLM 的主要进展——o1、DeepSeek-R1、Search-R1——共享一套 RL 技术基础，却在几个关键设计点上做了不同选择。把这些选择并列对比，比单独读任一篇论文都更能看清设计空间。</p> <p>这篇文章做的就是这件事。主线是 Search-R1<sup id="fnref:sr1:1"><a href="#fn:sr1" class="footnote" rel="footnote" role="doc-noteref">1</a></sup>：它是 Agentic RL 的一个清晰实例，又足够简单，能把 PPO、GRPO、verifier、token masking 这些概念自然串起来。</p> <p>假设读者熟悉 transformer、SFT 和基础 DL。不假设熟悉 RL 细节。</p> <p>不覆盖：RLHF 偏好数据构造、inference-time scaling（best-of-N、tree search）、multi-agent、硬件工程优化。</p> <hr/> <h2 id="post-training-">1. 问题设定：post-training 的技术栈演进</h2> <p>从 pretrained LLM 走到能自主调用工具的 agent，中间经过几次技术跳跃。每一跳都在解决上一跳的局限：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Pretrain
   │ 知道很多，但不听话
   ↓
SFT (Supervised Fine-Tuning)
   │ 只会模仿，不会主动抑制错误
   ↓
RLHF (PPO + Reward Model)
   │ RM 易被 hack；偏好数据贵；reasoning 信号弱
   ↓
Reasoning RL (GRPO + Verifier)
   │ reward 只从模型自己的输出来，无法与外界交互
   ↓
Agentic RL (+ Tool / Environment)
</code></pre></div> </div> <p>每一跳的技术内核：</p> <ul> <li><strong>SFT</strong>：cross entropy + 监督学习</li> <li><strong>RLHF</strong>：PPO + 神经网络 reward model</li> <li><strong>Reasoning RL</strong>：GRPO + rule-based verifier，砍掉 reward model</li> <li><strong>Agentic RL</strong>：在 Reasoning RL 基础上，把环境交互嵌入 rollout</li> </ul> <p>“Agentic RL” 并不是一个独立的新算法，而是”在 rollout 中引入环境交互”的训练范式。算法层仍然是 PPO 或 GRPO。关键变化在 <strong>环境的扩展</strong>：rollout 中间会插入工具调用，执行或检索的结果被注入到 sequence，成为后续 generation 的 context。</p> <p>Search-R1 是这个范式最干净的实例：把搜索引擎作为 environment，让 LLM 在 reasoning 过程中自主决定何时搜、搜什么。</p> <p>在这条线性主线之外还有一条支线：<strong>DPO</strong> 把 RLHF 直接化成监督学习，不跑 RL、不训 reward model。§7 会单独讨论。</p> <p>下面的章节依次展开每个组件。</p> <hr/> <h2 id="cross-entropy--policy-gradient">2. 从 Cross Entropy 到 Policy Gradient</h2> <p>本节建立 RL loss 和 SFT loss 的等价关系，作为后面理解 PPO 的地基。</p> <h3 id="cross-entropy--next-token-prediction-">2.1 Cross Entropy 在 next-token prediction 下的化简</h3> <p>一般定义：</p> \[H(p, q) = -\sum_i p_i \log q_i\] <p>next-token 场景下真实标签 \(p\) 是 one-hot，\(p_{y_t} = 1\)、其他为 0。对 vocab 求和只剩一项：</p> \[H(p, q) = -\log \pi_\theta(y_t \mid y_{&lt;t})\] <p>SFT 对整个序列求和：</p> \[\mathcal{L}^{SFT} = -\sum_{t=1}^{T} \log \pi_\theta(y_t \mid y_{&lt;t})\] <h3 id="ce--kl-">2.2 CE 和 KL 的细微之处</h3> <p>两者的恒等式：</p> \[H(p, q) = H(p) + D_{KL}(p \| q)\] <p>监督学习里 \(p\) 是 ground truth、固定不变，\(H(p)\) 对 \(\theta\) 是常数——所以 <strong>优化 CE 和优化 KL 在梯度上等价</strong>。我们默认用 CE 仅仅是因为它更便宜。</p> <p>在 RL 里这个等价会失效：KL 的两边都是模型（\(\pi_\theta\) vs \(\pi_{ref}\)），都不固定。这时 CE 和 KL 的差别才真正重要。后面 PPO 的 KL penalty 会用到这一点。</p> <h3 id="sft--reinforce">2.3 从 SFT 到 REINFORCE</h3> <p>SFT loss 对每个 token 都在”无条件推高它的概率”——demo data 里的 token 被默认是好的。在 imitation learning 里这个假设合理，但 RL 想做的更多：<strong>既要推高好行为，也要主动压低坏行为</strong>。</p> <p>REINFORCE 在 SFT loss 上乘一个权重 \(A_t\)：</p> \[\mathcal{L}^{REINFORCE} = -\mathbb{E}\Big[\sum_t \log \pi_\theta(y_t \mid y_{&lt;t}) \cdot A_t\Big]\] <ul> <li>\(A_t &gt; 0\)：好 token，推高概率</li> <li>\(A_t &lt; 0\)：差 token，压低概率</li> <li>\(\lvert A_t \rvert\) 越大，调整幅度越大</li> </ul> <p>三个 loss 的统一形式：</p> <table> <thead> <tr> <th>Loss</th> <th>单 token 形式</th> <th>权重含义</th> </tr> </thead> <tbody> <tr> <td>SFT</td> <td>\(-\log \pi_\theta(y_t)\)</td> <td>1（所有 token 都推高）</td> </tr> <tr> <td>REINFORCE</td> <td>\(-\log \pi_\theta(y_t) \cdot A_t\)</td> <td>advantage 加权</td> </tr> <tr> <td>PPO</td> <td>\(-\min(r_t A_t, \text{clip}(r_t) A_t)\)</td> <td>ratio 替代 log（下节解释）</td> </tr> </tbody> </table> <p><strong>核心洞察</strong>：RL loss 在形式上就是”带符号、加权的 cross entropy”。SFT 在 \(A_t \equiv 1\) 时和 RL 在 <strong>梯度形式</strong> 上重合。需注意这只是形式上的类比——SFT 采样自 demo data，RL 采样自 \(\pi_\theta\)，两者的样本分布并不相同。</p> <h3 id="section-1">2.4 代码对比</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># SFT loss
</span><span class="k">def</span> <span class="nf">sft_loss</span><span class="p">(</span><span class="n">logits</span><span class="p">,</span> <span class="n">actions</span><span class="p">,</span> <span class="n">mask</span><span class="p">):</span>
    <span class="n">log_probs</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">log_softmax</span><span class="p">(</span><span class="n">logits</span><span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>
    <span class="n">token_logprobs</span> <span class="o">=</span> <span class="n">log_probs</span><span class="p">.</span><span class="nf">gather</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="n">actions</span><span class="p">.</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)).</span><span class="nf">squeeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span>
    <span class="k">return</span> <span class="o">-</span><span class="p">(</span><span class="n">token_logprobs</span> <span class="o">*</span> <span class="n">mask</span><span class="p">).</span><span class="nf">sum</span><span class="p">()</span> <span class="o">/</span> <span class="n">mask</span><span class="p">.</span><span class="nf">sum</span><span class="p">()</span>

<span class="c1"># REINFORCE loss: only differs by * advantages
</span><span class="k">def</span> <span class="nf">reinforce_loss</span><span class="p">(</span><span class="n">logits</span><span class="p">,</span> <span class="n">actions</span><span class="p">,</span> <span class="n">advantages</span><span class="p">,</span> <span class="n">mask</span><span class="p">):</span>
    <span class="n">log_probs</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">log_softmax</span><span class="p">(</span><span class="n">logits</span><span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>
    <span class="n">token_logprobs</span> <span class="o">=</span> <span class="n">log_probs</span><span class="p">.</span><span class="nf">gather</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="n">actions</span><span class="p">.</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)).</span><span class="nf">squeeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span>
    <span class="k">return</span> <span class="o">-</span><span class="p">(</span><span class="n">token_logprobs</span> <span class="o">*</span> <span class="n">advantages</span> <span class="o">*</span> <span class="n">mask</span><span class="p">).</span><span class="nf">sum</span><span class="p">()</span> <span class="o">/</span> <span class="n">mask</span><span class="p">.</span><span class="nf">sum</span><span class="p">()</span>
</code></pre></div> </div> <p>两者差别就一个 <code class="language-plaintext highlighter-rouge">* advantages</code>。正是这个乘法打开了 SFT 和 RL 的分野：<strong>RL 能用负权重抑制错误行为，SFT 不能</strong>。</p> <hr/> <h2 id="ppo-">3. PPO 的设计动机与每个组件的作用</h2> <p>PPO<sup id="fnref:ppo:1"><a href="#fn:ppo" class="footnote" rel="footnote" role="doc-noteref">2</a></sup> 是当前 LLM RL 训练的事实标准。本节按”组件 → 解决什么问题”的顺序拆开。</p> <p>下面写的是 LLM RLHF 版 PPO，比原 PPO 论文多了一项 \(\pi_{ref}\) 的 KL 惩罚（这是 InstructGPT<sup id="fnref:instructgpt:1"><a href="#fn:instructgpt" class="footnote" rel="footnote" role="doc-noteref">3</a></sup> 起带进来的工程惯例，不是原始 PPO 的一部分）：</p> \[\mathcal{L}^{PPO}(\theta) = -\mathbb{E}_t\Big[\min\big(r_t(\theta) A_t,\ \text{clip}(r_t(\theta), 1-\epsilon, 1+\epsilon) A_t\big)\Big] + \beta \cdot D_{KL}\big[\pi_\theta \| \pi_{ref}\big]\] <h3 id="reinforce-">3.1 起点：REINFORCE 的两个病</h3> <p><strong>病一：方差大，单步更新可能崩。</strong> \(A_t\) 从单条 trajectory 估，方差大。偶然的高 reward 样本让某些 token 的 \(A_t\) 异常大，一次 gradient step 就能把 \(\pi_\theta\) 推到奇怪位置。</p> <p><strong>病二：样本效率太低。</strong> REINFORCE 严格 on-policy：每次更新后旧 rollout 作废。对 LLM 这种 rollout 极贵的场景不可接受。</p> <p>PPO 的每个组件都在治这两个病。</p> <h3 id="importance-ratio-rollout-">3.2 Importance Ratio：让一批 rollout 多用几次</h3> \[r_t(\theta) = \frac{\pi_\theta(y_t \mid y_{&lt;t})}{\pi_{old}(y_t \mid y_{&lt;t})}\] <p>\(\pi_{old}\) 是采样这批 rollout 时的 policy 快照。训练循环变成：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span> <span class="n">iteration</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">N</span><span class="p">):</span>
    <span class="c1"># snapshot current policy
</span>    <span class="n">pi_old</span> <span class="o">=</span> <span class="nf">copy</span><span class="p">(</span><span class="n">pi_theta</span><span class="p">).</span><span class="nf">detach</span><span class="p">()</span>

    <span class="c1"># collect rollouts using pi_old
</span>    <span class="n">rollouts</span> <span class="o">=</span> <span class="nf">sample_rollouts</span><span class="p">(</span><span class="n">pi_old</span><span class="p">,</span> <span class="n">batch_size</span><span class="o">=</span><span class="n">B</span><span class="p">)</span>
    <span class="n">advantages</span> <span class="o">=</span> <span class="nf">compute_gae</span><span class="p">(</span><span class="n">rollouts</span><span class="p">)</span>

    <span class="c1"># multiple gradient updates on the same rollouts
</span>    <span class="k">for</span> <span class="n">epoch</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">K</span><span class="p">):</span>  <span class="c1"># typically K = 2~4
</span>        <span class="k">for</span> <span class="n">minibatch</span> <span class="ow">in</span> <span class="nf">split</span><span class="p">(</span><span class="n">rollouts</span><span class="p">):</span>
            <span class="n">loss</span> <span class="o">=</span> <span class="nf">ppo_loss</span><span class="p">(</span><span class="n">pi_theta</span><span class="p">,</span> <span class="n">pi_old</span><span class="p">,</span> <span class="n">minibatch</span><span class="p">,</span> <span class="n">advantages</span><span class="p">)</span>
            <span class="n">loss</span><span class="p">.</span><span class="nf">backward</span><span class="p">()</span>
            <span class="n">optimizer</span><span class="p">.</span><span class="nf">step</span><span class="p">()</span>
</code></pre></div> </div> <p>同批 rollout 被用 \(K\) 次。更新到第 \(k\) 个 epoch 时 \(\pi_\theta \neq \pi_{old}\)，这就是 off-policy 的。Importance ratio 在数学上修正这个 mismatch（importance sampling 的标准应用）。</p> <p><strong>符号陷阱</strong>：这里 \(r_t\) 是 importance ratio，不是 reward。Reward 在 PPO 中通过 GAE 化成 \(A_t\)，loss 里根本没有 reward 出现。</p> <h3 id="clip">3.3 Clip：单步幅度约束</h3> <p>只有 ratio 还不够。如果 \(\pi_\theta\) 偏离 \(\pi_{old}\) 太远（比如 \(r_t = 5\)），importance sampling 的方差会爆炸。Clip 硬压回来：</p> \[\text{clip}(r_t, 1-\epsilon, 1+\epsilon)\] <p>典型 \(\epsilon = 0.2\)，policy 在单个 token 上的概率比例变化不超过 ±20%。</p> <p><strong>但单独用 clip 有坑</strong>。设 \(A_t = +1\)（好 token），\(r_t = 0.5\)（policy 反而压低了它——这是错误状态）。如果直接用 \(\text{clip}(r_t) \cdot A_t = 0.8\)，梯度被 <strong>人为放大</strong>，反而鼓励 policy 继续走错方向。</p> <h3 id="min-clip-">3.4 Min：让 clip 只刹车、不阻止纠错</h3> <p>PPO 的完整 policy 项：</p> \[L_t^{CLIP} = \min\big(r_t A_t,\ \text{clip}(r_t, 1-\epsilon, 1+\epsilon) \cdot A_t\big)\] <p>设 \(\epsilon = 0.2\)，四种情况：</p> <table> <thead> <tr> <th>情况</th> <th>\(A_t\)</th> <th>\(r_t\)</th> <th>\(r_t A_t\)</th> <th>\(\text{clip}(r_t) A_t\)</th> <th>min 选谁</th> <th>含义</th> </tr> </thead> <tbody> <tr> <td>B</td> <td>\(+1\)</td> <td>\(1.5\)（超上界）</td> <td>\(1.5\)</td> <td>\(1.2\)</td> <td>clip 项</td> <td>好 token 推过头，<strong>饱和</strong></td> </tr> <tr> <td>C</td> <td>\(+1\)</td> <td>\(0.5\)（低于下界）</td> <td>\(0.5\)</td> <td>\(0.8\)</td> <td><strong>原项</strong></td> <td>好 token 反被压低，<strong>允许纠正</strong></td> </tr> <tr> <td>D</td> <td>\(-1\)</td> <td>\(0.5\)（低于下界）</td> <td>\(-0.5\)</td> <td>\(-0.8\)</td> <td>clip 项</td> <td>坏 token 压够了，<strong>饱和</strong></td> </tr> <tr> <td>E</td> <td>\(-1\)</td> <td>\(1.5\)（超上界）</td> <td>\(-1.5\)</td> <td>\(-1.2\)</td> <td><strong>原项</strong></td> <td>坏 token 反被推高，<strong>允许纠正</strong></td> </tr> </tbody> </table> <p>规律：</p> <ul> <li>朝 <strong>正确方向</strong> 走过头（B, D）→ min 选 clip 项，梯度饱和</li> <li>朝 <strong>错误方向</strong> 走（C, E）→ min 选原项，梯度保留，继续纠错</li> </ul> <p>Schulman 称此为 <strong>pessimistic bound</strong>：取较小者做保守估计。一举两得：限制单步幅度（B, D），但不阻断错误修复（C, E）。</p> <h3 id="kl-penalty">3.5 KL Penalty：长期防漂移</h3> <p>Clip 管 <strong>单步</strong> 稳定性。但即使每步都在 \([1-\epsilon, 1+\epsilon]\)，多 iteration 累积仍可能让 \(\pi_\theta\) 漂到完全不同的分布。典型失败模式是 <strong>reward hacking</strong>：模型发现某种生成模式能稳定拿高 reward，但语言质量崩坏；每步 clip 都不触发，但累积后 policy 已面目全非。</p> <p>PPO 加 KL 项：</p> \[\beta \cdot D_{KL}\big[\pi_\theta(\cdot \mid s_t) \| \pi_{ref}(\cdot \mid s_t)\big]\] <p>\(\pi_{ref}\) 是 <strong>训练起点</strong> 的 policy（通常是 SFT 后的模型），整个 RL 过程冻结。它和 \(\pi_{old}\) 不是一回事：</p> <table> <thead> <tr> <th>维度</th> <th>Clip on \(r_t = \pi_\theta / \pi_{old}\)</th> <th>KL on \(\pi_\theta | \pi_{ref}\)</th> </tr> </thead> <tbody> <tr> <td>比较对象</td> <td>上一轮 rollout 时的 policy</td> <td>训练起点的 policy</td> </tr> <tr> <td>时间尺度</td> <td>单 iteration</td> <td>整个训练过程</td> </tr> <tr> <td>形式</td> <td>硬约束（饱和截断）</td> <td>软约束（连续惩罚）</td> </tr> <tr> <td>解决问题</td> <td>单步更新过激</td> <td>累积漂移</td> </tr> </tbody> </table> <p>两者 <strong>互补，不可替代</strong>。只有 clip、没有 KL，长期会 reward hacking；只有 KL、没有 clip，单次大更新可能直接崩。</p> <p><strong>k3 estimator</strong>。Token 级 KL 按定义要遍历整个 vocab，开销大。实际用 Schulman 的 k3<sup id="fnref:k3:1"><a href="#fn:k3" class="footnote" rel="footnote" role="doc-noteref">4</a></sup>：</p> \[\text{KL}_{k3} = \exp(\log\pi_{ref} - \log\pi_\theta) - (\log\pi_{ref} - \log\pi_\theta) - 1\] <p>严格讲 k3 是 \(D_{KL}[p \| q]\) 在 “从 \(p\) 采样” 时的低方差无偏估计。PPO 中我们实际从 \(\pi_{old}\) 采样（因为 rollout 是用 \(\pi_{old}\) 跑的），严格来说是一个近似。但 \(\pi_{old} \approx \pi_\theta\)（在 clip 的约束下），实践中这个近似足够好。</p> <p>代码一行：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">log_ratio</span> <span class="o">=</span> <span class="n">ref_logprobs</span> <span class="o">-</span> <span class="n">new_logprobs</span>
<span class="n">kl</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">log_ratio</span><span class="p">)</span> <span class="o">-</span> <span class="n">log_ratio</span> <span class="o">-</span> <span class="mi">1</span>  <span class="c1"># k3 estimator
</span></code></pre></div> </div> <h3 id="section-2">3.6 完整代码</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">compute_ppo_loss</span><span class="p">(</span>
    <span class="n">logits</span><span class="p">,</span>          <span class="c1"># [B, T, V] current policy outputs
</span>    <span class="n">old_logprobs</span><span class="p">,</span>    <span class="c1"># [B, T] log prob from pi_old (detached)
</span>    <span class="n">ref_logprobs</span><span class="p">,</span>    <span class="c1"># [B, T] log prob from pi_ref (frozen)
</span>    <span class="n">actions</span><span class="p">,</span>         <span class="c1"># [B, T] sampled tokens
</span>    <span class="n">advantages</span><span class="p">,</span>      <span class="c1"># [B, T] from GAE (Section 4)
</span>    <span class="n">action_mask</span><span class="p">,</span>     <span class="c1"># [B, T] 1 for policy-generated positions
</span>    <span class="n">clip_eps</span><span class="o">=</span><span class="mf">0.2</span><span class="p">,</span>
    <span class="n">kl_coef</span><span class="o">=</span><span class="mf">0.001</span><span class="p">,</span>
<span class="p">):</span>
    <span class="n">log_probs</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">log_softmax</span><span class="p">(</span><span class="n">logits</span><span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>
    <span class="n">new_logprobs</span> <span class="o">=</span> <span class="n">log_probs</span><span class="p">.</span><span class="nf">gather</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="n">actions</span><span class="p">.</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)).</span><span class="nf">squeeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span>

    <span class="c1"># === importance ratio (3.2) ===
</span>    <span class="n">ratio</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">new_logprobs</span> <span class="o">-</span> <span class="n">old_logprobs</span><span class="p">)</span>

    <span class="c1"># === clip + min (3.3, 3.4) ===
</span>    <span class="n">surr1</span> <span class="o">=</span> <span class="n">ratio</span> <span class="o">*</span> <span class="n">advantages</span>
    <span class="n">surr2</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">clamp</span><span class="p">(</span><span class="n">ratio</span><span class="p">,</span> <span class="mi">1</span> <span class="o">-</span> <span class="n">clip_eps</span><span class="p">,</span> <span class="mi">1</span> <span class="o">+</span> <span class="n">clip_eps</span><span class="p">)</span> <span class="o">*</span> <span class="n">advantages</span>
    <span class="n">policy_loss</span> <span class="o">=</span> <span class="o">-</span><span class="n">torch</span><span class="p">.</span><span class="nf">min</span><span class="p">(</span><span class="n">surr1</span><span class="p">,</span> <span class="n">surr2</span><span class="p">)</span>

    <span class="c1"># === KL penalty (3.5, k3 estimator) ===
</span>    <span class="n">log_ratio_ref</span> <span class="o">=</span> <span class="n">ref_logprobs</span> <span class="o">-</span> <span class="n">new_logprobs</span>
    <span class="n">kl</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">log_ratio_ref</span><span class="p">)</span> <span class="o">-</span> <span class="n">log_ratio_ref</span> <span class="o">-</span> <span class="mi">1</span>

    <span class="n">total</span> <span class="o">=</span> <span class="p">(</span><span class="n">policy_loss</span> <span class="o">+</span> <span class="n">kl_coef</span> <span class="o">*</span> <span class="n">kl</span><span class="p">)</span> <span class="o">*</span> <span class="n">action_mask</span>
    <span class="k">return</span> <span class="n">total</span><span class="p">.</span><span class="nf">sum</span><span class="p">()</span> <span class="o">/</span> <span class="n">action_mask</span><span class="p">.</span><span class="nf">sum</span><span class="p">()</span>
</code></pre></div> </div> <h3 id="section-3">3.7 五个正则机制一览</h3> <table> <thead> <tr> <th>机制</th> <th>作用对象</th> <th>解决问题</th> </tr> </thead> <tbody> <tr> <td>Importance ratio</td> <td>data / policy mismatch</td> <td>让 rollout 多次复用</td> </tr> <tr> <td>Clip on ratio</td> <td>单 token 单步更新</td> <td>单次偏离 \(\pi_{old}\) 过远</td> </tr> <tr> <td>Min on clip</td> <td>clip 的副作用</td> <td>错误方向保留纠错梯度</td> </tr> <tr> <td>KL on \(\pi_{ref}\)</td> <td>累积漂移</td> <td>reward hacking</td> </tr> <tr> <td>Value baseline</td> <td>advantage 估计</td> <td>降方差、实现 credit assignment</td> </tr> </tbody> </table> <p>PPO 的工程稳定性是这五件事叠加的结果。去掉其中任何一个，某种 setting 下训练会变脆。</p> <hr/> <h2 id="reward--credit-assignment">4. 稀疏 Reward 与 Credit Assignment</h2> <h3 id="section-4">4.1 问题定义</h3> <p>LLM RL 中 reward 通常只在 episode 末尾有值。Search-R1 典型 episode：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;think&gt;...&lt;/think&gt;&lt;search&gt;...&lt;/search&gt;&lt;information&gt;...&lt;/information&gt;
&lt;think&gt;...&lt;/think&gt;&lt;search&gt;...&lt;/search&gt;&lt;information&gt;...&lt;/information&gt;
&lt;think&gt;...&lt;/think&gt;&lt;answer&gt;McComb, Mississippi&lt;/answer&gt;   ← reward=1 只在这里
</code></pre></div> </div> <p>500 个 token，只有最后一位有非零 reward。直接把 terminal reward 当作所有 token 的 \(A_t\) 会有两个问题：所有 token 等权（无法区分关键 token），梯度方差大（每条 episode 只贡献一个标量 signal）。</p> <p>这就是 <strong>credit assignment</strong> 问题。</p> <h3 id="value-function-1">4.2 Value Function</h3> <p>标准解法是引入 value function \(V_\phi\)：</p> \[V_\phi(s_t) = \mathbb{E}\Big[\sum_{k=0}^{T-t} \gamma^k r_{t+k}\Big]\] <p>advantage 定义为：</p> \[A_t = R_t - V_\phi(s_t)\] <ul> <li>\(A_t &gt; 0\)：实际回报超预期 → 好选择</li> <li>\(A_t &lt; 0\)：低于预期 → 差选择</li> <li>\(A_t \approx 0\)：不关键</li> </ul> <p>value function 把稀疏的 terminal reward <strong>摊平</strong> 到每个 token 位置上。</p> <h3 id="monte-carlo-vs-td-1">4.3 Monte Carlo vs TD</h3> <p>\(R_t\) 的两种估计方式：</p> <table> <thead> <tr> <th>方法</th> <th>公式</th> <th>性质</th> </tr> </thead> <tbody> <tr> <td>Monte Carlo</td> <td>\(R_t^{MC} = \sum_{k=0}^{T-t} \gamma^k r_{t+k}\)</td> <td>无偏、方差高</td> </tr> <tr> <td>TD</td> <td>\(R_t^{TD} = r_t + \gamma V_\phi(s_{t+1})\)</td> <td>有偏、方差低</td> </tr> </tbody> </table> <h3 id="gae">4.4 GAE：两者的指数加权混合</h3> <p>GAE<sup id="fnref:gae:1"><a href="#fn:gae" class="footnote" rel="footnote" role="doc-noteref">5</a></sup> 用 \(\lambda \in [0, 1]\) 插值：</p> \[A_t^{GAE} = \sum_{l=0}^{T-t} (\gamma \lambda)^l \delta_{t+l}, \quad \delta_t = r_t + \gamma V_\phi(s_{t+1}) - V_\phi(s_t)\] <p>递推形式：</p> \[A_t^{GAE} = \delta_t + \gamma \lambda A_{t+1}^{GAE}\] <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">compute_gae</span><span class="p">(</span><span class="n">rewards</span><span class="p">,</span> <span class="n">values</span><span class="p">,</span> <span class="n">gamma</span><span class="o">=</span><span class="mf">1.0</span><span class="p">,</span> <span class="n">lam</span><span class="o">=</span><span class="mf">1.0</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    rewards: [T] per-step rewards (mostly zero in LLM setting)
    values:  [T+1] value estimates; values[T] is terminal bootstrap
    </span><span class="sh">"""</span>
    <span class="n">T</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">rewards</span><span class="p">)</span>
    <span class="n">advantages</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="n">T</span><span class="p">)</span>
    <span class="n">gae</span> <span class="o">=</span> <span class="mf">0.0</span>

    <span class="c1"># iterate in reverse: GAE is an exponentially weighted sum of future deltas
</span>    <span class="k">for</span> <span class="n">t</span> <span class="ow">in</span> <span class="nf">reversed</span><span class="p">(</span><span class="nf">range</span><span class="p">(</span><span class="n">T</span><span class="p">)):</span>
        <span class="n">delta</span> <span class="o">=</span> <span class="n">rewards</span><span class="p">[</span><span class="n">t</span><span class="p">]</span> <span class="o">+</span> <span class="n">gamma</span> <span class="o">*</span> <span class="n">values</span><span class="p">[</span><span class="n">t</span> <span class="o">+</span> <span class="mi">1</span><span class="p">]</span> <span class="o">-</span> <span class="n">values</span><span class="p">[</span><span class="n">t</span><span class="p">]</span>
        <span class="n">gae</span> <span class="o">=</span> <span class="n">delta</span> <span class="o">+</span> <span class="n">gamma</span> <span class="o">*</span> <span class="n">lam</span> <span class="o">*</span> <span class="n">gae</span>
        <span class="n">advantages</span><span class="p">[</span><span class="n">t</span><span class="p">]</span> <span class="o">=</span> <span class="n">gae</span>

    <span class="n">returns</span> <span class="o">=</span> <span class="n">advantages</span> <span class="o">+</span> <span class="n">values</span><span class="p">[:</span><span class="n">T</span><span class="p">]</span>
    <span class="k">return</span> <span class="n">advantages</span><span class="p">,</span> <span class="n">returns</span>
</code></pre></div> </div> <p>Search-R1 用 \(\lambda = \gamma = 1\)。此时 GAE 递推退化成 \(A_t = \sum_l \delta_{t+l}\)，telescope 后等于 \(R_t - V(s_t)\)——就是 MC advantage。</p> <h3 id="reward--llm--work">4.5 为什么稀疏 reward 在 LLM 上能 work</h3> <ol> <li><strong>预训练提供 strong prior</strong>：RL 不是从零学，只是 fine-grained 调整</li> <li><strong>Token 的因果结构</strong>：早期好选择和最终成功高度相关</li> <li><strong>Value function 的平滑</strong>：把 terminal reward 摊到每个位置</li> <li><strong>Batch size 的统计力量</strong>：Search-R1 batch = 512，梯度相对稳定</li> </ol> <hr/> <h2 id="reward-reward-model-vs-verifier">5. Reward 来源：Reward Model vs Verifier</h2> <h3 id="section-5">5.1 两个正交维度</h3> <p>LLM RL 的设计空间有两个正交轴：</p> <p><strong>轴一：Reward 从哪来</strong>（optimize what）</p> <ul> <li>Neural Reward Model（RM）</li> <li>Rule-based Verifier</li> <li>Generative Verifier</li> </ul> <p><strong>轴二：Credit Assignment 怎么做</strong>（propagate how）</p> <ul> <li>Critic + GAE（PPO）</li> <li>Group Normalization（GRPO）</li> <li>Offline data（DPO<sup id="fnref:dpo:2"><a href="#fn:dpo" class="footnote" rel="footnote" role="doc-noteref">6</a></sup>）</li> </ul> <table> <thead> <tr> <th>Reward 来源</th> <th>Credit Assignment</th> <th>代表工作</th> </tr> </thead> <tbody> <tr> <td>Neural RM</td> <td>Critic + GAE</td> <td>经典 RLHF（InstructGPT）</td> </tr> <tr> <td>Rule Verifier</td> <td>Critic + GAE</td> <td>Search-R1</td> </tr> <tr> <td>Rule Verifier</td> <td>Group Norm</td> <td>DeepSeek-R1<sup id="fnref:r1:1"><a href="#fn:r1" class="footnote" rel="footnote" role="doc-noteref">7</a></sup></td> </tr> </tbody> </table> <h3 id="reward-model-vs-critic">5.2 Reward Model vs Critic：常被搞混的两个概念</h3> <table> <thead> <tr> <th>维度</th> <th>Reward Model</th> <th>Critic (Value Model)</th> </tr> </thead> <tbody> <tr> <td>评什么</td> <td>整条 response 的 <strong>绝对质量</strong></td> <td>当前状态的 <strong>未来期望回报</strong></td> </tr> <tr> <td>输入</td> <td>(prompt, response)</td> <td>当前 state \(s_t\)</td> </tr> <tr> <td>输出</td> <td>单个 scalar \(r\)</td> <td>每个位置一个 \(V_\phi(s_t)\)</td> </tr> <tr> <td>训练信号</td> <td>人类偏好数据（或规则）</td> <td>从 reward bootstrap</td> </tr> <tr> <td>训练时机</td> <td>RL <strong>之前</strong> 训好，RL 中冻结</td> <td>和 policy <strong>一起</strong> 训</td> </tr> </tbody> </table> <p>常见误解：”有 RM 就不需要 Critic”。事实上两者职责完全不同：<strong>RM 把 (prompt, response) 压成一个稀疏 scalar，Critic 把这个 scalar 摊平到每个 token</strong>。一个管”整体是好是坏”，一个管”好在哪里坏在哪里”。</p> <h3 id="reasoning--rule-based-verifier">5.3 为什么 Reasoning 爱用 Rule-based Verifier</h3> <p>四个原因：</p> <p><strong>1. 抗 reward hacking。</strong> Neural RM 的经典问题：policy 学会 “骗过” RM。Rule-based verifier 是确定性的，无法被 hack。</p> <p><strong>2. 可扩展。</strong> 训 RM 要大量标注。写 verifier 函数只要几行：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">math_verifier</span><span class="p">(</span><span class="n">pred</span><span class="p">,</span> <span class="n">gt</span><span class="p">):</span>
    <span class="k">return</span> <span class="n">sympy</span><span class="p">.</span><span class="nf">simplify</span><span class="p">(</span><span class="nf">parse</span><span class="p">(</span><span class="n">pred</span><span class="p">)</span> <span class="o">-</span> <span class="nf">parse</span><span class="p">(</span><span class="n">gt</span><span class="p">))</span> <span class="o">==</span> <span class="mi">0</span>

<span class="k">def</span> <span class="nf">code_verifier</span><span class="p">(</span><span class="n">pred</span><span class="p">,</span> <span class="n">tests</span><span class="p">):</span>
    <span class="k">return</span> <span class="nf">all</span><span class="p">(</span><span class="nf">run_test</span><span class="p">(</span><span class="n">pred</span><span class="p">,</span> <span class="n">t</span><span class="p">)</span> <span class="k">for</span> <span class="n">t</span> <span class="ow">in</span> <span class="n">tests</span><span class="p">)</span>
</code></pre></div> </div> <p><strong>3. 信号更直接。</strong> RM 是 proxy indicator，verifier 是真实目标。训练目标越直接，RL 越好收敛。</p> <p><strong>4. 匹配 outcome-only reward 的新范式。</strong> DeepSeek-R1 证明了只用 outcome reward 就能涌现复杂 reasoning。Rule-based verifier 天然适合。</p> <h3 id="verifier-">5.4 Verifier 的局限</h3> <p>不是所有任务都有好的 verifier：</p> <table> <thead> <tr> <th>任务</th> <th>Verifier 容易写吗</th> </tr> </thead> <tbody> <tr> <td>数学题</td> <td>✅ 容易</td> </tr> <tr> <td>代码</td> <td>✅ 容易</td> </tr> <tr> <td>短答 QA</td> <td>✅ 中等</td> </tr> <tr> <td>长文写作</td> <td>❌ 难</td> </tr> <tr> <td>对话质量</td> <td>❌ 难</td> </tr> </tbody> </table> <p>这解释了为什么 reasoning LLM 的突破集中在 math / code / QA——它们有天然 verifier。</p> <hr/> <h2 id="grpo-critic--trade-off">6. GRPO：去掉 Critic 的 Trade-off</h2> <p>GRPO<sup id="fnref:grpo:1"><a href="#fn:grpo" class="footnote" rel="footnote" role="doc-noteref">8</a></sup> 由 DeepSeek 提出，是 DeepSeek-R1 用的 RL 算法。<strong>核心创新：去掉 Critic</strong>。</p> <h3 id="section-6">6.1 动机</h3> <p>LLM 场景下 Critic 不便宜：通常和 policy 同等规模，需要独立的 forward / backward，冷启动时 value 估计也不准。如果 reward 本来就是 outcome-level（一条 response 一个分），token-level Critic 的细粒度信号未必值这个计算开销。</p> <h3 id="group-normalization-1">6.2 Group Normalization</h3> <p>对同一个 prompt 采样 \(G\) 个 response，用 <strong>组内归一化</strong> 得 advantage：</p> \[\hat{A}_i = \frac{r_i - \text{mean}(r_1, ..., r_G)}{\text{std}(r_1, ..., r_G)}\] <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">compute_grpo_advantages</span><span class="p">(</span><span class="n">rewards</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    rewards: [G] rewards for G responses to the same prompt
    </span><span class="sh">"""</span>
    <span class="n">mean</span> <span class="o">=</span> <span class="n">rewards</span><span class="p">.</span><span class="nf">mean</span><span class="p">()</span>
    <span class="n">std</span> <span class="o">=</span> <span class="n">rewards</span><span class="p">.</span><span class="nf">std</span><span class="p">()</span> <span class="o">+</span> <span class="mf">1e-8</span>
    <span class="nf">return </span><span class="p">(</span><span class="n">rewards</span> <span class="o">-</span> <span class="n">mean</span><span class="p">)</span> <span class="o">/</span> <span class="n">std</span>
</code></pre></div> </div> <p>直觉：同 prompt 下的 \(G\) 个 response，好的相对于组内均值就是好。不需要 Critic 给绝对评分，组内相对比较就够了。</p> <h3 id="trade-off-1">6.3 Trade-off</h3> <p><strong>好处</strong>：</p> <ul> <li>✅ 省掉 Critic，计算量约减半</li> <li>✅ 训练流程更简单</li> <li>✅ Reasoning 任务 reward 本就 outcome-level，不需要 token-level advantage</li> </ul> <p><strong>代价</strong>：</p> <ul> <li>❌ Token-level 信息丢失：无法区分”同一 response 中哪些 token 关键”</li> <li>❌ 依赖 \(G\) 够大：\(G=1\) 时 GRPO 无法工作</li> <li>❌ Reward collapse 风险：训练后期 \(G\) 个 responses 全对或全错时 std 趋零</li> </ul> <p>Search-R1 的 PPO vs GRPO 对比（Qwen2.5-7B）：</p> <table> <thead> <tr> <th>方法</th> <th>收敛速度</th> <th>稳定性</th> <th>最终 avg EM</th> </tr> </thead> <tbody> <tr> <td>PPO</td> <td>慢（Critic 需 warmup）</td> <td>稳定</td> <td>0.431</td> </tr> <tr> <td>GRPO</td> <td>快</td> <td>后期易崩</td> <td>0.350–0.396</td> </tr> </tbody> </table> <h3 id="section-7">6.4 怎么选</h3> <ul> <li><strong>Reasoning + 短训练</strong>：GRPO。reward 本就 outcome-level</li> <li><strong>长训练 + 追求最终性能</strong>：PPO。Critic 开销值得</li> <li><strong>计算资源紧张</strong>：GRPO。省一个模型</li> </ul> <hr/> <h2 id="dpo-reward-model--rl-">7. DPO：绕开 reward model 和 RL 的捷径</h2> <p>DPO<sup id="fnref:dpo:3"><a href="#fn:dpo" class="footnote" rel="footnote" role="doc-noteref">6</a></sup>（Direct Preference Optimization）走了完全不同的路：<strong>不训 reward model，也不做 RL rollout</strong>。直接把偏好对 \((x, y_w, y_l)\)（\(y_w\) 是 chosen、\(y_l\) 是 rejected）当监督学习数据。</p> <h3 id="section-8">7.1 动机</h3> <p>经典 RLHF 训练链路很长：preference data → 训 RM → PPO rollout → policy update。每一步都有各自的失败模式：RM 被 hack、PPO 不稳、rollout 贵。DPO 的问题是：能不能把这条链短路？</p> <h3 id="rlhf-">7.2 核心推导：RLHF 的最优解有闭式</h3> <p>RLHF 的目标是：</p> \[\max_\pi \mathbb{E}_{x, y \sim \pi}[r(x, y)] - \beta D_{KL}[\pi \| \pi_{ref}]\] <p>这个问题有闭式最优解：</p> \[\pi^*(y \mid x) = \frac{1}{Z(x)} \pi_{ref}(y \mid x) \exp\Big(\frac{1}{\beta} r(x, y)\Big)\] <p>其中 \(Z(x) = \sum_y \pi_{ref}(y \mid x) \exp(r(x, y)/\beta)\) 是归一化常数。反解出 reward：</p> \[r(x, y) = \beta \log \frac{\pi^*(y \mid x)}{\pi_{ref}(y \mid x)} + \beta \log Z(x)\] <p><strong>关键观察</strong>：reward 可以写成 “log 概率比” 的形式。这是 DPO 整个推导的支点。</p> <h3 id="dpo-loss">7.3 从偏好概率到 DPO loss</h3> <p>代入 Bradley-Terry 偏好模型：</p> \[P(y_w \succ y_l \mid x) = \sigma\big(r(x, y_w) - r(x, y_l)\big)\] <p>两个 reward 相减，难算的 \(\log Z(x)\) 项消掉。剩下只包含 policy 和 reference 的 log 概率比：</p> \[\mathcal{L}^{DPO} = -\mathbb{E}_{(x, y_w, y_l)}\Big[\log \sigma\Big(\beta \log \frac{\pi_\theta(y_w \mid x)}{\pi_{ref}(y_w \mid x)} - \beta \log \frac{\pi_\theta(y_l \mid x)}{\pi_{ref}(y_l \mid x)}\Big)\Big]\] <p>这就是一个普通的二分类 loss（logistic regression on preference pairs），不需要 reward model、不需要 rollout、不需要 PPO。</p> <h3 id="section-9">7.4 代码</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">compute_dpo_loss</span><span class="p">(</span>
    <span class="n">policy_chosen_logps</span><span class="p">,</span>    <span class="c1"># [B] log prob of y_w under pi_theta (sequence-level)
</span>    <span class="n">policy_rejected_logps</span><span class="p">,</span>  <span class="c1"># [B] log prob of y_l under pi_theta
</span>    <span class="n">ref_chosen_logps</span><span class="p">,</span>       <span class="c1"># [B] log prob of y_w under pi_ref
</span>    <span class="n">ref_rejected_logps</span><span class="p">,</span>     <span class="c1"># [B] log prob of y_l under pi_ref
</span>    <span class="n">beta</span><span class="o">=</span><span class="mf">0.1</span><span class="p">,</span>
<span class="p">):</span>
    <span class="c1"># log ratio of policy to reference for both chosen and rejected
</span>    <span class="n">chosen_ratio</span> <span class="o">=</span> <span class="n">policy_chosen_logps</span> <span class="o">-</span> <span class="n">ref_chosen_logps</span>
    <span class="n">rejected_ratio</span> <span class="o">=</span> <span class="n">policy_rejected_logps</span> <span class="o">-</span> <span class="n">ref_rejected_logps</span>

    <span class="c1"># how much more the policy prefers y_w over y_l, scaled by beta
</span>    <span class="n">logits</span> <span class="o">=</span> <span class="n">beta</span> <span class="o">*</span> <span class="p">(</span><span class="n">chosen_ratio</span> <span class="o">-</span> <span class="n">rejected_ratio</span><span class="p">)</span>

    <span class="c1"># binary classification: push chosen above rejected
</span>    <span class="k">return</span> <span class="o">-</span><span class="n">F</span><span class="p">.</span><span class="nf">logsigmoid</span><span class="p">(</span><span class="n">logits</span><span class="p">).</span><span class="nf">mean</span><span class="p">()</span>
</code></pre></div> </div> <p><strong>实现细节</strong>：<code class="language-plaintext highlighter-rouge">logps</code> 是整个 response 的 log 概率之和（sequence-level），不是单 token。这和 PPO token-level advantage 是本质区别。</p> <h3 id="dpo-vs-ppotrade-off">7.5 DPO vs PPO：Trade-off</h3> <table> <thead> <tr> <th>维度</th> <th>PPO</th> <th>DPO</th> </tr> </thead> <tbody> <tr> <td>数据来源</td> <td>online rollout</td> <td>offline preference pairs</td> </tr> <tr> <td>Reward model</td> <td>需要</td> <td>不需要</td> </tr> <tr> <td>训练稳定性</td> <td>需调 clip、KL、GAE 等超参</td> <td>几乎像 SFT 一样好训</td> </tr> <tr> <td>数据效率</td> <td>rollout 昂贵</td> <td>偏好数据一次性收集</td> </tr> <tr> <td>分布外泛化</td> <td>好（rollout 中能探索）</td> <td>依赖偏好数据覆盖</td> </tr> <tr> <td>粒度</td> <td>token-level</td> <td>sequence-level</td> </tr> <tr> <td>适用场景</td> <td>有 verifier 或在线交互</td> <td>有静态偏好数据</td> </tr> </tbody> </table> <p><strong>核心洞察</strong>：DPO 把 RL 问题化成了监督学习。代价是失去了 “online exploration”——policy 只能从静态偏好对学，无法从新的 rollout 自我改进。这在分布外泛化和 reasoning 任务上是实打实的 disadvantage。</p> <h3 id="dpo">7.6 什么时候选 DPO</h3> <ul> <li><strong>偏好数据已有、rollout 贵</strong>：经典 RLHF 的工程替代品</li> <li><strong>对话风格、写作偏好等 “难写 verifier” 的任务</strong></li> <li><strong>工程简单</strong>：不想维护 RM + PPO 基础设施</li> </ul> <p>不适合：</p> <ul> <li><strong>有 rule verifier 的 reasoning 任务</strong>：PPO / GRPO 的在线探索更强</li> <li><strong>需要多轮环境交互的 Agentic 任务</strong>：DPO 没有 rollout 的概念，无法处理</li> </ul> <p>DPO 和 PPO / GRPO 互为补充，不是替代。理解清楚这点，后面看 Search-R1 时就不会问 “为什么不用 DPO”——因为 Search-R1 本质需要 rollout 和环境交互。</p> <hr/> <h2 id="search-r1agentic-rl-">8. Search-R1：Agentic RL 的完整实例</h2> <p>前面所有组件在这一节组装起来。</p> <h3 id="section-10">7.1 任务形式化</h3> <p>Search-R1 的 rollout 是 LLM 和搜索引擎的交替交互：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;think&gt;reasoning about what to search&lt;/think&gt;
&lt;search&gt;query string&lt;/search&gt;
&lt;information&gt;[retrieved passages injected by environment]&lt;/information&gt;
&lt;think&gt;reasoning about retrieved info&lt;/think&gt;
&lt;search&gt;follow-up query&lt;/search&gt;
&lt;information&gt;[more retrieved passages]&lt;/information&gt;
...
&lt;answer&gt;final answer&lt;/answer&gt;
</code></pre></div> </div> <p>Template 只约束 <strong>结构</strong>，不约束 <strong>策略</strong>——让 RL 自己发现好的搜索 policy。</p> <h3 id="rollout-">7.2 Rollout 过程</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">rollout_with_search</span><span class="p">(</span><span class="n">model</span><span class="p">,</span> <span class="n">tokenizer</span><span class="p">,</span> <span class="n">search_engine</span><span class="p">,</span> <span class="n">prompt</span><span class="p">,</span> <span class="n">max_turns</span><span class="o">=</span><span class="mi">4</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    Search-R1 rollout: interleave LLM generation with search engine calls.
    </span><span class="sh">"""</span>
    <span class="n">tokens</span> <span class="o">=</span> <span class="n">tokenizer</span><span class="p">.</span><span class="nf">encode</span><span class="p">(</span><span class="n">prompt</span><span class="p">)</span>
    <span class="n">action_mask</span> <span class="o">=</span> <span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">*</span> <span class="nf">len</span><span class="p">(</span><span class="n">tokens</span><span class="p">)</span>  <span class="c1"># prompt tokens are not LLM-generated
</span>
    <span class="k">for</span> <span class="n">turn</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">max_turns</span><span class="p">):</span>
        <span class="n">new_tokens</span><span class="p">,</span> <span class="n">stop</span> <span class="o">=</span> <span class="n">model</span><span class="p">.</span><span class="nf">generate</span><span class="p">(</span>
            <span class="n">tokens</span><span class="p">,</span>
            <span class="n">stop_sequences</span><span class="o">=</span><span class="p">[</span><span class="sh">"</span><span class="s">&lt;/search&gt;</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">&lt;/answer&gt;</span><span class="sh">"</span><span class="p">],</span>
            <span class="n">max_new_tokens</span><span class="o">=</span><span class="mi">500</span><span class="p">,</span>
        <span class="p">)</span>
        <span class="n">tokens</span><span class="p">.</span><span class="nf">extend</span><span class="p">(</span><span class="n">new_tokens</span><span class="p">)</span>
        <span class="n">action_mask</span><span class="p">.</span><span class="nf">extend</span><span class="p">([</span><span class="mi">1</span><span class="p">]</span> <span class="o">*</span> <span class="nf">len</span><span class="p">(</span><span class="n">new_tokens</span><span class="p">))</span>       <span class="c1"># LLM-generated
</span>
        <span class="k">if</span> <span class="n">stop</span> <span class="o">==</span> <span class="sh">"</span><span class="s">&lt;/answer&gt;</span><span class="sh">"</span><span class="p">:</span>
            <span class="k">break</span>
        <span class="k">elif</span> <span class="n">stop</span> <span class="o">==</span> <span class="sh">"</span><span class="s">&lt;/search&gt;</span><span class="sh">"</span><span class="p">:</span>
            <span class="n">query</span> <span class="o">=</span> <span class="nf">extract_search_query</span><span class="p">(</span><span class="n">tokens</span><span class="p">)</span>
            <span class="n">retrieved</span> <span class="o">=</span> <span class="n">search_engine</span><span class="p">.</span><span class="nf">retrieve</span><span class="p">(</span><span class="n">query</span><span class="p">,</span> <span class="n">top_k</span><span class="o">=</span><span class="mi">3</span><span class="p">)</span>
            <span class="n">retrieved_text</span> <span class="o">=</span> <span class="sa">f</span><span class="sh">"</span><span class="s">&lt;information&gt;</span><span class="si">{</span><span class="nf">format_passages</span><span class="p">(</span><span class="n">retrieved</span><span class="p">)</span><span class="si">}</span><span class="s">&lt;/information&gt;</span><span class="sh">"</span>
            <span class="n">retrieved_tokens</span> <span class="o">=</span> <span class="n">tokenizer</span><span class="p">.</span><span class="nf">encode</span><span class="p">(</span><span class="n">retrieved_text</span><span class="p">)</span>
            <span class="n">tokens</span><span class="p">.</span><span class="nf">extend</span><span class="p">(</span><span class="n">retrieved_tokens</span><span class="p">)</span>
            <span class="n">action_mask</span><span class="p">.</span><span class="nf">extend</span><span class="p">([</span><span class="mi">0</span><span class="p">]</span> <span class="o">*</span> <span class="nf">len</span><span class="p">(</span><span class="n">retrieved_tokens</span><span class="p">))</span>  <span class="c1"># NOT LLM-generated
</span>
    <span class="k">return</span> <span class="n">tokens</span><span class="p">,</span> <span class="n">action_mask</span>
</code></pre></div> </div> <h3 id="retrieved-token-masking">7.3 核心技术贡献：Retrieved Token Masking</h3> <p><strong>问题</strong>：rollout sequence 里混入了非 LLM 生成的 token（retrieved passages）。如果不处理，policy gradient 会对这些 token 也算梯度——这是错的：它们不是 policy 的 action。</p> <p>更糟的是，不 mask 会让 loss 把 “搜索引擎返回的文本风格” 当成 policy 要学的东西，policy 学着模仿 retrieval 文风，而不是学 “如何搜索”。</p> <p><strong>解法</strong>：loss 只对 LLM 生成的 token 计算：</p> \[\mathcal{L}^{Search\text{-}R1} = \frac{\sum_t I(y_t) \cdot \mathcal{L}^{PPO}_t}{\sum_t I(y_t)}, \quad I(y_t) = \begin{cases} 1 &amp; y_t \text{ is LLM-generated} \\ 0 &amp; y_t \text{ is retrieved} \end{cases}\] <p>实现上就是把 <code class="language-plaintext highlighter-rouge">action_mask</code> 传进 PPO loss：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">search_r1_loss</span><span class="p">(</span><span class="n">logits</span><span class="p">,</span> <span class="n">old_logprobs</span><span class="p">,</span> <span class="n">ref_logprobs</span><span class="p">,</span> <span class="n">actions</span><span class="p">,</span>
                   <span class="n">advantages</span><span class="p">,</span> <span class="n">action_mask</span><span class="p">,</span> <span class="n">clip_eps</span><span class="o">=</span><span class="mf">0.2</span><span class="p">,</span> <span class="n">kl_coef</span><span class="o">=</span><span class="mf">0.001</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    Identical to standard PPO loss, except action_mask now also
    excludes retrieved tokens in addition to padding.
    </span><span class="sh">"""</span>
    <span class="n">log_probs</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">log_softmax</span><span class="p">(</span><span class="n">logits</span><span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>
    <span class="n">new_logprobs</span> <span class="o">=</span> <span class="n">log_probs</span><span class="p">.</span><span class="nf">gather</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="n">actions</span><span class="p">.</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)).</span><span class="nf">squeeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span>

    <span class="n">ratio</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">new_logprobs</span> <span class="o">-</span> <span class="n">old_logprobs</span><span class="p">)</span>
    <span class="n">surr1</span> <span class="o">=</span> <span class="n">ratio</span> <span class="o">*</span> <span class="n">advantages</span>
    <span class="n">surr2</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">clamp</span><span class="p">(</span><span class="n">ratio</span><span class="p">,</span> <span class="mi">1</span> <span class="o">-</span> <span class="n">clip_eps</span><span class="p">,</span> <span class="mi">1</span> <span class="o">+</span> <span class="n">clip_eps</span><span class="p">)</span> <span class="o">*</span> <span class="n">advantages</span>
    <span class="n">policy_loss</span> <span class="o">=</span> <span class="o">-</span><span class="n">torch</span><span class="p">.</span><span class="nf">min</span><span class="p">(</span><span class="n">surr1</span><span class="p">,</span> <span class="n">surr2</span><span class="p">)</span>

    <span class="n">log_ratio_ref</span> <span class="o">=</span> <span class="n">ref_logprobs</span> <span class="o">-</span> <span class="n">new_logprobs</span>
    <span class="n">kl</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">log_ratio_ref</span><span class="p">)</span> <span class="o">-</span> <span class="n">log_ratio_ref</span> <span class="o">-</span> <span class="mi">1</span>

    <span class="c1"># action_mask = 0 for padding OR retrieved tokens
</span>    <span class="n">total</span> <span class="o">=</span> <span class="p">(</span><span class="n">policy_loss</span> <span class="o">+</span> <span class="n">kl_coef</span> <span class="o">*</span> <span class="n">kl</span><span class="p">)</span> <span class="o">*</span> <span class="n">action_mask</span>
    <span class="k">return</span> <span class="n">total</span><span class="p">.</span><span class="nf">sum</span><span class="p">()</span> <span class="o">/</span> <span class="n">action_mask</span><span class="p">.</span><span class="nf">sum</span><span class="p">()</span>
</code></pre></div> </div> <p><strong>效果</strong>（Qwen2.5-7B, avg EM over 7 QA datasets）：</p> <table> <thead> <tr> <th>Setting</th> <th>Avg EM</th> </tr> </thead> <tbody> <tr> <td>不 mask retrieved tokens</td> <td>0.343</td> </tr> <tr> <td>Mask retrieved tokens</td> <td><strong>0.431</strong></td> </tr> </tbody> </table> <p>单这一个改动带来约 25% 相对提升。<strong>正确定义 action space 对 RL 训练的影响，可能比算法本身的改动更大</strong>。</p> <p>更抽象地说：<strong>RL 应该只优化 agent 能控制的变量</strong>。Retrieved content 是环境的 response，agent 不控制它，不该被 policy gradient 塑形。这是 RL 的基础原则，在 “纯 LLM generation” setting 下容易被忽略；Agentic RL 让它重新变得重要。</p> <h3 id="section-11">7.4 实验观察</h3> <p><strong>主结果</strong>（avg EM over 7 QA datasets, Qwen2.5-7B）：</p> <table> <thead> <tr> <th>方法</th> <th>Avg EM</th> </tr> </thead> <tbody> <tr> <td>Direct Inference</td> <td>0.181</td> </tr> <tr> <td>RAG</td> <td>0.304</td> </tr> <tr> <td>R1 (RL without search)</td> <td>0.276</td> </tr> <tr> <td>Rejection Sampling + SFT</td> <td>0.348</td> </tr> <tr> <td><strong>Search-R1 (PPO)</strong></td> <td><strong>0.431</strong></td> </tr> </tbody> </table> <p><strong>涌现行为</strong>：训出的模型展现了论文没有显式教授的 pattern：</p> <ol> <li><strong>Self-verification</strong>：拿到答案后再发一次 query 确认</li> <li><strong>Query refinement</strong>：首次搜索失败后改写 query 重试</li> <li><strong>Problem decomposition</strong>：复杂问题自动拆成多个 sub-query</li> </ol> <p>论文 Table 9 的案例：问 “Curious 香水的歌手出生在哪个城市和州”。Search-R1 会：</p> <ol> <li><code class="language-plaintext highlighter-rouge">&lt;search&gt;Curious fragrance information&lt;/search&gt;</code> → 发现是 Britney Spears 的</li> <li><code class="language-plaintext highlighter-rouge">&lt;search&gt;Britney Spears birthplace&lt;/search&gt;</code> → McComb, Mississippi</li> <li><code class="language-plaintext highlighter-rouge">&lt;search&gt;McComb, Mississippi location&lt;/search&gt;</code> → 验证确认</li> <li><code class="language-plaintext highlighter-rouge">&lt;answer&gt;McComb, Mississippi&lt;/answer&gt;</code></li> </ol> <p>对比没有搜索能力的 R1：直接猜成 “Houston”（错把 Curious 归给 Beyoncé）。</p> <hr/> <h2 id="section-12">9. 设计空间的全景总结</h2> <h3 id="section-13">8.1 两个正交维度</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>                    │ Critic + GAE      │ Group Norm         │
                    │ (token-level adv) │ (response-level)   │
────────────────────┼───────────────────┼────────────────────┤
  Neural RM         │ 经典 RLHF          │ 少见                │
────────────────────┼───────────────────┼────────────────────┤
  Rule Verifier     │ Search-R1          │ DeepSeek-R1        │
────────────────────┼───────────────────┼────────────────────┤
  Gen Verifier      │ 新兴方向           │ 新兴方向            │
</code></pre></div> </div> <p><strong>横轴决定成本和稳定性</strong>。Critic + GAE 多一个模型但更稳；Group Norm 省一个模型但粒度更粗。</p> <p><strong>纵轴决定适用范围</strong>。Neural RM 覆盖广但易 hack；Rule Verifier 抗 hack 但覆盖窄；Generative Verifier 介于两者之间。</p> <h3 id="agentic-rl-">8.2 Agentic RL 的本质</h3> <p>Agentic RL 不是新算法，是 <strong>环境的扩展</strong>。传统 RL for LLM 的环境只有 prompt 和 terminal reward；Agentic RL 的环境还包括工具、中间反馈、状态变化。</p> <p>算法层仍是 PPO 或 GRPO。关键变化在 rollout——不再是一次性 generation，而是 policy 与环境的交替交互。这带来两个新挑战：</p> <ol> <li><strong>Rollout sequence 里混入非 policy token</strong> → 需要 retrieved token masking</li> <li><strong>Rollout 成本更高</strong> → 对 sample efficiency 要求更高</li> </ol> <h3 id="section-14">8.3 当前的局限与开放问题</h3> <ol> <li><strong>开放式任务的 reward 设计</strong>：Rule verifier 只适用有明确对错的任务。写作、对话等仍需 Neural RM 或人类反馈。</li> <li><strong>长 horizon reasoning</strong>：推理链达几万 token 时，Critic 和 Group Norm 都面临挑战。</li> <li><strong>Multi-tool 协同</strong>：reward 如何跨工具分配、rollout 如何管理复杂工具调用图，都还 open。</li> <li><strong>Outcome reward 下涌现行为的理论解释</strong>：为什么 self-verification 等行为会自发出现？在什么条件下？缺乏系统回答。</li> </ol> <hr/> <h2 id="section-15">10. 结语与参考资料</h2> <h3 id="section-16">快速索引</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Cross Entropy                  -log π(y_t)              单 token 基础 loss
SFT Loss                       Σ CE                     无权重
REINFORCE                      Σ CE · A_t               advantage 加权
PPO                            + clip + min + KL        稳定化的 policy gradient
GRPO                           + group norm             去掉 Critic
DPO                            logistic on pref pairs   跳过 RL 的离线路线
Search-R1                      + retrieved token mask   Agentic RL 实例

Policy (π_θ)                   被训练的 LLM
Reference (π_ref)              训练起点的 LLM，冻结
Old (π_old)                    本轮 rollout 时的快照
Critic (V_φ)                   估计 future return
Reward Model                   scalar on (prompt, response)
Verifier                       广义概念：判断正确性的函数/模型
</code></pre></div> </div> </div> <h3 id="references">References</h3> <div class="footnotes" role="doc-endnotes"> <ol> <li id="fn:sr1"> <p>Jin et al. (2025). Search-R1: Training LLMs to Reason and Leverage Search Engines with Reinforcement Learning. <a href="https://arxiv.org/abs/2503.09516">arXiv:2503.09516</a> <a href="#fnref:sr1" class="reversefootnote" role="doc-backlink">&#8617;</a> <a href="#fnref:sr1:1" class="reversefootnote" role="doc-backlink">&#8617;<sup>2</sup></a></p> </li> <li id="fn:ppo"> <p>Schulman et al. (2017). Proximal Policy Optimization Algorithms. <a href="https://arxiv.org/abs/1707.06347">arXiv:1707.06347</a> <a href="#fnref:ppo" class="reversefootnote" role="doc-backlink">&#8617;</a> <a href="#fnref:ppo:1" class="reversefootnote" role="doc-backlink">&#8617;<sup>2</sup></a></p> </li> <li id="fn:instructgpt"> <p>Ouyang et al. (2022). Training language models to follow instructions with human feedback. <a href="https://arxiv.org/abs/2203.02155">arXiv:2203.02155</a> <a href="#fnref:instructgpt" class="reversefootnote" role="doc-backlink">&#8617;</a> <a href="#fnref:instructgpt:1" class="reversefootnote" role="doc-backlink">&#8617;<sup>2</sup></a></p> </li> <li id="fn:k3"> <p>Schulman, J. (2020). Approximating KL Divergence. <a href="http://joschu.net/blog/kl-approx.html">http://joschu.net/blog/kl-approx.html</a> <a href="#fnref:k3" class="reversefootnote" role="doc-backlink">&#8617;</a> <a href="#fnref:k3:1" class="reversefootnote" role="doc-backlink">&#8617;<sup>2</sup></a></p> </li> <li id="fn:gae"> <p>Schulman et al. (2015). High-Dimensional Continuous Control Using Generalized Advantage Estimation. <a href="https://arxiv.org/abs/1506.02438">arXiv:1506.02438</a> <a href="#fnref:gae" class="reversefootnote" role="doc-backlink">&#8617;</a> <a href="#fnref:gae:1" class="reversefootnote" role="doc-backlink">&#8617;<sup>2</sup></a></p> </li> <li id="fn:dpo"> <p>Rafailov et al. (2023). Direct Preference Optimization: Your Language Model is Secretly a Reward Model. <a href="https://arxiv.org/abs/2305.18290">arXiv:2305.18290</a> <a href="#fnref:dpo" class="reversefootnote" role="doc-backlink">&#8617;</a> <a href="#fnref:dpo:1" class="reversefootnote" role="doc-backlink">&#8617;<sup>2</sup></a> <a href="#fnref:dpo:2" class="reversefootnote" role="doc-backlink">&#8617;<sup>3</sup></a> <a href="#fnref:dpo:3" class="reversefootnote" role="doc-backlink">&#8617;<sup>4</sup></a></p> </li> <li id="fn:r1"> <p>Guo et al. (2025). DeepSeek-R1: Incentivizing Reasoning Capability in LLMs via Reinforcement Learning. <a href="https://arxiv.org/abs/2501.12948">arXiv:2501.12948</a> <a href="#fnref:r1" class="reversefootnote" role="doc-backlink">&#8617;</a> <a href="#fnref:r1:1" class="reversefootnote" role="doc-backlink">&#8617;<sup>2</sup></a></p> </li> <li id="fn:grpo"> <p>Shao et al. (2024). DeepSeekMath: Pushing the Limits of Mathematical Reasoning in Open Language Models. <a href="https://arxiv.org/abs/2402.03300">arXiv:2402.03300</a> <a href="#fnref:grpo" class="reversefootnote" role="doc-backlink">&#8617;</a> <a href="#fnref:grpo:1" class="reversefootnote" role="doc-backlink">&#8617;<sup>2</sup></a></p> </li> </ol> </div>]]></content><author><name>Boxun Xu</name></author><category term="reasoning"/><category term="agentic-rl"/><summary type="html"><![CDATA[A component-by-component decomposition of PPO through GRPO, verifiers, and retrieved-token masking to a complete instance of Agentic RL.]]></summary></entry></feed>